schema: tc.fitness/check-contract/v1
check: core:shellcheck_disable_with_reason
config: {"roots":["src"]}
cases:
  - id: compliant
    fixture: compliant
    expected:
      status: pass
      exit: zero
      findings: []
  - id: violation
    fixture: violation
    expected:
      status: fail
      exit: nonzero
      findings:
        - rule: shellcheck-disable-with-reason
          path: src/example.sh
          message_contains: "explains WHY"
dependencies: []
