schema: tc.fitness/check-contract/v1
check: core:bicep_arm_lint
config:
  roots: [infra]
cases:
  - id: compliant
    fixture: compliant
    expected: {status: pass, exit: zero, findings: []}
  - id: violation
    fixture: violation
    expected:
      status: fail
      exit: nonzero
      findings:
        - rule: bicep-arm-lint
          path: infra/main.bicep
          message_contains: empty-literal
dependencies: []
