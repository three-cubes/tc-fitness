schema: tc.fitness/check-contract/v1
check: core:no_logging_secrets
config: {"roots":["src"]}
cases:
  - id: compliant
    fixture: compliant
    expected:
      status: pass
      exit: zero
      findings: []
  - id: violation
    fixture: violation
    expected:
      status: fail
      exit: nonzero
      findings:
        - rule: no-logging-secrets
          path: src/example.py
          message_contains: "secret-named value"
dependencies: []
