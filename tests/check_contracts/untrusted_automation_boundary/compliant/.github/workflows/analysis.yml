name: Analysis
on: workflow_dispatch
permissions:
  contents: read
jobs:
  analyse:
    runs-on: ubuntu-latest
    steps:
      - uses: example/analyse@v1
        with:
          contract: runtime/contracts/analysis.md
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: azure/login@v2
