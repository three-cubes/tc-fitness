schema: tc.fitness/check-contract/v1
check: core:coverage_includes_branches
config:
  coverage_report: reports/branch-coverage.xml
cases:
- id: compliant
  fixture: compliant
  expected:
    status: pass
    exit: zero
    findings: []
- id: violation
  fixture: violation
  expected:
    status: fail
    exit: nonzero
    findings:
    - rule: coverage-includes-branches
      path: reports/branch-coverage.xml
      message_contains: enable branch coverage
dependencies: []
