schema: tc.fitness/check-contract/v1
check: core:no_duplicate_string
config: {"roots":["src"]}
cases:
  - id: compliant
    fixture: compliant
    expected:
      status: pass
      exit: zero
      findings: []
  - id: violation
    fixture: violation
    expected:
      status: fail
      exit: nonzero
      findings:
        - rule: no-duplicate-string
          path: src/example.py
          message_contains: "single edit site"
dependencies: []
