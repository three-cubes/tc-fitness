schema: tc.fitness/check-contract/v1
check: core:suppressions_have_rationale
config: {"roots":["src"]}
cases:
  - id: compliant
    fixture: compliant
    expected:
      status: pass
      exit: zero
      findings: []
  - id: violation
    fixture: violation
    expected:
      status: fail
      exit: nonzero
      findings:
        - rule: suppressions-have-rationale
          path: src/example.py
          message_contains: "same-line rationale"
dependencies: []
