schema: tc.fitness/check-contract/v1
check: core:no_llm_attribution
config: {"roots":["src"]}
cases:
  - id: compliant
    fixture: compliant
    expected:
      status: pass
      exit: zero
      findings: []
  - id: violation
    fixture: violation
    expected:
      status: fail
      exit: nonzero
      findings:
        - rule: no-llm-attribution
          path: src/authorship.txt
          message_contains: "remove the AI/LLM self-attribution"
dependencies: []
