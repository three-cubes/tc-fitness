schema: tc.fitness/check-contract/v1
check: core:no_noop_test_scripts
config: {}
cases:
  - id: compliant
    fixture: compliant
    expected:
      status: pass
      exit: zero
      findings: []
  - id: violation
    fixture: violation
    expected:
      status: fail
      exit: nonzero
      findings:
        - rule: no-noop-test-scripts
          path: package.json
          message_contains: "placeholder command"
dependencies: []
