name: CI
on: push
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - run: pytest
  gate:
    name: Quality gate
    needs: [tests]
    runs-on: ubuntu-latest
    steps:
      - run: echo success
