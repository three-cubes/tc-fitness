schema: tc.fitness/check-contract/v1
check: core:no_test_doubles_in_runtime_tiers
config: {"roots":["src"],"runtime_markers":["integration"]}
cases:
  - id: compliant
    fixture: compliant
    expected:
      status: pass
      exit: zero
      findings: []
  - id: violation
    fixture: violation
    expected:
      status: fail
      exit: nonzero
      findings:
        - rule: no-test-doubles-in-runtime-tiers
          path: src/example.py
          message_contains: "replace the double"
dependencies: []
