schema: tc.fitness/check-contract/v1
check: core:pattern_chokepoint
config: {"roots":["src"],"patterns":["default_access_mode\\s*="]}
cases:
  - id: compliant
    fixture: compliant
    expected:
      status: pass
      exit: zero
      findings: []
  - id: violation
    fixture: violation
    expected:
      status: fail
      exit: nonzero
      findings:
        - rule: pattern-chokepoint
          path: src/example.py
          message_contains: "single chokepoint"
dependencies: []
