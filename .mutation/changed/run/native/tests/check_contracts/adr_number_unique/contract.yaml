schema: tc.fitness/check-contract/v1
check: core:adr_number_unique
config:
  record_dir: docs/decisions
cases:
- id: compliant
  fixture: compliant
  expected:
    status: pass
    exit: zero
    findings: []
- id: violation
  fixture: violation
  expected:
    status: fail
    exit: nonzero
    findings:
    - rule: adr-number-unique
      path: docs/decisions/ADR-001-first.md
      message_contains: renumber the newer file
    - rule: adr-number-unique
      path: docs/decisions/ADR-001-second.md
      message_contains: renumber the newer file
dependencies: []
