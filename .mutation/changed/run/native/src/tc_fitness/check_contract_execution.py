"""Execute a contract case through the public gate's existing CORE dispatcher."""

from __future__ import annotations

import hashlib
import json
import os
import re
import shutil
import subprocess
from collections.abc import Iterator
from contextlib import contextmanager
from dataclasses import asdict
from datetime import UTC, datetime
from importlib.metadata import version
from pathlib import Path
from tempfile import TemporaryDirectory
from typing import Any
from uuid import UUID, uuid4

from tc_fitness.catalogue import RuleEntry
from tc_fitness.check_contract_policy import validate_contract_configuration
from tc_fitness.check_contracts import (
    CheckContractError,
    FindingExpectation,
    GitCaseEnvironment,
    load_check_contract,
)
from tc_fitness.check_evidence import CheckEvidence, CheckResult, capture_check_evidence
from tc_fitness.core_checks import CORE_CHECKS
from tc_fitness.runner import run
from tc_fitness.runner import run_contract_case as run_contract_case

LEDGER_SCHEMA = "tc.fitness/check-ledger/v1"


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_payload_digest__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_payload_digest__mutmut)
def payload_digest(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_orig(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_1(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = None
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_2(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(None, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_3(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=None, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_4(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=None, ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_5(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=None, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_6(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=None)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_7(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_8(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_9(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_10(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_11(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, )
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_12(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=False, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_13(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=("XX,XX", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_14(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", "XX:XX"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_15(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=True, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_16(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=True)
    return "sha256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_17(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" - hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_18(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "XXsha256:XX" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_19(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "SHA256:" + hashlib.sha256(encoded.encode("utf-8")).hexdigest()


def x_payload_digest__mutmut_20(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(None).hexdigest()


def x_payload_digest__mutmut_21(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode(None)).hexdigest()


def x_payload_digest__mutmut_22(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("XXutf-8XX")).hexdigest()


def x_payload_digest__mutmut_23(value: object) -> str:
    """SHA-256 of canonical UTF-8 JSON; the digest field is excluded by callers."""
    encoded = json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
    return "sha256:" + hashlib.sha256(encoded.encode("UTF-8")).hexdigest()

mutants_x_payload_digest__mutmut['_mutmut_orig'] = x_payload_digest__mutmut_orig # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_1'] = x_payload_digest__mutmut_1 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_2'] = x_payload_digest__mutmut_2 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_3'] = x_payload_digest__mutmut_3 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_4'] = x_payload_digest__mutmut_4 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_5'] = x_payload_digest__mutmut_5 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_6'] = x_payload_digest__mutmut_6 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_7'] = x_payload_digest__mutmut_7 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_8'] = x_payload_digest__mutmut_8 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_9'] = x_payload_digest__mutmut_9 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_10'] = x_payload_digest__mutmut_10 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_11'] = x_payload_digest__mutmut_11 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_12'] = x_payload_digest__mutmut_12 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_13'] = x_payload_digest__mutmut_13 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_14'] = x_payload_digest__mutmut_14 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_15'] = x_payload_digest__mutmut_15 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_16'] = x_payload_digest__mutmut_16 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_17'] = x_payload_digest__mutmut_17 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_18'] = x_payload_digest__mutmut_18 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_19'] = x_payload_digest__mutmut_19 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_20'] = x_payload_digest__mutmut_20 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_21'] = x_payload_digest__mutmut_21 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_22'] = x_payload_digest__mutmut_22 # type: ignore # mutmut generated
mutants_x_payload_digest__mutmut['x_payload_digest__mutmut_23'] = x_payload_digest__mutmut_23 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_tree_digest__mutmut)
def tree_digest(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_orig(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_1(root: Path, *, ignore_caches: bool = True, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_2(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = True) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_3(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = None
    return payload_digest(files)


def x_tree_digest__mutmut_4(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(None).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_5(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "XXcontentXX": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_6(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "CONTENT": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_7(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(None).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_8(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "XXmodeXX": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_9(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "MODE": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_10(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode | 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_11(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 512,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_12(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(None)
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_13(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob(None))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_14(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("XX*XX"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_15(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_16(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches or "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_17(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "XX__pycache__XX" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_18(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__PYCACHE__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_19(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" not in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_20(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(None).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_21(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_22(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file() or path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_23(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta") or path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_24(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results or path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_25(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(None)
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_26(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith("XX.py.metaXX")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_27(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".PY.META")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_28(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix(None).is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_29(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("XXXX").is_file()
        )
    }
    return payload_digest(files)


def x_tree_digest__mutmut_30(root: Path, *, ignore_caches: bool = False, ignore_native_results: bool = False) -> str:
    """Bind names, bytes, file permissions and empty directory presence."""
    files = {
        path.relative_to(root).as_posix(): {
            "content": hashlib.sha256(path.read_bytes()).hexdigest() if path.is_file() else None,
            "mode": path.stat().st_mode & 0o777,
        }
        for path in sorted(root.rglob("*"))
        if not (ignore_caches and "__pycache__" in path.relative_to(root).parts)
        if not (
            ignore_native_results
            and path.name.endswith(".py.meta")
            and path.is_file()
            and path.with_suffix("").is_file()
        )
    }
    return payload_digest(None)

mutants_x_tree_digest__mutmut['_mutmut_orig'] = x_tree_digest__mutmut_orig # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_1'] = x_tree_digest__mutmut_1 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_2'] = x_tree_digest__mutmut_2 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_3'] = x_tree_digest__mutmut_3 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_4'] = x_tree_digest__mutmut_4 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_5'] = x_tree_digest__mutmut_5 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_6'] = x_tree_digest__mutmut_6 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_7'] = x_tree_digest__mutmut_7 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_8'] = x_tree_digest__mutmut_8 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_9'] = x_tree_digest__mutmut_9 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_10'] = x_tree_digest__mutmut_10 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_11'] = x_tree_digest__mutmut_11 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_12'] = x_tree_digest__mutmut_12 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_13'] = x_tree_digest__mutmut_13 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_14'] = x_tree_digest__mutmut_14 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_15'] = x_tree_digest__mutmut_15 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_16'] = x_tree_digest__mutmut_16 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_17'] = x_tree_digest__mutmut_17 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_18'] = x_tree_digest__mutmut_18 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_19'] = x_tree_digest__mutmut_19 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_20'] = x_tree_digest__mutmut_20 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_21'] = x_tree_digest__mutmut_21 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_22'] = x_tree_digest__mutmut_22 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_23'] = x_tree_digest__mutmut_23 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_24'] = x_tree_digest__mutmut_24 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_25'] = x_tree_digest__mutmut_25 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_26'] = x_tree_digest__mutmut_26 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_27'] = x_tree_digest__mutmut_27 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_28'] = x_tree_digest__mutmut_28 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_29'] = x_tree_digest__mutmut_29 # type: ignore # mutmut generated
mutants_x_tree_digest__mutmut['x_tree_digest__mutmut_30'] = x_tree_digest__mutmut_30 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_candidate_identity__mutmut)
def candidate_identity() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_orig() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_1() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "XXpackage_versionXX": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_2() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "PACKAGE_VERSION": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_3() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version(None),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_4() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("XXthree-cubes-fitnessXX"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_5() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("THREE-CUBES-FITNESS"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_6() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "XXsource_digestXX": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_7() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "SOURCE_DIGEST": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_8() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(None, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_9() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=None, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_10() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=None),
    }


def x_candidate_identity__mutmut_11() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_12() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_13() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ),
    }


def x_candidate_identity__mutmut_14() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(None).parent, ignore_caches=True, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_15() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=False, ignore_native_results=True),
    }


def x_candidate_identity__mutmut_16() -> dict[str, str]:
    """Identity of the executing package, including uncommitted source changes."""
    return {
        "package_version": version("three-cubes-fitness"),
        # Native worker result sidecars change during execution, not candidate inputs.
        "source_digest": tree_digest(Path(__file__).parent, ignore_caches=True, ignore_native_results=False),
    }

mutants_x_candidate_identity__mutmut['_mutmut_orig'] = x_candidate_identity__mutmut_orig # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_1'] = x_candidate_identity__mutmut_1 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_2'] = x_candidate_identity__mutmut_2 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_3'] = x_candidate_identity__mutmut_3 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_4'] = x_candidate_identity__mutmut_4 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_5'] = x_candidate_identity__mutmut_5 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_6'] = x_candidate_identity__mutmut_6 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_7'] = x_candidate_identity__mutmut_7 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_8'] = x_candidate_identity__mutmut_8 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_9'] = x_candidate_identity__mutmut_9 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_10'] = x_candidate_identity__mutmut_10 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_11'] = x_candidate_identity__mutmut_11 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_12'] = x_candidate_identity__mutmut_12 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_13'] = x_candidate_identity__mutmut_13 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_14'] = x_candidate_identity__mutmut_14 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_15'] = x_candidate_identity__mutmut_15 # type: ignore # mutmut generated
mutants_x_candidate_identity__mutmut['x_candidate_identity__mutmut_16'] = x_candidate_identity__mutmut_16 # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_copy_verified_fixture__mutmut)
def copy_verified_fixture(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(fixture, destination)
    if tree_digest(destination) != expected_digest:
        raise CheckContractError("fixture changed while copying the execution snapshot")


def x_copy_verified_fixture__mutmut_orig(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(fixture, destination)
    if tree_digest(destination) != expected_digest:
        raise CheckContractError("fixture changed while copying the execution snapshot")


def x_copy_verified_fixture__mutmut_1(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(None, destination)
    if tree_digest(destination) != expected_digest:
        raise CheckContractError("fixture changed while copying the execution snapshot")


def x_copy_verified_fixture__mutmut_2(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(fixture, None)
    if tree_digest(destination) != expected_digest:
        raise CheckContractError("fixture changed while copying the execution snapshot")


def x_copy_verified_fixture__mutmut_3(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(destination)
    if tree_digest(destination) != expected_digest:
        raise CheckContractError("fixture changed while copying the execution snapshot")


def x_copy_verified_fixture__mutmut_4(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(fixture, )
    if tree_digest(destination) != expected_digest:
        raise CheckContractError("fixture changed while copying the execution snapshot")


def x_copy_verified_fixture__mutmut_5(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(fixture, destination)
    if tree_digest(None) != expected_digest:
        raise CheckContractError("fixture changed while copying the execution snapshot")


def x_copy_verified_fixture__mutmut_6(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(fixture, destination)
    if tree_digest(destination) == expected_digest:
        raise CheckContractError("fixture changed while copying the execution snapshot")


def x_copy_verified_fixture__mutmut_7(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(fixture, destination)
    if tree_digest(destination) != expected_digest:
        raise CheckContractError(None)


def x_copy_verified_fixture__mutmut_8(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(fixture, destination)
    if tree_digest(destination) != expected_digest:
        raise CheckContractError("XXfixture changed while copying the execution snapshotXX")


def x_copy_verified_fixture__mutmut_9(fixture: Path, destination: Path, expected_digest: str) -> None:
    """Copy a fixture and reject a snapshot that differs from its bound digest."""
    shutil.copytree(fixture, destination)
    if tree_digest(destination) != expected_digest:
        raise CheckContractError("FIXTURE CHANGED WHILE COPYING THE EXECUTION SNAPSHOT")

mutants_x_copy_verified_fixture__mutmut['_mutmut_orig'] = x_copy_verified_fixture__mutmut_orig # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut['x_copy_verified_fixture__mutmut_1'] = x_copy_verified_fixture__mutmut_1 # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut['x_copy_verified_fixture__mutmut_2'] = x_copy_verified_fixture__mutmut_2 # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut['x_copy_verified_fixture__mutmut_3'] = x_copy_verified_fixture__mutmut_3 # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut['x_copy_verified_fixture__mutmut_4'] = x_copy_verified_fixture__mutmut_4 # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut['x_copy_verified_fixture__mutmut_5'] = x_copy_verified_fixture__mutmut_5 # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut['x_copy_verified_fixture__mutmut_6'] = x_copy_verified_fixture__mutmut_6 # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut['x_copy_verified_fixture__mutmut_7'] = x_copy_verified_fixture__mutmut_7 # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut['x_copy_verified_fixture__mutmut_8'] = x_copy_verified_fixture__mutmut_8 # type: ignore # mutmut generated
mutants_x_copy_verified_fixture__mutmut['x_copy_verified_fixture__mutmut_9'] = x_copy_verified_fixture__mutmut_9 # type: ignore # mutmut generated
mutants_x_terminal_check_result__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_terminal_check_result__mutmut)
def terminal_check_result(evidence: CheckEvidence) -> CheckResult:
    """Return the one terminal result required by a contract execution."""
    if len(evidence.results) != 1:
        raise CheckContractError("missing or multiple terminal check results")
    return evidence.results[0]


def x_terminal_check_result__mutmut_orig(evidence: CheckEvidence) -> CheckResult:
    """Return the one terminal result required by a contract execution."""
    if len(evidence.results) != 1:
        raise CheckContractError("missing or multiple terminal check results")
    return evidence.results[0]


def x_terminal_check_result__mutmut_1(evidence: CheckEvidence) -> CheckResult:
    """Return the one terminal result required by a contract execution."""
    if len(evidence.results) == 1:
        raise CheckContractError("missing or multiple terminal check results")
    return evidence.results[0]


def x_terminal_check_result__mutmut_2(evidence: CheckEvidence) -> CheckResult:
    """Return the one terminal result required by a contract execution."""
    if len(evidence.results) != 2:
        raise CheckContractError("missing or multiple terminal check results")
    return evidence.results[0]


def x_terminal_check_result__mutmut_3(evidence: CheckEvidence) -> CheckResult:
    """Return the one terminal result required by a contract execution."""
    if len(evidence.results) != 1:
        raise CheckContractError(None)
    return evidence.results[0]


def x_terminal_check_result__mutmut_4(evidence: CheckEvidence) -> CheckResult:
    """Return the one terminal result required by a contract execution."""
    if len(evidence.results) != 1:
        raise CheckContractError("XXmissing or multiple terminal check resultsXX")
    return evidence.results[0]


def x_terminal_check_result__mutmut_5(evidence: CheckEvidence) -> CheckResult:
    """Return the one terminal result required by a contract execution."""
    if len(evidence.results) != 1:
        raise CheckContractError("MISSING OR MULTIPLE TERMINAL CHECK RESULTS")
    return evidence.results[0]


def x_terminal_check_result__mutmut_6(evidence: CheckEvidence) -> CheckResult:
    """Return the one terminal result required by a contract execution."""
    if len(evidence.results) != 1:
        raise CheckContractError("missing or multiple terminal check results")
    return evidence.results[1]

mutants_x_terminal_check_result__mutmut['_mutmut_orig'] = x_terminal_check_result__mutmut_orig # type: ignore # mutmut generated
mutants_x_terminal_check_result__mutmut['x_terminal_check_result__mutmut_1'] = x_terminal_check_result__mutmut_1 # type: ignore # mutmut generated
mutants_x_terminal_check_result__mutmut['x_terminal_check_result__mutmut_2'] = x_terminal_check_result__mutmut_2 # type: ignore # mutmut generated
mutants_x_terminal_check_result__mutmut['x_terminal_check_result__mutmut_3'] = x_terminal_check_result__mutmut_3 # type: ignore # mutmut generated
mutants_x_terminal_check_result__mutmut['x_terminal_check_result__mutmut_4'] = x_terminal_check_result__mutmut_4 # type: ignore # mutmut generated
mutants_x_terminal_check_result__mutmut['x_terminal_check_result__mutmut_5'] = x_terminal_check_result__mutmut_5 # type: ignore # mutmut generated
mutants_x_terminal_check_result__mutmut['x_terminal_check_result__mutmut_6'] = x_terminal_check_result__mutmut_6 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_execute_contract_case__mutmut)
def execute_contract_case(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_orig(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_1(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = None
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_2(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = None
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_3(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(None, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_4(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=None)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_5(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_6(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, )
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_7(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(None)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_8(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_9(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(None)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_10(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(None, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_11(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, None)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_12(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_13(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, )
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_14(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = None
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_15(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next(None, None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_16(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next(None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_17(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), )
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_18(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id != case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_19(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is not None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_20(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(None)
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_21(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = None
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_22(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(None, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_23(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, None)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_24(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_25(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, )
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_26(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(None):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_27(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError(None)
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_28(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("XXledger must be outside the input fixtureXX")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_29(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("LEDGER MUST BE OUTSIDE THE INPUT FIXTURE")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_30(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError(None)
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_31(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("XXledger already exists; retain it and select a new output for the retryXX")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_32(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("LEDGER ALREADY EXISTS; RETAIN IT AND SELECT A NEW OUTPUT FOR THE RETRY")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_33(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = None
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_34(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(None)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_35(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(None) from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_36(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = None
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_37(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = None
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_38(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(None).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_39(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix=None) as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_40(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="XXtc-fitness-contract-XX") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_41(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="TC-FITNESS-CONTRACT-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_42(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = None
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_43(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) * "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_44(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(None) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_45(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "XXrepoXX"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_46(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "REPO"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_47(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(None, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_48(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, None, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_49(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, None)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_50(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_51(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_52(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, )
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_53(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(None, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_54(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, None)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_55(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_56(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, )
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_57(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(None, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_58(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, None),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_59(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_60(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, ),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_61(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(None)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_62(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = None
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_63(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=None, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_64(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=None, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_65(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=None)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_66(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_67(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_68(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, )
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_69(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                None,
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_70(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=None,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_71(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs=None,
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_72(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_73(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_74(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_75(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix(None): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_76(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removesuffix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_77(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("XXcore:XX"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_78(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("CORE:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_79(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() == manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_80(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError(None)
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_81(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("XXmanifest changed during executionXX")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_82(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("MANIFEST CHANGED DURING EXECUTION")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_83(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(None) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_84(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(None, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_85(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, None)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_86(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_87(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, )) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_88(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) == fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_89(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError(None)
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_90(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("XXoriginal fixture changed during executionXX")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_91(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("ORIGINAL FIXTURE CHANGED DURING EXECUTION")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_92(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() == candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_93(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError(None)
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_94(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("XXcandidate source changed during executionXX")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_95(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("CANDIDATE SOURCE CHANGED DURING EXECUTION")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_96(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = None
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_97(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(None)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_98(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = None
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_99(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 3 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_100(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status != "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_101(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "XXerrorXX" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_102(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "ERROR" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_103(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = None
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_104(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "XXschemaXX": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_105(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "SCHEMA": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_106(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "XXcheckXX": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_107(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "CHECK": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_108(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "XXcase_idXX": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_109(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "CASE_ID": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_110(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "XXcontract_digestXX": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_111(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "CONTRACT_DIGEST": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_112(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" - hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_113(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "XXsha256:XX" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_114(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "SHA256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_115(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(None).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_116(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "XXcase_digestXX": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_117(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "CASE_DIGEST": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_118(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(None),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_119(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(None)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_120(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "XXfixture_digestXX": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_121(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "FIXTURE_DIGEST": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_122(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "XXevidence_classXX": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_123(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "EVIDENCE_CLASS": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_124(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "XXlive_qualificationXX": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_125(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "LIVE_QUALIFICATION": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_126(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "XXrelease_admissionXX": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_127(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "RELEASE_ADMISSION": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_128(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "XXcandidateXX": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_129(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "CANDIDATE": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_130(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "XXexecution_idXX": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_131(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "EXECUTION_ID": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_132(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(None),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_133(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "XXstarted_atXX": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_134(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "STARTED_AT": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_135(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "XXfinished_atXX": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_136(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "FINISHED_AT": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_137(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(None).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_138(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "XXexpectedXX": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_139(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "EXPECTED": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_140(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(None),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_141(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "XXenvironmentXX": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_142(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "ENVIRONMENT": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_143(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(None),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_144(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "XXactualXX": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_145(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "ACTUAL": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_146(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "XXstatusXX": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_147(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "STATUS": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_148(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "XXexit_codeXX": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_149(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "EXIT_CODE": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_150(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "XXexitXX": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_151(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "EXIT": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_152(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "XXzeroXX" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_153(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "ZERO" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_154(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code != 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_155(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 1 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_156(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "XXnonzeroXX",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_157(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "NONZERO",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_158(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "XXfindingsXX": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_159(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "FINDINGS": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_160(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(None) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_161(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = None
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_162(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["XXpayload_digestXX"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_163(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["PAYLOAD_DIGEST"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_164(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(None)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_165(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open(None, encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_166(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding=None) as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_167(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open(encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_168(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", ) as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_169(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("XXxXX", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_170(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("X", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_171(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="XXutf-8XX") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_172(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="UTF-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_173(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(None)
        return exit_code


def x_execute_contract_case__mutmut_174(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) - "\n")
        return exit_code


def x_execute_contract_case__mutmut_175(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(None, sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_176(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=None, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_177(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=None) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_178(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(sort_keys=True, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_179(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_180(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, ) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_181(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=False, indent=2) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_182(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=3) + "\n")
        return exit_code


def x_execute_contract_case__mutmut_183(manifest: Path, case_id: str, ledger: Path) -> int:
    """Run one case and retain its terminal evidence; return its actual exit."""
    manifest_bytes = manifest.read_bytes()
    contract = load_check_contract(manifest, source=manifest_bytes)
    _portable_configuration(contract.config)
    if contract.check not in CORE_CHECKS:
        raise CheckContractError(f"unregistered CORE check: {contract.check}")
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    fixture = _fixture_path(manifest, case.fixture)
    if ledger.resolve().is_relative_to(fixture.resolve()):
        raise CheckContractError("ledger must be outside the input fixture")
    if ledger.exists():
        raise CheckContractError("ledger already exists; retain it and select a new output for the retry")
    try:
        fixture_digest = tree_digest(fixture)
    except OSError as exc:
        raise CheckContractError(f"cannot read contract fixture: {exc}") from exc
    candidate = candidate_identity()
    started = datetime.now(UTC).isoformat()
    with TemporaryDirectory(prefix="tc-fitness-contract-") as temporary:
        repo = Path(temporary) / "repo"
        copy_verified_fixture(fixture, repo, fixture_digest)
        _materialize_git_fixture(repo, case.environment)
        with (
            _case_environment(case.environment.path, Path(temporary)),
            capture_check_evidence() as evidence,
        ):
            entry = RuleEntry(id=contract.check, gate=contract.check, check=contract.check)
            run(
                (entry,),
                repo_root=repo,
                core_check_configs={contract.check.removeprefix("core:"): contract.config},
            )
        if manifest.read_bytes() != manifest_bytes:
            raise CheckContractError("manifest changed during execution")
        if tree_digest(_fixture_path(manifest, case.fixture)) != fixture_digest:
            raise CheckContractError("original fixture changed during execution")
        if candidate_identity() != candidate:
            raise CheckContractError("candidate source changed during execution")
        result = terminal_check_result(evidence)
        exit_code = 2 if result.status == "error" else result.exit_code
        payload: dict[str, Any] = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "case_digest": payload_digest(asdict(case)),
            "fixture_digest": fixture_digest,
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate,
            "execution_id": str(uuid4()),
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "expected": asdict(case.expected),
            "environment": asdict(case.environment),
            "actual": {
                "status": result.status,
                "exit_code": exit_code,
                "exit": "zero" if exit_code == 0 else "nonzero",
                "findings": [asdict(finding) for finding in evidence.findings],
            },
        }
        payload["payload_digest"] = payload_digest(payload)
        with ledger.open("x", encoding="utf-8") as output:
            output.write(json.dumps(payload, sort_keys=True, indent=2) + "XX\nXX")
        return exit_code

mutants_x_execute_contract_case__mutmut['_mutmut_orig'] = x_execute_contract_case__mutmut_orig # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_1'] = x_execute_contract_case__mutmut_1 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_2'] = x_execute_contract_case__mutmut_2 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_3'] = x_execute_contract_case__mutmut_3 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_4'] = x_execute_contract_case__mutmut_4 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_5'] = x_execute_contract_case__mutmut_5 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_6'] = x_execute_contract_case__mutmut_6 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_7'] = x_execute_contract_case__mutmut_7 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_8'] = x_execute_contract_case__mutmut_8 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_9'] = x_execute_contract_case__mutmut_9 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_10'] = x_execute_contract_case__mutmut_10 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_11'] = x_execute_contract_case__mutmut_11 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_12'] = x_execute_contract_case__mutmut_12 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_13'] = x_execute_contract_case__mutmut_13 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_14'] = x_execute_contract_case__mutmut_14 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_15'] = x_execute_contract_case__mutmut_15 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_16'] = x_execute_contract_case__mutmut_16 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_17'] = x_execute_contract_case__mutmut_17 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_18'] = x_execute_contract_case__mutmut_18 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_19'] = x_execute_contract_case__mutmut_19 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_20'] = x_execute_contract_case__mutmut_20 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_21'] = x_execute_contract_case__mutmut_21 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_22'] = x_execute_contract_case__mutmut_22 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_23'] = x_execute_contract_case__mutmut_23 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_24'] = x_execute_contract_case__mutmut_24 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_25'] = x_execute_contract_case__mutmut_25 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_26'] = x_execute_contract_case__mutmut_26 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_27'] = x_execute_contract_case__mutmut_27 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_28'] = x_execute_contract_case__mutmut_28 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_29'] = x_execute_contract_case__mutmut_29 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_30'] = x_execute_contract_case__mutmut_30 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_31'] = x_execute_contract_case__mutmut_31 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_32'] = x_execute_contract_case__mutmut_32 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_33'] = x_execute_contract_case__mutmut_33 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_34'] = x_execute_contract_case__mutmut_34 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_35'] = x_execute_contract_case__mutmut_35 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_36'] = x_execute_contract_case__mutmut_36 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_37'] = x_execute_contract_case__mutmut_37 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_38'] = x_execute_contract_case__mutmut_38 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_39'] = x_execute_contract_case__mutmut_39 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_40'] = x_execute_contract_case__mutmut_40 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_41'] = x_execute_contract_case__mutmut_41 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_42'] = x_execute_contract_case__mutmut_42 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_43'] = x_execute_contract_case__mutmut_43 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_44'] = x_execute_contract_case__mutmut_44 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_45'] = x_execute_contract_case__mutmut_45 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_46'] = x_execute_contract_case__mutmut_46 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_47'] = x_execute_contract_case__mutmut_47 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_48'] = x_execute_contract_case__mutmut_48 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_49'] = x_execute_contract_case__mutmut_49 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_50'] = x_execute_contract_case__mutmut_50 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_51'] = x_execute_contract_case__mutmut_51 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_52'] = x_execute_contract_case__mutmut_52 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_53'] = x_execute_contract_case__mutmut_53 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_54'] = x_execute_contract_case__mutmut_54 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_55'] = x_execute_contract_case__mutmut_55 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_56'] = x_execute_contract_case__mutmut_56 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_57'] = x_execute_contract_case__mutmut_57 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_58'] = x_execute_contract_case__mutmut_58 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_59'] = x_execute_contract_case__mutmut_59 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_60'] = x_execute_contract_case__mutmut_60 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_61'] = x_execute_contract_case__mutmut_61 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_62'] = x_execute_contract_case__mutmut_62 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_63'] = x_execute_contract_case__mutmut_63 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_64'] = x_execute_contract_case__mutmut_64 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_65'] = x_execute_contract_case__mutmut_65 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_66'] = x_execute_contract_case__mutmut_66 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_67'] = x_execute_contract_case__mutmut_67 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_68'] = x_execute_contract_case__mutmut_68 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_69'] = x_execute_contract_case__mutmut_69 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_70'] = x_execute_contract_case__mutmut_70 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_71'] = x_execute_contract_case__mutmut_71 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_72'] = x_execute_contract_case__mutmut_72 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_73'] = x_execute_contract_case__mutmut_73 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_74'] = x_execute_contract_case__mutmut_74 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_75'] = x_execute_contract_case__mutmut_75 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_76'] = x_execute_contract_case__mutmut_76 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_77'] = x_execute_contract_case__mutmut_77 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_78'] = x_execute_contract_case__mutmut_78 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_79'] = x_execute_contract_case__mutmut_79 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_80'] = x_execute_contract_case__mutmut_80 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_81'] = x_execute_contract_case__mutmut_81 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_82'] = x_execute_contract_case__mutmut_82 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_83'] = x_execute_contract_case__mutmut_83 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_84'] = x_execute_contract_case__mutmut_84 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_85'] = x_execute_contract_case__mutmut_85 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_86'] = x_execute_contract_case__mutmut_86 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_87'] = x_execute_contract_case__mutmut_87 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_88'] = x_execute_contract_case__mutmut_88 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_89'] = x_execute_contract_case__mutmut_89 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_90'] = x_execute_contract_case__mutmut_90 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_91'] = x_execute_contract_case__mutmut_91 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_92'] = x_execute_contract_case__mutmut_92 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_93'] = x_execute_contract_case__mutmut_93 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_94'] = x_execute_contract_case__mutmut_94 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_95'] = x_execute_contract_case__mutmut_95 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_96'] = x_execute_contract_case__mutmut_96 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_97'] = x_execute_contract_case__mutmut_97 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_98'] = x_execute_contract_case__mutmut_98 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_99'] = x_execute_contract_case__mutmut_99 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_100'] = x_execute_contract_case__mutmut_100 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_101'] = x_execute_contract_case__mutmut_101 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_102'] = x_execute_contract_case__mutmut_102 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_103'] = x_execute_contract_case__mutmut_103 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_104'] = x_execute_contract_case__mutmut_104 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_105'] = x_execute_contract_case__mutmut_105 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_106'] = x_execute_contract_case__mutmut_106 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_107'] = x_execute_contract_case__mutmut_107 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_108'] = x_execute_contract_case__mutmut_108 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_109'] = x_execute_contract_case__mutmut_109 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_110'] = x_execute_contract_case__mutmut_110 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_111'] = x_execute_contract_case__mutmut_111 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_112'] = x_execute_contract_case__mutmut_112 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_113'] = x_execute_contract_case__mutmut_113 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_114'] = x_execute_contract_case__mutmut_114 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_115'] = x_execute_contract_case__mutmut_115 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_116'] = x_execute_contract_case__mutmut_116 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_117'] = x_execute_contract_case__mutmut_117 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_118'] = x_execute_contract_case__mutmut_118 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_119'] = x_execute_contract_case__mutmut_119 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_120'] = x_execute_contract_case__mutmut_120 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_121'] = x_execute_contract_case__mutmut_121 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_122'] = x_execute_contract_case__mutmut_122 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_123'] = x_execute_contract_case__mutmut_123 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_124'] = x_execute_contract_case__mutmut_124 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_125'] = x_execute_contract_case__mutmut_125 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_126'] = x_execute_contract_case__mutmut_126 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_127'] = x_execute_contract_case__mutmut_127 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_128'] = x_execute_contract_case__mutmut_128 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_129'] = x_execute_contract_case__mutmut_129 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_130'] = x_execute_contract_case__mutmut_130 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_131'] = x_execute_contract_case__mutmut_131 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_132'] = x_execute_contract_case__mutmut_132 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_133'] = x_execute_contract_case__mutmut_133 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_134'] = x_execute_contract_case__mutmut_134 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_135'] = x_execute_contract_case__mutmut_135 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_136'] = x_execute_contract_case__mutmut_136 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_137'] = x_execute_contract_case__mutmut_137 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_138'] = x_execute_contract_case__mutmut_138 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_139'] = x_execute_contract_case__mutmut_139 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_140'] = x_execute_contract_case__mutmut_140 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_141'] = x_execute_contract_case__mutmut_141 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_142'] = x_execute_contract_case__mutmut_142 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_143'] = x_execute_contract_case__mutmut_143 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_144'] = x_execute_contract_case__mutmut_144 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_145'] = x_execute_contract_case__mutmut_145 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_146'] = x_execute_contract_case__mutmut_146 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_147'] = x_execute_contract_case__mutmut_147 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_148'] = x_execute_contract_case__mutmut_148 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_149'] = x_execute_contract_case__mutmut_149 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_150'] = x_execute_contract_case__mutmut_150 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_151'] = x_execute_contract_case__mutmut_151 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_152'] = x_execute_contract_case__mutmut_152 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_153'] = x_execute_contract_case__mutmut_153 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_154'] = x_execute_contract_case__mutmut_154 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_155'] = x_execute_contract_case__mutmut_155 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_156'] = x_execute_contract_case__mutmut_156 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_157'] = x_execute_contract_case__mutmut_157 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_158'] = x_execute_contract_case__mutmut_158 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_159'] = x_execute_contract_case__mutmut_159 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_160'] = x_execute_contract_case__mutmut_160 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_161'] = x_execute_contract_case__mutmut_161 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_162'] = x_execute_contract_case__mutmut_162 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_163'] = x_execute_contract_case__mutmut_163 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_164'] = x_execute_contract_case__mutmut_164 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_165'] = x_execute_contract_case__mutmut_165 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_166'] = x_execute_contract_case__mutmut_166 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_167'] = x_execute_contract_case__mutmut_167 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_168'] = x_execute_contract_case__mutmut_168 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_169'] = x_execute_contract_case__mutmut_169 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_170'] = x_execute_contract_case__mutmut_170 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_171'] = x_execute_contract_case__mutmut_171 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_172'] = x_execute_contract_case__mutmut_172 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_173'] = x_execute_contract_case__mutmut_173 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_174'] = x_execute_contract_case__mutmut_174 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_175'] = x_execute_contract_case__mutmut_175 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_176'] = x_execute_contract_case__mutmut_176 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_177'] = x_execute_contract_case__mutmut_177 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_178'] = x_execute_contract_case__mutmut_178 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_179'] = x_execute_contract_case__mutmut_179 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_180'] = x_execute_contract_case__mutmut_180 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_181'] = x_execute_contract_case__mutmut_181 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_182'] = x_execute_contract_case__mutmut_182 # type: ignore # mutmut generated
mutants_x_execute_contract_case__mutmut['x_execute_contract_case__mutmut_183'] = x_execute_contract_case__mutmut_183 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__portable_configuration__mutmut)
def _portable_configuration(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_orig(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_1(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" or not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_2(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key != "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_3(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "XXnameXX" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_4(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "NAME" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_5(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_6(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(None, str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_7(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", None):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_8(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_9(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", ):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_10(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"XX[a-zA-Z0-9_-]+XX", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_11(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-za-z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_12(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[A-ZA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_13(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(None)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_14(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError(None)
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_15(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("XXcontract configuration cannot select an external rule nameXX")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_16(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("CONTRACT CONFIGURATION CANNOT SELECT AN EXTERNAL RULE NAME")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_17(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(None)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_18(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(None)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_19(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) or (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_20(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() and ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_21(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(None).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_22(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or "XX..XX" in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_23(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." not in Path(value).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_24(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(None).parts):
        raise CheckContractError("contract configuration paths must be portable and fixture-relative")


def x__portable_configuration__mutmut_25(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError(None)


def x__portable_configuration__mutmut_26(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("XXcontract configuration paths must be portable and fixture-relativeXX")


def x__portable_configuration__mutmut_27(value: object) -> None:
    """Keep configured inputs inside the fixture."""
    if isinstance(value, dict):
        for key, item in value.items():
            if key == "name" and not re.fullmatch(r"[a-zA-Z0-9_-]+", str(item)):
                raise CheckContractError("contract configuration cannot select an external rule name")
            _portable_configuration(item)
    elif isinstance(value, list):
        for item in value:
            _portable_configuration(item)
    elif isinstance(value, str) and (Path(value).is_absolute() or ".." in Path(value).parts):
        raise CheckContractError("CONTRACT CONFIGURATION PATHS MUST BE PORTABLE AND FIXTURE-RELATIVE")

mutants_x__portable_configuration__mutmut['_mutmut_orig'] = x__portable_configuration__mutmut_orig # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_1'] = x__portable_configuration__mutmut_1 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_2'] = x__portable_configuration__mutmut_2 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_3'] = x__portable_configuration__mutmut_3 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_4'] = x__portable_configuration__mutmut_4 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_5'] = x__portable_configuration__mutmut_5 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_6'] = x__portable_configuration__mutmut_6 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_7'] = x__portable_configuration__mutmut_7 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_8'] = x__portable_configuration__mutmut_8 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_9'] = x__portable_configuration__mutmut_9 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_10'] = x__portable_configuration__mutmut_10 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_11'] = x__portable_configuration__mutmut_11 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_12'] = x__portable_configuration__mutmut_12 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_13'] = x__portable_configuration__mutmut_13 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_14'] = x__portable_configuration__mutmut_14 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_15'] = x__portable_configuration__mutmut_15 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_16'] = x__portable_configuration__mutmut_16 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_17'] = x__portable_configuration__mutmut_17 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_18'] = x__portable_configuration__mutmut_18 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_19'] = x__portable_configuration__mutmut_19 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_20'] = x__portable_configuration__mutmut_20 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_21'] = x__portable_configuration__mutmut_21 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_22'] = x__portable_configuration__mutmut_22 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_23'] = x__portable_configuration__mutmut_23 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_24'] = x__portable_configuration__mutmut_24 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_25'] = x__portable_configuration__mutmut_25 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_26'] = x__portable_configuration__mutmut_26 # type: ignore # mutmut generated
mutants_x__portable_configuration__mutmut['x__portable_configuration__mutmut_27'] = x__portable_configuration__mutmut_27 # type: ignore # mutmut generated


_GIT_FIXTURE_TIMEOUT_SECONDS = 30
_GIT_FIXTURE_MAX_BYTES = 1024 * 1024
mutants_x__run_fixture_git__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__run_fixture_git__mutmut)
def _run_fixture_git(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_orig(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_1(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = None
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_2(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            None,  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_3(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=None,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_4(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=None,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_5(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=None,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_6(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=None,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_7(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=None,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_8(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env=None,
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_9(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_10(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_11(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_12(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_13(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_14(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_15(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_16(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["XXgitXX", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_17(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["GIT", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_18(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=False,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_19(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=True,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_20(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "XXGIT_CONFIG_NOSYSTEMXX": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_21(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "git_config_nosystem": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_22(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "XX1XX",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_23(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "XXGIT_CONFIG_GLOBALXX": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_24(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "git_config_global": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_25(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "XXGIT_TERMINAL_PROMPTXX": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_26(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "git_terminal_prompt": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_27(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "XX0XX",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_28(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "XXGCM_INTERACTIVEXX": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_29(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "gcm_interactive": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_30(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "XXNeverXX",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_31(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_32(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "NEVER",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_33(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(None) from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_34(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(None).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_35(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode == 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_36(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 1:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_37(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = None
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_38(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode(None, "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_39(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", None).strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_40(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_41(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", ).strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_42(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("XXutf-8XX", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_43(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("UTF-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_44(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "XXreplaceXX").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_45(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "REPLACE").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_46(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = None
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_47(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[+1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_48(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-2] if detail else f"git exited {result.returncode}"
        raise CheckContractError(f"cannot materialise Git contract fixture: {reason}")
    return result


def x__run_fixture_git__mutmut_49(
    repo: Path,
    arguments: list[str],
    *,
    input_bytes: bytes | None = None,
) -> subprocess.CompletedProcess[bytes]:
    """Run one bounded, non-interactive Git fixture operation."""
    try:
        # argv0 and every subcommand are internal constants; the only manifest
        # value reaching argv is the strictly validated refs/heads checkout.
        result = subprocess.run(  # noqa: S603
            ["git", *arguments],  # noqa: S607
            cwd=repo,
            input=input_bytes,
            capture_output=True,
            check=False,
            timeout=_GIT_FIXTURE_TIMEOUT_SECONDS,
            env={
                **os.environ,
                "GIT_CONFIG_NOSYSTEM": "1",
                "GIT_CONFIG_GLOBAL": os.devnull,
                "GIT_TERMINAL_PROMPT": "0",
                "GCM_INTERACTIVE": "Never",
            },
        )
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        raise CheckContractError(f"cannot materialise Git contract fixture: {type(exc).__name__}") from exc
    if result.returncode != 0:
        detail = result.stderr.decode("utf-8", "replace").strip().splitlines()
        reason = detail[-1] if detail else f"git exited {result.returncode}"
        raise CheckContractError(None)
    return result

mutants_x__run_fixture_git__mutmut['_mutmut_orig'] = x__run_fixture_git__mutmut_orig # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_1'] = x__run_fixture_git__mutmut_1 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_2'] = x__run_fixture_git__mutmut_2 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_3'] = x__run_fixture_git__mutmut_3 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_4'] = x__run_fixture_git__mutmut_4 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_5'] = x__run_fixture_git__mutmut_5 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_6'] = x__run_fixture_git__mutmut_6 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_7'] = x__run_fixture_git__mutmut_7 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_8'] = x__run_fixture_git__mutmut_8 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_9'] = x__run_fixture_git__mutmut_9 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_10'] = x__run_fixture_git__mutmut_10 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_11'] = x__run_fixture_git__mutmut_11 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_12'] = x__run_fixture_git__mutmut_12 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_13'] = x__run_fixture_git__mutmut_13 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_14'] = x__run_fixture_git__mutmut_14 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_15'] = x__run_fixture_git__mutmut_15 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_16'] = x__run_fixture_git__mutmut_16 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_17'] = x__run_fixture_git__mutmut_17 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_18'] = x__run_fixture_git__mutmut_18 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_19'] = x__run_fixture_git__mutmut_19 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_20'] = x__run_fixture_git__mutmut_20 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_21'] = x__run_fixture_git__mutmut_21 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_22'] = x__run_fixture_git__mutmut_22 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_23'] = x__run_fixture_git__mutmut_23 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_24'] = x__run_fixture_git__mutmut_24 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_25'] = x__run_fixture_git__mutmut_25 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_26'] = x__run_fixture_git__mutmut_26 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_27'] = x__run_fixture_git__mutmut_27 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_28'] = x__run_fixture_git__mutmut_28 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_29'] = x__run_fixture_git__mutmut_29 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_30'] = x__run_fixture_git__mutmut_30 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_31'] = x__run_fixture_git__mutmut_31 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_32'] = x__run_fixture_git__mutmut_32 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_33'] = x__run_fixture_git__mutmut_33 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_34'] = x__run_fixture_git__mutmut_34 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_35'] = x__run_fixture_git__mutmut_35 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_36'] = x__run_fixture_git__mutmut_36 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_37'] = x__run_fixture_git__mutmut_37 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_38'] = x__run_fixture_git__mutmut_38 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_39'] = x__run_fixture_git__mutmut_39 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_40'] = x__run_fixture_git__mutmut_40 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_41'] = x__run_fixture_git__mutmut_41 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_42'] = x__run_fixture_git__mutmut_42 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_43'] = x__run_fixture_git__mutmut_43 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_44'] = x__run_fixture_git__mutmut_44 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_45'] = x__run_fixture_git__mutmut_45 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_46'] = x__run_fixture_git__mutmut_46 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_47'] = x__run_fixture_git__mutmut_47 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_48'] = x__run_fixture_git__mutmut_48 # type: ignore # mutmut generated
mutants_x__run_fixture_git__mutmut['x__run_fixture_git__mutmut_49'] = x__run_fixture_git__mutmut_49 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__materialize_git_fixture__mutmut)
def _materialize_git_fixture(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_orig(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_1(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_2(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = None
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_3(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo * environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_4(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() and history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_5(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_6(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError(None)
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_7(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("XXGit contract history must be a regular fileXX")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_8(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_9(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("GIT CONTRACT HISTORY MUST BE A REGULAR FILE")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_10(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size >= _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_11(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError(None)
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_12(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("XXGit contract history exceeds the 1 MiB limitXX")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_13(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("git contract history exceeds the 1 mib limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_14(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("GIT CONTRACT HISTORY EXCEEDS THE 1 MIB LIMIT")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_15(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = None
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_16(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = None
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_17(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo * ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_18(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / "XX.contractXX"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_19(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".CONTRACT"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_20(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(None)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_21(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(None):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_22(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError(None)

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_23(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("XXGit contract fixture may contain only its .contract history inputXX")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_24(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_25(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("GIT CONTRACT FIXTURE MAY CONTAIN ONLY ITS .CONTRACT HISTORY INPUT")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_26(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(None, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_27(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, None)
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_28(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_29(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, )
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_30(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["XXinitXX", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_31(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["INIT", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_32(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "XX--quietXX", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_33(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--QUIET", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_34(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "XX--object-format=sha1XX"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_35(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--OBJECT-FORMAT=SHA1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_36(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(None, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_37(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, None, input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_38(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=None)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_39(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_40(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_41(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], )
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_42(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["XXfast-importXX", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_43(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["FAST-IMPORT", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_44(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "XX--quietXX"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_45(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--QUIET"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_46(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(None, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_47(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, None)
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_48(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_49(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, )
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_50(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["XXrev-parseXX", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_51(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["REV-PARSE", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_52(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "XX--verifyXX", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_53(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--VERIFY", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_54(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(None, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_55(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, None)
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_56(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_57(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, )
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_58(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["XXcheckoutXX", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_59(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["CHECKOUT", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_60(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "XX--quietXX", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_61(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--QUIET", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_62(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "XX--forceXX", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_63(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--FORCE", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_64(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "XX--detachXX", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_65(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--DETACH", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_66(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = None
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_67(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(None)
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_68(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(None, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_69(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, None).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_70(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_71(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_72(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["XXls-filesXX", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_73(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["LS-FILES", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_74(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "XX--stageXX", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_75(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--STAGE", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_76(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "XX-zXX"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_77(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-Z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_78(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"XX\x00XX")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_79(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = None
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_80(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        None
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_81(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode(None, "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_82(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", None)
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_83(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_84(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", )
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_85(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=None)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_86(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.rsplit(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_87(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=2)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_88(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[1].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_89(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("XXasciiXX", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_90(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ASCII", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_91(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "XXreplaceXX")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_92(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "REPLACE")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_93(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry or entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_94(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=None)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_95(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.rsplit(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_96(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=2)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_97(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[1] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_98(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_99(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"XX100644XX", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_100(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"XX100755XX"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_101(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(None)
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_102(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {', '.join(None)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.


def x__materialize_git_fixture__mutmut_103(
    repo: Path,
    environment: object,
) -> None:
    """Replace a bound ``.contract`` input with its deterministic Git tree."""
    if not isinstance(environment, GitCaseEnvironment):
        return
    history = repo / environment.git.history
    if not history.is_file() or history.is_symlink():
        raise CheckContractError("Git contract history must be a regular file")
    if history.stat().st_size > _GIT_FIXTURE_MAX_BYTES:
        raise CheckContractError("Git contract history exceeds the 1 MiB limit")
    stream = history.read_bytes()
    contract_root = repo / ".contract"
    shutil.rmtree(contract_root)
    if any(repo.iterdir()):
        raise CheckContractError("Git contract fixture may contain only its .contract history input")

    _run_fixture_git(repo, ["init", "--quiet", "--object-format=sha1"])
    _run_fixture_git(repo, ["fast-import", "--quiet"], input_bytes=stream)
    _run_fixture_git(repo, ["rev-parse", "--verify", f"{environment.git.checkout}^{{commit}}"])
    _run_fixture_git(repo, ["checkout", "--quiet", "--force", "--detach", environment.git.checkout])
    modes = _run_fixture_git(repo, ["ls-files", "--stage", "-z"]).stdout.split(b"\x00")
    invalid_modes = sorted(
        entry.split(maxsplit=1)[0].decode("ascii", "replace")
        for entry in modes
        if entry and entry.split(maxsplit=1)[0] not in {b"100644", b"100755"}
    )
    if invalid_modes:
        raise CheckContractError(f"Git contract tree contains unsupported modes: {'XX, XX'.join(invalid_modes)}")
    # A force checkout into a freshly initialised repository is clean by
    # construction; the stage-mode validation above rejects non-file entries.

mutants_x__materialize_git_fixture__mutmut['_mutmut_orig'] = x__materialize_git_fixture__mutmut_orig # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_1'] = x__materialize_git_fixture__mutmut_1 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_2'] = x__materialize_git_fixture__mutmut_2 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_3'] = x__materialize_git_fixture__mutmut_3 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_4'] = x__materialize_git_fixture__mutmut_4 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_5'] = x__materialize_git_fixture__mutmut_5 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_6'] = x__materialize_git_fixture__mutmut_6 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_7'] = x__materialize_git_fixture__mutmut_7 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_8'] = x__materialize_git_fixture__mutmut_8 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_9'] = x__materialize_git_fixture__mutmut_9 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_10'] = x__materialize_git_fixture__mutmut_10 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_11'] = x__materialize_git_fixture__mutmut_11 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_12'] = x__materialize_git_fixture__mutmut_12 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_13'] = x__materialize_git_fixture__mutmut_13 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_14'] = x__materialize_git_fixture__mutmut_14 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_15'] = x__materialize_git_fixture__mutmut_15 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_16'] = x__materialize_git_fixture__mutmut_16 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_17'] = x__materialize_git_fixture__mutmut_17 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_18'] = x__materialize_git_fixture__mutmut_18 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_19'] = x__materialize_git_fixture__mutmut_19 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_20'] = x__materialize_git_fixture__mutmut_20 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_21'] = x__materialize_git_fixture__mutmut_21 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_22'] = x__materialize_git_fixture__mutmut_22 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_23'] = x__materialize_git_fixture__mutmut_23 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_24'] = x__materialize_git_fixture__mutmut_24 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_25'] = x__materialize_git_fixture__mutmut_25 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_26'] = x__materialize_git_fixture__mutmut_26 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_27'] = x__materialize_git_fixture__mutmut_27 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_28'] = x__materialize_git_fixture__mutmut_28 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_29'] = x__materialize_git_fixture__mutmut_29 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_30'] = x__materialize_git_fixture__mutmut_30 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_31'] = x__materialize_git_fixture__mutmut_31 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_32'] = x__materialize_git_fixture__mutmut_32 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_33'] = x__materialize_git_fixture__mutmut_33 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_34'] = x__materialize_git_fixture__mutmut_34 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_35'] = x__materialize_git_fixture__mutmut_35 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_36'] = x__materialize_git_fixture__mutmut_36 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_37'] = x__materialize_git_fixture__mutmut_37 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_38'] = x__materialize_git_fixture__mutmut_38 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_39'] = x__materialize_git_fixture__mutmut_39 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_40'] = x__materialize_git_fixture__mutmut_40 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_41'] = x__materialize_git_fixture__mutmut_41 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_42'] = x__materialize_git_fixture__mutmut_42 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_43'] = x__materialize_git_fixture__mutmut_43 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_44'] = x__materialize_git_fixture__mutmut_44 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_45'] = x__materialize_git_fixture__mutmut_45 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_46'] = x__materialize_git_fixture__mutmut_46 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_47'] = x__materialize_git_fixture__mutmut_47 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_48'] = x__materialize_git_fixture__mutmut_48 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_49'] = x__materialize_git_fixture__mutmut_49 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_50'] = x__materialize_git_fixture__mutmut_50 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_51'] = x__materialize_git_fixture__mutmut_51 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_52'] = x__materialize_git_fixture__mutmut_52 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_53'] = x__materialize_git_fixture__mutmut_53 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_54'] = x__materialize_git_fixture__mutmut_54 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_55'] = x__materialize_git_fixture__mutmut_55 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_56'] = x__materialize_git_fixture__mutmut_56 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_57'] = x__materialize_git_fixture__mutmut_57 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_58'] = x__materialize_git_fixture__mutmut_58 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_59'] = x__materialize_git_fixture__mutmut_59 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_60'] = x__materialize_git_fixture__mutmut_60 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_61'] = x__materialize_git_fixture__mutmut_61 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_62'] = x__materialize_git_fixture__mutmut_62 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_63'] = x__materialize_git_fixture__mutmut_63 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_64'] = x__materialize_git_fixture__mutmut_64 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_65'] = x__materialize_git_fixture__mutmut_65 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_66'] = x__materialize_git_fixture__mutmut_66 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_67'] = x__materialize_git_fixture__mutmut_67 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_68'] = x__materialize_git_fixture__mutmut_68 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_69'] = x__materialize_git_fixture__mutmut_69 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_70'] = x__materialize_git_fixture__mutmut_70 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_71'] = x__materialize_git_fixture__mutmut_71 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_72'] = x__materialize_git_fixture__mutmut_72 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_73'] = x__materialize_git_fixture__mutmut_73 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_74'] = x__materialize_git_fixture__mutmut_74 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_75'] = x__materialize_git_fixture__mutmut_75 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_76'] = x__materialize_git_fixture__mutmut_76 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_77'] = x__materialize_git_fixture__mutmut_77 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_78'] = x__materialize_git_fixture__mutmut_78 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_79'] = x__materialize_git_fixture__mutmut_79 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_80'] = x__materialize_git_fixture__mutmut_80 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_81'] = x__materialize_git_fixture__mutmut_81 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_82'] = x__materialize_git_fixture__mutmut_82 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_83'] = x__materialize_git_fixture__mutmut_83 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_84'] = x__materialize_git_fixture__mutmut_84 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_85'] = x__materialize_git_fixture__mutmut_85 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_86'] = x__materialize_git_fixture__mutmut_86 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_87'] = x__materialize_git_fixture__mutmut_87 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_88'] = x__materialize_git_fixture__mutmut_88 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_89'] = x__materialize_git_fixture__mutmut_89 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_90'] = x__materialize_git_fixture__mutmut_90 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_91'] = x__materialize_git_fixture__mutmut_91 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_92'] = x__materialize_git_fixture__mutmut_92 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_93'] = x__materialize_git_fixture__mutmut_93 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_94'] = x__materialize_git_fixture__mutmut_94 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_95'] = x__materialize_git_fixture__mutmut_95 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_96'] = x__materialize_git_fixture__mutmut_96 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_97'] = x__materialize_git_fixture__mutmut_97 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_98'] = x__materialize_git_fixture__mutmut_98 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_99'] = x__materialize_git_fixture__mutmut_99 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_100'] = x__materialize_git_fixture__mutmut_100 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_101'] = x__materialize_git_fixture__mutmut_101 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_102'] = x__materialize_git_fixture__mutmut_102 # type: ignore # mutmut generated
mutants_x__materialize_git_fixture__mutmut['x__materialize_git_fixture__mutmut_103'] = x__materialize_git_fixture__mutmut_103 # type: ignore # mutmut generated


@contextmanager
def _case_environment(policy: str, temporary: Path) -> Iterator[None]:
    """Hide PATH-resolved tools for an explicit unavailable case, then restore."""
    if policy == "inherit":
        yield
        return
    empty = temporary / "empty-path"
    empty.mkdir()
    original = os.environ.get("PATH")
    os.environ["PATH"] = str(empty)
    try:
        yield
    finally:
        if original is None:
            os.environ.pop("PATH", None)
        else:
            os.environ["PATH"] = original
mutants_x__fixture_path__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fixture_path__mutmut)
def _fixture_path(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_orig(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_1(manifest: Path, name: str) -> Path:
    relative = None
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_2(manifest: Path, name: str) -> Path:
    relative = Path(None)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_3(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = None
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_4(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent * relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_5(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts and not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_6(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() and ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_7(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or "XX..XX" in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_8(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." not in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_9(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_10(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError(None)
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_11(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("XXfixture must be an existing directory beneath the manifestXX")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_12(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("FIXTURE MUST BE AN EXISTING DIRECTORY BENEATH THE MANIFEST")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_13(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = None
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_14(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(None, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_15(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, None)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_16(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_17(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, )
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_18(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(2, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_19(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) - 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_20(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 2)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_21(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) and any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_22(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(None) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_23(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(None):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_24(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob(None)):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_25(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("XX*XX")):
        raise CheckContractError("contract fixtures cannot contain symlinks")
    return fixture


def x__fixture_path__mutmut_26(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError(None)
    return fixture


def x__fixture_path__mutmut_27(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("XXcontract fixtures cannot contain symlinksXX")
    return fixture


def x__fixture_path__mutmut_28(manifest: Path, name: str) -> Path:
    relative = Path(name)
    fixture = manifest.parent / relative
    if relative.is_absolute() or ".." in relative.parts or not fixture.is_dir():
        raise CheckContractError("fixture must be an existing directory beneath the manifest")
    ancestors = [
        manifest.parent.joinpath(*relative.parts[:index]) for index in range(1, len(relative.parts) + 1)
    ]
    if any(path.is_symlink() for path in ancestors) or any(path.is_symlink() for path in fixture.rglob("*")):
        raise CheckContractError("CONTRACT FIXTURES CANNOT CONTAIN SYMLINKS")
    return fixture

mutants_x__fixture_path__mutmut['_mutmut_orig'] = x__fixture_path__mutmut_orig # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_1'] = x__fixture_path__mutmut_1 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_2'] = x__fixture_path__mutmut_2 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_3'] = x__fixture_path__mutmut_3 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_4'] = x__fixture_path__mutmut_4 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_5'] = x__fixture_path__mutmut_5 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_6'] = x__fixture_path__mutmut_6 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_7'] = x__fixture_path__mutmut_7 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_8'] = x__fixture_path__mutmut_8 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_9'] = x__fixture_path__mutmut_9 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_10'] = x__fixture_path__mutmut_10 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_11'] = x__fixture_path__mutmut_11 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_12'] = x__fixture_path__mutmut_12 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_13'] = x__fixture_path__mutmut_13 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_14'] = x__fixture_path__mutmut_14 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_15'] = x__fixture_path__mutmut_15 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_16'] = x__fixture_path__mutmut_16 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_17'] = x__fixture_path__mutmut_17 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_18'] = x__fixture_path__mutmut_18 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_19'] = x__fixture_path__mutmut_19 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_20'] = x__fixture_path__mutmut_20 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_21'] = x__fixture_path__mutmut_21 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_22'] = x__fixture_path__mutmut_22 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_23'] = x__fixture_path__mutmut_23 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_24'] = x__fixture_path__mutmut_24 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_25'] = x__fixture_path__mutmut_25 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_26'] = x__fixture_path__mutmut_26 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_27'] = x__fixture_path__mutmut_27 # type: ignore # mutmut generated
mutants_x__fixture_path__mutmut['x__fixture_path__mutmut_28'] = x__fixture_path__mutmut_28 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__match_findings__mutmut)
def _match_findings(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_orig(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_1(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) == len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_2(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return True
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_3(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_4(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return False
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_5(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = None
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_6(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[1]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_7(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(None):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_8(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"] or _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_9(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path or finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_10(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule or observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_11(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["XXruleXX"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_12(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["RULE"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_13(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] != finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_14(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["XXpathXX"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_15(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["PATH"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_16(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] != finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_17(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains not in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_18(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["XXmessageXX"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_19(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["MESSAGE"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_20(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(None, actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_21(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], None)
        ):
            return True
    return False


def x__match_findings__mutmut_22(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_23(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], )
        ):
            return True
    return False


def x__match_findings__mutmut_24(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[2:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_25(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] - actual[index + 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_26(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index - 1 :])
        ):
            return True
    return False


def x__match_findings__mutmut_27(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 2 :])
        ):
            return True
    return False


def x__match_findings__mutmut_28(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return False
    return False


def x__match_findings__mutmut_29(expected: tuple[FindingExpectation, ...], actual: list[dict[str, Any]]) -> bool:
    """Require a one-to-one match, including every unexpected finding."""
    if len(expected) != len(actual):
        return False
    if not expected:
        return True
    finding = expected[0]
    for index, observed in enumerate(actual):
        if (
            observed["rule"] == finding.rule
            and observed["path"] == finding.path
            and finding.message_contains in observed["message"]
            and _match_findings(expected[1:], actual[:index] + actual[index + 1 :])
        ):
            return True
    return True

mutants_x__match_findings__mutmut['_mutmut_orig'] = x__match_findings__mutmut_orig # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_1'] = x__match_findings__mutmut_1 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_2'] = x__match_findings__mutmut_2 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_3'] = x__match_findings__mutmut_3 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_4'] = x__match_findings__mutmut_4 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_5'] = x__match_findings__mutmut_5 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_6'] = x__match_findings__mutmut_6 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_7'] = x__match_findings__mutmut_7 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_8'] = x__match_findings__mutmut_8 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_9'] = x__match_findings__mutmut_9 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_10'] = x__match_findings__mutmut_10 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_11'] = x__match_findings__mutmut_11 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_12'] = x__match_findings__mutmut_12 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_13'] = x__match_findings__mutmut_13 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_14'] = x__match_findings__mutmut_14 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_15'] = x__match_findings__mutmut_15 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_16'] = x__match_findings__mutmut_16 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_17'] = x__match_findings__mutmut_17 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_18'] = x__match_findings__mutmut_18 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_19'] = x__match_findings__mutmut_19 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_20'] = x__match_findings__mutmut_20 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_21'] = x__match_findings__mutmut_21 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_22'] = x__match_findings__mutmut_22 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_23'] = x__match_findings__mutmut_23 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_24'] = x__match_findings__mutmut_24 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_25'] = x__match_findings__mutmut_25 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_26'] = x__match_findings__mutmut_26 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_27'] = x__match_findings__mutmut_27 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_28'] = x__match_findings__mutmut_28 # type: ignore # mutmut generated
mutants_x__match_findings__mutmut['x__match_findings__mutmut_29'] = x__match_findings__mutmut_29 # type: ignore # mutmut generated
mutants_x__unique_object__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__unique_object__mutmut)
def _unique_object(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
    value: dict[str, Any] = {}
    for key, item in pairs:
        if key in value:
            raise CheckContractError(f"duplicate ledger key: {key}")
        value[key] = item
    return value


def x__unique_object__mutmut_orig(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
    value: dict[str, Any] = {}
    for key, item in pairs:
        if key in value:
            raise CheckContractError(f"duplicate ledger key: {key}")
        value[key] = item
    return value


def x__unique_object__mutmut_1(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
    value: dict[str, Any] = None
    for key, item in pairs:
        if key in value:
            raise CheckContractError(f"duplicate ledger key: {key}")
        value[key] = item
    return value


def x__unique_object__mutmut_2(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
    value: dict[str, Any] = {}
    for key, item in pairs:
        if key not in value:
            raise CheckContractError(f"duplicate ledger key: {key}")
        value[key] = item
    return value


def x__unique_object__mutmut_3(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
    value: dict[str, Any] = {}
    for key, item in pairs:
        if key in value:
            raise CheckContractError(None)
        value[key] = item
    return value


def x__unique_object__mutmut_4(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
    value: dict[str, Any] = {}
    for key, item in pairs:
        if key in value:
            raise CheckContractError(f"duplicate ledger key: {key}")
        value[key] = None
    return value

mutants_x__unique_object__mutmut['_mutmut_orig'] = x__unique_object__mutmut_orig # type: ignore # mutmut generated
mutants_x__unique_object__mutmut['x__unique_object__mutmut_1'] = x__unique_object__mutmut_1 # type: ignore # mutmut generated
mutants_x__unique_object__mutmut['x__unique_object__mutmut_2'] = x__unique_object__mutmut_2 # type: ignore # mutmut generated
mutants_x__unique_object__mutmut['x__unique_object__mutmut_3'] = x__unique_object__mutmut_3 # type: ignore # mutmut generated
mutants_x__unique_object__mutmut['x__unique_object__mutmut_4'] = x__unique_object__mutmut_4 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_validate_contract_ledger__mutmut)
def validate_contract_ledger(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_orig(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_1(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = None
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_2(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(None) from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_3(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = None
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_4(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(None, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_5(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=None)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_6(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_7(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, )
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_8(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(None, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_9(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, None)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_10(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_11(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, )
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_12(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = None
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_13(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next(None, None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_14(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next(None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_15(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), )
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_16(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id != case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_17(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is not None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_18(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(None)
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_19(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = None
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_20(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            None, object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_21(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=None
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_22(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_23(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_24(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding=None), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_25(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="XXutf-8XX"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_26(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="UTF-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_27(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = None
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_28(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop(None)
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_29(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("XXpayload_digestXX")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_30(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("PAYLOAD_DIGEST")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_31(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest == payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_32(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(None):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_33(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError(None)
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_34(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("XXledger payload digest mismatchXX")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_35(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("LEDGER PAYLOAD DIGEST MISMATCH")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_36(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = None
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_37(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "XXschemaXX": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_38(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "SCHEMA": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_39(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "XXcheckXX": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_40(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "CHECK": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_41(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "XXcase_idXX": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_42(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "CASE_ID": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_43(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "XXcase_digestXX": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_44(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "CASE_DIGEST": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_45(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(None),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_46(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(None)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_47(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "XXcontract_digestXX": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_48(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "CONTRACT_DIGEST": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_49(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" - hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_50(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "XXsha256:XX" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_51(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "SHA256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_52(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(None).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_53(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "XXfixture_digestXX": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_54(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "FIXTURE_DIGEST": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_55(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(None),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_56(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(None, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_57(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, None)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_58(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_59(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, )),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_60(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "XXevidence_classXX": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_61(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "EVIDENCE_CLASS": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_62(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "XXlive_qualificationXX": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_63(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "LIVE_QUALIFICATION": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_64(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "XXrelease_admissionXX": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_65(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "RELEASE_ADMISSION": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_66(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "XXcandidateXX": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_67(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "CANDIDATE": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_68(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "XXexpectedXX": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_69(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "EXPECTED": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_70(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(None),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_71(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(None)),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_72(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(None))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_73(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "XXenvironmentXX": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_74(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "ENVIRONMENT": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_75(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(None),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_76(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] == expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_77(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(None)
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_78(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(None)
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_79(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["XXexecution_idXX"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_80(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["EXECUTION_ID"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_81(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = None
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_82(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(None)
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_83(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["XXstarted_atXX"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_84(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["STARTED_AT"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_85(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = None
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_86(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(None)
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_87(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["XXfinished_atXX"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_88(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["FINISHED_AT"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_89(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_90(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after < started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_91(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started < finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_92(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished < datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_93(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(None):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_94(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError(None)
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_95(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("XXledger timestamps are stale, reversed or in the futureXX")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_96(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("LEDGER TIMESTAMPS ARE STALE, REVERSED OR IN THE FUTURE")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_97(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = None
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_98(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["XXactualXX"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_99(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["ACTUAL"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_100(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = None
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_101(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "XXzeroXX" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_102(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "ZERO" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_103(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit != 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_104(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 1 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_105(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "XXnonzeroXX"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_106(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "NONZERO"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_107(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit and actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_108(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class and exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_109(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit and actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_110(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int and actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_111(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(None) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_112(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["XXexit_codeXX"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_113(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["EXIT_CODE"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_114(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_115(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["XXexit_codeXX"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_116(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["EXIT_CODE"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_117(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] == process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_118(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["XXexitXX"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_119(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["EXIT"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_120(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] == exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_121(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class == case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_122(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["XXstatusXX"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_123(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["STATUS"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_124(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] == case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_125(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError(None)
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_126(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("XXledger status or process exit does not match the caseXX")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_127(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("LEDGER STATUS OR PROCESS EXIT DOES NOT MATCH THE CASE")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_128(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = None
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_129(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["XXfindingsXX"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_130(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["FINDINGS"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_131(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) and any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_132(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_133(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            None
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_134(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message")) and finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_135(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict) and not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_136(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_137(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_138(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(None)
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_139(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("XXruleXX", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_140(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("RULE", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_141(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "XXpathXX", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_142(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "PATH", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_143(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "XXmessageXX"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_144(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "MESSAGE"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_145(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get(None) != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_146(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("XXstatusXX") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_147(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("STATUS") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_148(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") == actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_149(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["XXstatusXX"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_150(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["STATUS"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_151(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError(None)
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_152(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("XXinvalid structured findingsXX")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_153(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("INVALID STRUCTURED FINDINGS")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_154(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_155(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(None, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_156(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, None):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_157(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_158(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, ):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_159(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError(None)
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_160(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("XXledger findings do not match the caseXX")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_161(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("LEDGER FINDINGS DO NOT MATCH THE CASE")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_162(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = None
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_163(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["XXpayload_digestXX"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_164(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["PAYLOAD_DIGEST"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(f"invalid or missing ledger: {exc}") from exc


def x_validate_contract_ledger__mutmut_165(
    manifest: Path,
    case_id: str,
    ledger: Path,
    *,
    process_exit: int,
    started_after: datetime,
) -> dict[str, Any]:
    """Admit only fresh, complete evidence bound to this candidate and input.

    ``process_exit`` and ``started_after`` come from the caller's invocation,
    never from the ledger being validated. A digest protects integrity, not
    authenticity; release signing remains the admission layer's responsibility.
    """
    try:
        manifest_bytes = manifest.read_bytes()
    except OSError as exc:
        raise CheckContractError(f"cannot read contract snapshot: {exc}") from exc
    contract = load_check_contract(manifest, source=manifest_bytes)
    validate_contract_configuration(contract.check, contract.config)
    case = next((case for case in contract.cases if case.id == case_id), None)
    if case is None:
        raise CheckContractError(f"unknown contract case: {case_id}")
    try:
        value: dict[str, Any] = json.loads(
            ledger.read_text(encoding="utf-8"), object_pairs_hook=_unique_object
        )
        digest = value.pop("payload_digest")
        if digest != payload_digest(value):
            raise CheckContractError("ledger payload digest mismatch")
        bindings = {
            "schema": LEDGER_SCHEMA,
            "check": contract.check,
            "case_id": case.id,
            "case_digest": payload_digest(asdict(case)),
            "contract_digest": "sha256:" + hashlib.sha256(manifest_bytes).hexdigest(),
            "fixture_digest": tree_digest(_fixture_path(manifest, case.fixture)),
            "evidence_class": contract.evidence_class,
            "live_qualification": contract.live_qualification,
            "release_admission": contract.release_admission,
            "candidate": candidate_identity(),
            "expected": json.loads(json.dumps(asdict(case.expected))),
            "environment": asdict(case.environment),
        }
        for name, expected in bindings.items():
            if value[name] != expected:
                raise CheckContractError(f"ledger {name} mismatch")
        UUID(value["execution_id"])
        started = datetime.fromisoformat(value["started_at"])
        finished = datetime.fromisoformat(value["finished_at"])
        if not started_after <= started <= finished <= datetime.now(UTC):
            raise CheckContractError("ledger timestamps are stale, reversed or in the future")
        actual = value["actual"]
        exit_class = "zero" if process_exit == 0 else "nonzero"
        if (
            type(actual["exit_code"]) is not int
            or actual["exit_code"] != process_exit
            or actual["exit"] != exit_class
            or exit_class != case.expected.exit
            or actual["status"] != case.expected.status
        ):
            raise CheckContractError("ledger status or process exit does not match the case")
        findings = actual["findings"]
        if not isinstance(findings, list) or any(
            not isinstance(finding, dict)
            or not all(isinstance(finding.get(key), str) for key in ("rule", "path", "message"))
            or finding.get("status") != actual["status"]
            for finding in findings
        ):
            raise CheckContractError("invalid structured findings")
        if not _match_findings(case.expected.findings, findings):
            raise CheckContractError("ledger findings do not match the case")
        value["payload_digest"] = digest
        return value
    except (OSError, ValueError, TypeError, KeyError, AttributeError) as exc:
        if isinstance(exc, CheckContractError):
            raise
        raise CheckContractError(None) from exc

mutants_x_validate_contract_ledger__mutmut['_mutmut_orig'] = x_validate_contract_ledger__mutmut_orig # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_1'] = x_validate_contract_ledger__mutmut_1 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_2'] = x_validate_contract_ledger__mutmut_2 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_3'] = x_validate_contract_ledger__mutmut_3 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_4'] = x_validate_contract_ledger__mutmut_4 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_5'] = x_validate_contract_ledger__mutmut_5 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_6'] = x_validate_contract_ledger__mutmut_6 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_7'] = x_validate_contract_ledger__mutmut_7 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_8'] = x_validate_contract_ledger__mutmut_8 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_9'] = x_validate_contract_ledger__mutmut_9 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_10'] = x_validate_contract_ledger__mutmut_10 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_11'] = x_validate_contract_ledger__mutmut_11 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_12'] = x_validate_contract_ledger__mutmut_12 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_13'] = x_validate_contract_ledger__mutmut_13 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_14'] = x_validate_contract_ledger__mutmut_14 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_15'] = x_validate_contract_ledger__mutmut_15 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_16'] = x_validate_contract_ledger__mutmut_16 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_17'] = x_validate_contract_ledger__mutmut_17 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_18'] = x_validate_contract_ledger__mutmut_18 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_19'] = x_validate_contract_ledger__mutmut_19 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_20'] = x_validate_contract_ledger__mutmut_20 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_21'] = x_validate_contract_ledger__mutmut_21 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_22'] = x_validate_contract_ledger__mutmut_22 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_23'] = x_validate_contract_ledger__mutmut_23 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_24'] = x_validate_contract_ledger__mutmut_24 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_25'] = x_validate_contract_ledger__mutmut_25 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_26'] = x_validate_contract_ledger__mutmut_26 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_27'] = x_validate_contract_ledger__mutmut_27 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_28'] = x_validate_contract_ledger__mutmut_28 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_29'] = x_validate_contract_ledger__mutmut_29 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_30'] = x_validate_contract_ledger__mutmut_30 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_31'] = x_validate_contract_ledger__mutmut_31 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_32'] = x_validate_contract_ledger__mutmut_32 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_33'] = x_validate_contract_ledger__mutmut_33 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_34'] = x_validate_contract_ledger__mutmut_34 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_35'] = x_validate_contract_ledger__mutmut_35 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_36'] = x_validate_contract_ledger__mutmut_36 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_37'] = x_validate_contract_ledger__mutmut_37 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_38'] = x_validate_contract_ledger__mutmut_38 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_39'] = x_validate_contract_ledger__mutmut_39 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_40'] = x_validate_contract_ledger__mutmut_40 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_41'] = x_validate_contract_ledger__mutmut_41 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_42'] = x_validate_contract_ledger__mutmut_42 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_43'] = x_validate_contract_ledger__mutmut_43 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_44'] = x_validate_contract_ledger__mutmut_44 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_45'] = x_validate_contract_ledger__mutmut_45 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_46'] = x_validate_contract_ledger__mutmut_46 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_47'] = x_validate_contract_ledger__mutmut_47 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_48'] = x_validate_contract_ledger__mutmut_48 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_49'] = x_validate_contract_ledger__mutmut_49 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_50'] = x_validate_contract_ledger__mutmut_50 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_51'] = x_validate_contract_ledger__mutmut_51 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_52'] = x_validate_contract_ledger__mutmut_52 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_53'] = x_validate_contract_ledger__mutmut_53 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_54'] = x_validate_contract_ledger__mutmut_54 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_55'] = x_validate_contract_ledger__mutmut_55 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_56'] = x_validate_contract_ledger__mutmut_56 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_57'] = x_validate_contract_ledger__mutmut_57 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_58'] = x_validate_contract_ledger__mutmut_58 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_59'] = x_validate_contract_ledger__mutmut_59 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_60'] = x_validate_contract_ledger__mutmut_60 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_61'] = x_validate_contract_ledger__mutmut_61 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_62'] = x_validate_contract_ledger__mutmut_62 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_63'] = x_validate_contract_ledger__mutmut_63 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_64'] = x_validate_contract_ledger__mutmut_64 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_65'] = x_validate_contract_ledger__mutmut_65 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_66'] = x_validate_contract_ledger__mutmut_66 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_67'] = x_validate_contract_ledger__mutmut_67 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_68'] = x_validate_contract_ledger__mutmut_68 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_69'] = x_validate_contract_ledger__mutmut_69 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_70'] = x_validate_contract_ledger__mutmut_70 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_71'] = x_validate_contract_ledger__mutmut_71 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_72'] = x_validate_contract_ledger__mutmut_72 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_73'] = x_validate_contract_ledger__mutmut_73 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_74'] = x_validate_contract_ledger__mutmut_74 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_75'] = x_validate_contract_ledger__mutmut_75 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_76'] = x_validate_contract_ledger__mutmut_76 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_77'] = x_validate_contract_ledger__mutmut_77 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_78'] = x_validate_contract_ledger__mutmut_78 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_79'] = x_validate_contract_ledger__mutmut_79 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_80'] = x_validate_contract_ledger__mutmut_80 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_81'] = x_validate_contract_ledger__mutmut_81 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_82'] = x_validate_contract_ledger__mutmut_82 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_83'] = x_validate_contract_ledger__mutmut_83 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_84'] = x_validate_contract_ledger__mutmut_84 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_85'] = x_validate_contract_ledger__mutmut_85 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_86'] = x_validate_contract_ledger__mutmut_86 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_87'] = x_validate_contract_ledger__mutmut_87 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_88'] = x_validate_contract_ledger__mutmut_88 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_89'] = x_validate_contract_ledger__mutmut_89 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_90'] = x_validate_contract_ledger__mutmut_90 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_91'] = x_validate_contract_ledger__mutmut_91 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_92'] = x_validate_contract_ledger__mutmut_92 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_93'] = x_validate_contract_ledger__mutmut_93 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_94'] = x_validate_contract_ledger__mutmut_94 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_95'] = x_validate_contract_ledger__mutmut_95 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_96'] = x_validate_contract_ledger__mutmut_96 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_97'] = x_validate_contract_ledger__mutmut_97 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_98'] = x_validate_contract_ledger__mutmut_98 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_99'] = x_validate_contract_ledger__mutmut_99 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_100'] = x_validate_contract_ledger__mutmut_100 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_101'] = x_validate_contract_ledger__mutmut_101 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_102'] = x_validate_contract_ledger__mutmut_102 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_103'] = x_validate_contract_ledger__mutmut_103 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_104'] = x_validate_contract_ledger__mutmut_104 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_105'] = x_validate_contract_ledger__mutmut_105 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_106'] = x_validate_contract_ledger__mutmut_106 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_107'] = x_validate_contract_ledger__mutmut_107 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_108'] = x_validate_contract_ledger__mutmut_108 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_109'] = x_validate_contract_ledger__mutmut_109 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_110'] = x_validate_contract_ledger__mutmut_110 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_111'] = x_validate_contract_ledger__mutmut_111 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_112'] = x_validate_contract_ledger__mutmut_112 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_113'] = x_validate_contract_ledger__mutmut_113 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_114'] = x_validate_contract_ledger__mutmut_114 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_115'] = x_validate_contract_ledger__mutmut_115 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_116'] = x_validate_contract_ledger__mutmut_116 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_117'] = x_validate_contract_ledger__mutmut_117 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_118'] = x_validate_contract_ledger__mutmut_118 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_119'] = x_validate_contract_ledger__mutmut_119 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_120'] = x_validate_contract_ledger__mutmut_120 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_121'] = x_validate_contract_ledger__mutmut_121 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_122'] = x_validate_contract_ledger__mutmut_122 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_123'] = x_validate_contract_ledger__mutmut_123 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_124'] = x_validate_contract_ledger__mutmut_124 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_125'] = x_validate_contract_ledger__mutmut_125 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_126'] = x_validate_contract_ledger__mutmut_126 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_127'] = x_validate_contract_ledger__mutmut_127 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_128'] = x_validate_contract_ledger__mutmut_128 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_129'] = x_validate_contract_ledger__mutmut_129 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_130'] = x_validate_contract_ledger__mutmut_130 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_131'] = x_validate_contract_ledger__mutmut_131 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_132'] = x_validate_contract_ledger__mutmut_132 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_133'] = x_validate_contract_ledger__mutmut_133 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_134'] = x_validate_contract_ledger__mutmut_134 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_135'] = x_validate_contract_ledger__mutmut_135 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_136'] = x_validate_contract_ledger__mutmut_136 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_137'] = x_validate_contract_ledger__mutmut_137 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_138'] = x_validate_contract_ledger__mutmut_138 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_139'] = x_validate_contract_ledger__mutmut_139 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_140'] = x_validate_contract_ledger__mutmut_140 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_141'] = x_validate_contract_ledger__mutmut_141 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_142'] = x_validate_contract_ledger__mutmut_142 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_143'] = x_validate_contract_ledger__mutmut_143 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_144'] = x_validate_contract_ledger__mutmut_144 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_145'] = x_validate_contract_ledger__mutmut_145 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_146'] = x_validate_contract_ledger__mutmut_146 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_147'] = x_validate_contract_ledger__mutmut_147 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_148'] = x_validate_contract_ledger__mutmut_148 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_149'] = x_validate_contract_ledger__mutmut_149 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_150'] = x_validate_contract_ledger__mutmut_150 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_151'] = x_validate_contract_ledger__mutmut_151 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_152'] = x_validate_contract_ledger__mutmut_152 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_153'] = x_validate_contract_ledger__mutmut_153 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_154'] = x_validate_contract_ledger__mutmut_154 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_155'] = x_validate_contract_ledger__mutmut_155 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_156'] = x_validate_contract_ledger__mutmut_156 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_157'] = x_validate_contract_ledger__mutmut_157 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_158'] = x_validate_contract_ledger__mutmut_158 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_159'] = x_validate_contract_ledger__mutmut_159 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_160'] = x_validate_contract_ledger__mutmut_160 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_161'] = x_validate_contract_ledger__mutmut_161 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_162'] = x_validate_contract_ledger__mutmut_162 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_163'] = x_validate_contract_ledger__mutmut_163 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_164'] = x_validate_contract_ledger__mutmut_164 # type: ignore # mutmut generated
mutants_x_validate_contract_ledger__mutmut['x_validate_contract_ledger__mutmut_165'] = x_validate_contract_ledger__mutmut_165 # type: ignore # mutmut generated
