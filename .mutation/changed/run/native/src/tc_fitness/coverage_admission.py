"""Exact-source coverage evidence shared by the existing CORE admission checks.

Strict measurement requires Coverage.py (a direct runtime dependency).
It uses Coverage.py's executable-statement analysis, not an approximate Python
interpreter or report-line intersection that could omit executable source.
"""

from __future__ import annotations

import argparse
import hashlib
import importlib
import json
import math
import os
import re
import sys
from dataclasses import asdict
from datetime import UTC, datetime
from pathlib import Path
from typing import Any

from tc_fitness.core_checks._coverage_evidence import resolve_coverage_filename
from tc_fitness.core_checks.coverage_floor import parse_coverage_details
from tc_fitness.runner import run_bounded_process

SCHEMA = "tc.fitness/coverage-receipt/v1"


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_digest__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_digest__mutmut)
def digest(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=True, separators=(",", ":"), allow_nan=False).encode())


def x_digest__mutmut_orig(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=True, separators=(",", ":"), allow_nan=False).encode())


def x_digest__mutmut_1(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(None)


def x_digest__mutmut_2(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(None, sort_keys=True, separators=(",", ":"), allow_nan=False).encode())


def x_digest__mutmut_3(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=None, separators=(",", ":"), allow_nan=False).encode())


def x_digest__mutmut_4(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=True, separators=None, allow_nan=False).encode())


def x_digest__mutmut_5(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=True, separators=(",", ":"), allow_nan=None).encode())


def x_digest__mutmut_6(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(sort_keys=True, separators=(",", ":"), allow_nan=False).encode())


def x_digest__mutmut_7(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, separators=(",", ":"), allow_nan=False).encode())


def x_digest__mutmut_8(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=True, allow_nan=False).encode())


def x_digest__mutmut_9(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=True, separators=(",", ":"), ).encode())


def x_digest__mutmut_10(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=False, separators=(",", ":"), allow_nan=False).encode())


def x_digest__mutmut_11(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=True, separators=("XX,XX", ":"), allow_nan=False).encode())


def x_digest__mutmut_12(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=True, separators=(",", "XX:XX"), allow_nan=False).encode())


def x_digest__mutmut_13(value: object) -> str:
    """Canonical, finite JSON content digest; not a signature or trust anchor."""
    return bytes_digest(json.dumps(value, sort_keys=True, separators=(",", ":"), allow_nan=True).encode())

mutants_x_digest__mutmut['_mutmut_orig'] = x_digest__mutmut_orig # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_1'] = x_digest__mutmut_1 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_2'] = x_digest__mutmut_2 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_3'] = x_digest__mutmut_3 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_4'] = x_digest__mutmut_4 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_5'] = x_digest__mutmut_5 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_6'] = x_digest__mutmut_6 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_7'] = x_digest__mutmut_7 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_8'] = x_digest__mutmut_8 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_9'] = x_digest__mutmut_9 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_10'] = x_digest__mutmut_10 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_11'] = x_digest__mutmut_11 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_12'] = x_digest__mutmut_12 # type: ignore # mutmut generated
mutants_x_digest__mutmut['x_digest__mutmut_13'] = x_digest__mutmut_13 # type: ignore # mutmut generated
mutants_x_bytes_digest__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_bytes_digest__mutmut)
def bytes_digest(value: bytes) -> str:
    return "sha256:" + hashlib.sha256(value).hexdigest()


def x_bytes_digest__mutmut_orig(value: bytes) -> str:
    return "sha256:" + hashlib.sha256(value).hexdigest()


def x_bytes_digest__mutmut_1(value: bytes) -> str:
    return "sha256:" - hashlib.sha256(value).hexdigest()


def x_bytes_digest__mutmut_2(value: bytes) -> str:
    return "XXsha256:XX" + hashlib.sha256(value).hexdigest()


def x_bytes_digest__mutmut_3(value: bytes) -> str:
    return "SHA256:" + hashlib.sha256(value).hexdigest()


def x_bytes_digest__mutmut_4(value: bytes) -> str:
    return "sha256:" + hashlib.sha256(None).hexdigest()

mutants_x_bytes_digest__mutmut['_mutmut_orig'] = x_bytes_digest__mutmut_orig # type: ignore # mutmut generated
mutants_x_bytes_digest__mutmut['x_bytes_digest__mutmut_1'] = x_bytes_digest__mutmut_1 # type: ignore # mutmut generated
mutants_x_bytes_digest__mutmut['x_bytes_digest__mutmut_2'] = x_bytes_digest__mutmut_2 # type: ignore # mutmut generated
mutants_x_bytes_digest__mutmut['x_bytes_digest__mutmut_3'] = x_bytes_digest__mutmut_3 # type: ignore # mutmut generated
mutants_x_bytes_digest__mutmut['x_bytes_digest__mutmut_4'] = x_bytes_digest__mutmut_4 # type: ignore # mutmut generated
mutants_x_configured__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_configured__mutmut)
def configured(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_orig(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_1(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) or value.startswith("env:"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_2(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith(None):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_3(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("XXenv:XX"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_4(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("ENV:"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_5(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = None
    if not isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_6(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(None)
    if not isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_7(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(value[5:])
    if not isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_8(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) and not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_9(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(value[4:])
    if isinstance(value, str) or not value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_10(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or value:
        raise ValueError("coverage requires a non-empty execution input")
    return value


def x_configured__mutmut_11(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or not value:
        raise ValueError(None)
    return value


def x_configured__mutmut_12(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or not value:
        raise ValueError("XXcoverage requires a non-empty execution inputXX")
    return value


def x_configured__mutmut_13(value: object) -> str:
    """Read an explicit literal or environment binding; absence is an error."""
    if isinstance(value, str) and value.startswith("env:"):
        value = os.environ.get(value[4:])
    if not isinstance(value, str) or not value:
        raise ValueError("COVERAGE REQUIRES A NON-EMPTY EXECUTION INPUT")
    return value

mutants_x_configured__mutmut['_mutmut_orig'] = x_configured__mutmut_orig # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_1'] = x_configured__mutmut_1 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_2'] = x_configured__mutmut_2 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_3'] = x_configured__mutmut_3 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_4'] = x_configured__mutmut_4 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_5'] = x_configured__mutmut_5 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_6'] = x_configured__mutmut_6 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_7'] = x_configured__mutmut_7 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_8'] = x_configured__mutmut_8 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_9'] = x_configured__mutmut_9 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_10'] = x_configured__mutmut_10 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_11'] = x_configured__mutmut_11 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_12'] = x_configured__mutmut_12 # type: ignore # mutmut generated
mutants_x_configured__mutmut['x_configured__mutmut_13'] = x_configured__mutmut_13 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_trusted_digest__mutmut)
def trusted_digest(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_orig(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_1(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = None
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_2(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(None)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_3(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith(None):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_4(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("XXfile:XX"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_5(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("FILE:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_6(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = None
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_7(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(None)
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_8(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(None))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_9(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[6:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_10(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() and path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_11(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() and path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_12(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_13(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(None):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_14(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError(None)
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_15(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("XXcoverage digest handoff must be a regular file outside the candidate checkoutXX")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_16(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("COVERAGE DIGEST HANDOFF MUST BE A REGULAR FILE OUTSIDE THE CANDIDATE CHECKOUT")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_17(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = None
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_18(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(None, token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_19(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", None) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_20(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_21(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", ) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_22(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"XXsha256:[0-9a-f]{64}XX", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_23(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"SHA256:[0-9A-F]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_24(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is not None:
        raise ValueError("coverage requires an independently supplied SHA-256 digest")
    return token


def x_trusted_digest__mutmut_25(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError(None)
    return token


def x_trusted_digest__mutmut_26(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("XXcoverage requires an independently supplied SHA-256 digestXX")
    return token


def x_trusted_digest__mutmut_27(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("coverage requires an independently supplied sha-256 digest")
    return token


def x_trusted_digest__mutmut_28(root: Path, value: object) -> str:
    """Resolve an independent digest handoff, never a receipt's own field."""
    token = configured(value)
    if token.startswith("file:"):
        path = Path(configured(token[5:]))
        if not path.is_absolute() or path.is_symlink() or path.resolve().is_relative_to(root.resolve()):
            raise ValueError("coverage digest handoff must be a regular file outside the candidate checkout")
        token = path.read_text().strip()
    if re.fullmatch(r"sha256:[0-9a-f]{64}", token) is None:
        raise ValueError("COVERAGE REQUIRES AN INDEPENDENTLY SUPPLIED SHA-256 DIGEST")
    return token

mutants_x_trusted_digest__mutmut['_mutmut_orig'] = x_trusted_digest__mutmut_orig # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_1'] = x_trusted_digest__mutmut_1 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_2'] = x_trusted_digest__mutmut_2 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_3'] = x_trusted_digest__mutmut_3 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_4'] = x_trusted_digest__mutmut_4 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_5'] = x_trusted_digest__mutmut_5 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_6'] = x_trusted_digest__mutmut_6 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_7'] = x_trusted_digest__mutmut_7 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_8'] = x_trusted_digest__mutmut_8 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_9'] = x_trusted_digest__mutmut_9 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_10'] = x_trusted_digest__mutmut_10 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_11'] = x_trusted_digest__mutmut_11 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_12'] = x_trusted_digest__mutmut_12 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_13'] = x_trusted_digest__mutmut_13 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_14'] = x_trusted_digest__mutmut_14 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_15'] = x_trusted_digest__mutmut_15 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_16'] = x_trusted_digest__mutmut_16 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_17'] = x_trusted_digest__mutmut_17 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_18'] = x_trusted_digest__mutmut_18 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_19'] = x_trusted_digest__mutmut_19 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_20'] = x_trusted_digest__mutmut_20 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_21'] = x_trusted_digest__mutmut_21 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_22'] = x_trusted_digest__mutmut_22 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_23'] = x_trusted_digest__mutmut_23 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_24'] = x_trusted_digest__mutmut_24 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_25'] = x_trusted_digest__mutmut_25 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_26'] = x_trusted_digest__mutmut_26 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_27'] = x_trusted_digest__mutmut_27 # type: ignore # mutmut generated
mutants_x_trusted_digest__mutmut['x_trusted_digest__mutmut_28'] = x_trusted_digest__mutmut_28 # type: ignore # mutmut generated
mutants_x_identity__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_identity__mutmut)
def identity(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_orig(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_1(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = None
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_2(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(None)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_3(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(None, value) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_4(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", None) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_5(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(value) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_6(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", ) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_7(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"XX[0-9a-f]{40}|[0-9a-f]{64}XX", value) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_8(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9A-F]{40}|[0-9A-F]{64}", value) is None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_9(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is not None:
        raise ValueError("coverage identity requires an immutable full Git commit")
    return value


def x_identity__mutmut_10(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None:
        raise ValueError(None)
    return value


def x_identity__mutmut_11(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None:
        raise ValueError("XXcoverage identity requires an immutable full Git commitXX")
    return value


def x_identity__mutmut_12(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None:
        raise ValueError("coverage identity requires an immutable full git commit")
    return value


def x_identity__mutmut_13(value: object) -> str:
    """Resolve an explicitly named environment input, never a moving Git ref."""
    value = configured(value)
    if re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None:
        raise ValueError("COVERAGE IDENTITY REQUIRES AN IMMUTABLE FULL GIT COMMIT")
    return value

mutants_x_identity__mutmut['_mutmut_orig'] = x_identity__mutmut_orig # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_1'] = x_identity__mutmut_1 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_2'] = x_identity__mutmut_2 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_3'] = x_identity__mutmut_3 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_4'] = x_identity__mutmut_4 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_5'] = x_identity__mutmut_5 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_6'] = x_identity__mutmut_6 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_7'] = x_identity__mutmut_7 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_8'] = x_identity__mutmut_8 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_9'] = x_identity__mutmut_9 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_10'] = x_identity__mutmut_10 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_11'] = x_identity__mutmut_11 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_12'] = x_identity__mutmut_12 # type: ignore # mutmut generated
mutants_x_identity__mutmut['x_identity__mutmut_13'] = x_identity__mutmut_13 # type: ignore # mutmut generated
mutants_x_git__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_git__mutmut)
def git(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("utf-8", "surrogateescape").strip("\n")


def x_git__mutmut_orig(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("utf-8", "surrogateescape").strip("\n")


def x_git__mutmut_1(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("utf-8", "surrogateescape").strip(None)


def x_git__mutmut_2(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode(None, "surrogateescape").strip("\n")


def x_git__mutmut_3(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("utf-8", None).strip("\n")


def x_git__mutmut_4(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("surrogateescape").strip("\n")


def x_git__mutmut_5(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("utf-8", ).strip("\n")


def x_git__mutmut_6(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(None, *args).decode("utf-8", "surrogateescape").strip("\n")


def x_git__mutmut_7(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(*args).decode("utf-8", "surrogateescape").strip("\n")


def x_git__mutmut_8(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, ).decode("utf-8", "surrogateescape").strip("\n")


def x_git__mutmut_9(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("XXutf-8XX", "surrogateescape").strip("\n")


def x_git__mutmut_10(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("UTF-8", "surrogateescape").strip("\n")


def x_git__mutmut_11(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("utf-8", "XXsurrogateescapeXX").strip("\n")


def x_git__mutmut_12(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("utf-8", "SURROGATEESCAPE").strip("\n")


def x_git__mutmut_13(root: Path, *args: str) -> str:
    """Use the check's fixed-argv Git boundary and fail on unavailable history."""
    return git_bytes(root, *args).decode("utf-8", "surrogateescape").strip("XX\nXX")

mutants_x_git__mutmut['_mutmut_orig'] = x_git__mutmut_orig # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_1'] = x_git__mutmut_1 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_2'] = x_git__mutmut_2 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_3'] = x_git__mutmut_3 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_4'] = x_git__mutmut_4 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_5'] = x_git__mutmut_5 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_6'] = x_git__mutmut_6 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_7'] = x_git__mutmut_7 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_8'] = x_git__mutmut_8 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_9'] = x_git__mutmut_9 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_10'] = x_git__mutmut_10 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_11'] = x_git__mutmut_11 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_12'] = x_git__mutmut_12 # type: ignore # mutmut generated
mutants_x_git__mutmut['x_git__mutmut_13'] = x_git__mutmut_13 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_git_bytes__mutmut)
def git_bytes(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_orig(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_1(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = None
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_2(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(None, root)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_3(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), None)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_4(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(root)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_5(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), )
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_6(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(None), root)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_7(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError(None)
    return result.stdout


def x_git_bytes__mutmut_8(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " - " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_9(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError("XXcoverage Git evidence unavailable: XX" + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_10(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError("coverage git evidence unavailable: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_11(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError("COVERAGE GIT EVIDENCE UNAVAILABLE: " + " ".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_12(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(None))
    return result.stdout


def x_git_bytes__mutmut_13(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + "XX XX".join(args[:2]))
    return result.stdout


def x_git_bytes__mutmut_14(root: Path, *args: str) -> bytes:
    from tc_fitness.core_checks.new_code_coverage import _default_git_runner

    result = _default_git_runner(list(args), root)
    if result.returncode:
        raise ValueError("coverage Git evidence unavailable: " + " ".join(args[:3]))
    return result.stdout

mutants_x_git_bytes__mutmut['_mutmut_orig'] = x_git_bytes__mutmut_orig # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_1'] = x_git_bytes__mutmut_1 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_2'] = x_git_bytes__mutmut_2 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_3'] = x_git_bytes__mutmut_3 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_4'] = x_git_bytes__mutmut_4 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_5'] = x_git_bytes__mutmut_5 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_6'] = x_git_bytes__mutmut_6 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_7'] = x_git_bytes__mutmut_7 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_8'] = x_git_bytes__mutmut_8 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_9'] = x_git_bytes__mutmut_9 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_10'] = x_git_bytes__mutmut_10 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_11'] = x_git_bytes__mutmut_11 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_12'] = x_git_bytes__mutmut_12 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_13'] = x_git_bytes__mutmut_13 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_14'] = x_git_bytes__mutmut_14 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_exact_checkout__mutmut)
def exact_checkout(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_orig(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_1(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = None
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_2(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(None), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_3(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(None)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_4(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(None, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_5(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, None, "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_6(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", None, "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_7(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", None) != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_8(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git("rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_9(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_10(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_11(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", ) != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_12(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "XXrev-parseXX", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_13(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "REV-PARSE", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_14(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "XX--verifyXX", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_15(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--VERIFY", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_16(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "XXHEAD^{commit}XX") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_17(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "head^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_18(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{COMMIT}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_19(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") == candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_20(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError(None)
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_21(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("XXcoverage candidate does not match HEADXX")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_22(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match head")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_23(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("COVERAGE CANDIDATE DOES NOT MATCH HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_24(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(None, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_25(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, None, "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_26(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", None, base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_27(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", None) != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_28(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git("rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_29(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_30(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_31(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", ) != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_32(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "XXrev-parseXX", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_33(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "REV-PARSE", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_34(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "XX--verifyXX", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_35(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--VERIFY", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_36(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base - "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_37(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "XX^{commit}XX") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_38(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{COMMIT}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_39(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") == base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_40(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError(None)
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_41(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("XXcoverage exact base is not a commitXX")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_42(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("COVERAGE EXACT BASE IS NOT A COMMIT")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_43(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(None, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_44(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, None, "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_45(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", None, base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_46(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", None, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_47(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, None)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_48(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git("merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_49(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_50(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_51(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_52(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, )
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_53(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "XXmerge-baseXX", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_54(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "MERGE-BASE", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_55(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "XX--is-ancestorXX", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_56(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--IS-ANCESTOR", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_57(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(None, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_58(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, None, "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_59(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", None, "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_60(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", None, "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_61(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", None, "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_62(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", None)
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_63(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git("diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_64(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_65(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_66(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_67(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_68(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", )
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_69(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "XXdiffXX", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_70(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "DIFF", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_71(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "XX--quietXX", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_72(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--QUIET", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_73(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "XX--no-ext-diffXX", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_74(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--NO-EXT-DIFF", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_75(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "XXHEADXX", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_76(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "head", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_77(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "XX--XX")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_78(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(None, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_79(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, None, "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_80(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", None, "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_81(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", None, "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_82(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", None, "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_83(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", None):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_84(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git("ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_85(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_86(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_87(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_88(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_89(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", ):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_90(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "XXls-filesXX", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_91(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "LS-FILES", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_92(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "XX--othersXX", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_93(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--OTHERS", "--exclude-standard", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_94(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "XX--exclude-standardXX", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_95(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--EXCLUDE-STANDARD", "-z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_96(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "XX-zXX", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_97(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-Z", "--"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_98(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "XX--XX"):
        raise ValueError("coverage candidate contains uncommitted files")


def x_exact_checkout__mutmut_99(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError(None)


def x_exact_checkout__mutmut_100(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("XXcoverage candidate contains uncommitted filesXX")


def x_exact_checkout__mutmut_101(root: Path, base: str, candidate: str) -> None:
    """Bind the full tracked checkout to HEAD and the declared exact ancestor."""
    base, candidate = identity(base), identity(candidate)
    if git(root, "rev-parse", "--verify", "HEAD^{commit}") != candidate:
        raise ValueError("coverage candidate does not match HEAD")
    if git(root, "rev-parse", "--verify", base + "^{commit}") != base:
        raise ValueError("coverage exact base is not a commit")
    git(root, "merge-base", "--is-ancestor", base, candidate)
    git(root, "diff", "--quiet", "--no-ext-diff", "HEAD", "--")
    if git(root, "ls-files", "--others", "--exclude-standard", "-z", "--"):
        raise ValueError("COVERAGE CANDIDATE CONTAINS UNCOMMITTED FILES")

mutants_x_exact_checkout__mutmut['_mutmut_orig'] = x_exact_checkout__mutmut_orig # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_1'] = x_exact_checkout__mutmut_1 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_2'] = x_exact_checkout__mutmut_2 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_3'] = x_exact_checkout__mutmut_3 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_4'] = x_exact_checkout__mutmut_4 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_5'] = x_exact_checkout__mutmut_5 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_6'] = x_exact_checkout__mutmut_6 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_7'] = x_exact_checkout__mutmut_7 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_8'] = x_exact_checkout__mutmut_8 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_9'] = x_exact_checkout__mutmut_9 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_10'] = x_exact_checkout__mutmut_10 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_11'] = x_exact_checkout__mutmut_11 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_12'] = x_exact_checkout__mutmut_12 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_13'] = x_exact_checkout__mutmut_13 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_14'] = x_exact_checkout__mutmut_14 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_15'] = x_exact_checkout__mutmut_15 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_16'] = x_exact_checkout__mutmut_16 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_17'] = x_exact_checkout__mutmut_17 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_18'] = x_exact_checkout__mutmut_18 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_19'] = x_exact_checkout__mutmut_19 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_20'] = x_exact_checkout__mutmut_20 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_21'] = x_exact_checkout__mutmut_21 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_22'] = x_exact_checkout__mutmut_22 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_23'] = x_exact_checkout__mutmut_23 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_24'] = x_exact_checkout__mutmut_24 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_25'] = x_exact_checkout__mutmut_25 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_26'] = x_exact_checkout__mutmut_26 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_27'] = x_exact_checkout__mutmut_27 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_28'] = x_exact_checkout__mutmut_28 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_29'] = x_exact_checkout__mutmut_29 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_30'] = x_exact_checkout__mutmut_30 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_31'] = x_exact_checkout__mutmut_31 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_32'] = x_exact_checkout__mutmut_32 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_33'] = x_exact_checkout__mutmut_33 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_34'] = x_exact_checkout__mutmut_34 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_35'] = x_exact_checkout__mutmut_35 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_36'] = x_exact_checkout__mutmut_36 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_37'] = x_exact_checkout__mutmut_37 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_38'] = x_exact_checkout__mutmut_38 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_39'] = x_exact_checkout__mutmut_39 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_40'] = x_exact_checkout__mutmut_40 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_41'] = x_exact_checkout__mutmut_41 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_42'] = x_exact_checkout__mutmut_42 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_43'] = x_exact_checkout__mutmut_43 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_44'] = x_exact_checkout__mutmut_44 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_45'] = x_exact_checkout__mutmut_45 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_46'] = x_exact_checkout__mutmut_46 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_47'] = x_exact_checkout__mutmut_47 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_48'] = x_exact_checkout__mutmut_48 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_49'] = x_exact_checkout__mutmut_49 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_50'] = x_exact_checkout__mutmut_50 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_51'] = x_exact_checkout__mutmut_51 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_52'] = x_exact_checkout__mutmut_52 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_53'] = x_exact_checkout__mutmut_53 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_54'] = x_exact_checkout__mutmut_54 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_55'] = x_exact_checkout__mutmut_55 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_56'] = x_exact_checkout__mutmut_56 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_57'] = x_exact_checkout__mutmut_57 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_58'] = x_exact_checkout__mutmut_58 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_59'] = x_exact_checkout__mutmut_59 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_60'] = x_exact_checkout__mutmut_60 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_61'] = x_exact_checkout__mutmut_61 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_62'] = x_exact_checkout__mutmut_62 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_63'] = x_exact_checkout__mutmut_63 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_64'] = x_exact_checkout__mutmut_64 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_65'] = x_exact_checkout__mutmut_65 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_66'] = x_exact_checkout__mutmut_66 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_67'] = x_exact_checkout__mutmut_67 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_68'] = x_exact_checkout__mutmut_68 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_69'] = x_exact_checkout__mutmut_69 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_70'] = x_exact_checkout__mutmut_70 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_71'] = x_exact_checkout__mutmut_71 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_72'] = x_exact_checkout__mutmut_72 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_73'] = x_exact_checkout__mutmut_73 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_74'] = x_exact_checkout__mutmut_74 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_75'] = x_exact_checkout__mutmut_75 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_76'] = x_exact_checkout__mutmut_76 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_77'] = x_exact_checkout__mutmut_77 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_78'] = x_exact_checkout__mutmut_78 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_79'] = x_exact_checkout__mutmut_79 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_80'] = x_exact_checkout__mutmut_80 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_81'] = x_exact_checkout__mutmut_81 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_82'] = x_exact_checkout__mutmut_82 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_83'] = x_exact_checkout__mutmut_83 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_84'] = x_exact_checkout__mutmut_84 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_85'] = x_exact_checkout__mutmut_85 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_86'] = x_exact_checkout__mutmut_86 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_87'] = x_exact_checkout__mutmut_87 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_88'] = x_exact_checkout__mutmut_88 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_89'] = x_exact_checkout__mutmut_89 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_90'] = x_exact_checkout__mutmut_90 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_91'] = x_exact_checkout__mutmut_91 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_92'] = x_exact_checkout__mutmut_92 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_93'] = x_exact_checkout__mutmut_93 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_94'] = x_exact_checkout__mutmut_94 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_95'] = x_exact_checkout__mutmut_95 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_96'] = x_exact_checkout__mutmut_96 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_97'] = x_exact_checkout__mutmut_97 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_98'] = x_exact_checkout__mutmut_98 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_99'] = x_exact_checkout__mutmut_99 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_100'] = x_exact_checkout__mutmut_100 # type: ignore # mutmut generated
mutants_x_exact_checkout__mutmut['x_exact_checkout__mutmut_101'] = x_exact_checkout__mutmut_101 # type: ignore # mutmut generated
mutants_x_source_files__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_source_files__mutmut)
def source_files(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_orig(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_1(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = None
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_2(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_3(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError(None)
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_4(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("XXcoverage requires non-empty source rootsXX")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_5(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("COVERAGE REQUIRES NON-EMPTY SOURCE ROOTS")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_6(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = None
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_7(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root * name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_8(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts and not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_9(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() and ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_10(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(None).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_11(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or "XX..XX" in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_12(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." not in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_13(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(None).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_14(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_15(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError(None)
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_16(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("XXcoverage requires existing repository-relative rootsXX")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_17(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("COVERAGE REQUIRES EXISTING REPOSITORY-RELATIVE ROOTS")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_18(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob(None):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_19(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("XX*.pyXX"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_20(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.PY"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_21(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() and not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_22(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_23(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(None):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_24(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError(None)
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_25(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("XXcoverage source cannot escape through symlinksXX")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_26(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("COVERAGE SOURCE CANNOT ESCAPE THROUGH SYMLINKS")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_27(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = None
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_28(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(None).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_29(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_30(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError(None)
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_31(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("XXcoverage scope contains no Python sourceXX")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_32(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_33(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("COVERAGE SCOPE CONTAINS NO PYTHON SOURCE")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_34(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = None
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_35(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(None)
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_36(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split(None))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_37(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(None, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_38(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, None, "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_39(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", None, "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_40(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", None, *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_41(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git("ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_42(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_43(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_44(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_45(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", ).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_46(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "XXls-filesXX", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_47(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "LS-FILES", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_48(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "XX-zXX", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_49(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-Z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_50(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "XX--XX", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_51(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("XX\0XX"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_52(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) + tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_53(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(None) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_54(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError(None)
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_55(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("XXcoverage source contains uncommitted filesXX")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_56(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("COVERAGE SOURCE CONTAINS UNCOMMITTED FILES")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_57(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() == git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_58(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(None, "show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_59(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, None, "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_60(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", None):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_61(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes("show", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_62(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_63(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", ):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_64(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "XXshowXX", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_65(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "SHOW", "HEAD:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_66(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" - name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_67(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "XXHEAD:XX" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_68(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "head:" + name):
            raise ValueError("coverage source bytes differ from the candidate")
    return files


def x_source_files__mutmut_69(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError(None)
    return files


def x_source_files__mutmut_70(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("XXcoverage source bytes differ from the candidateXX")
    return files


def x_source_files__mutmut_71(root: Path, roots: list[str]) -> dict[str, Path]:
    """Enumerate the complete Python scope, including untracked source."""
    files: dict[str, Path] = {}
    if not roots:
        raise ValueError("coverage requires non-empty source roots")
    for name in roots:
        path = root / name
        if Path(name).is_absolute() or ".." in Path(name).parts or not path.is_dir():
            raise ValueError("coverage requires existing repository-relative roots")
        for source in path.rglob("*.py"):
            if source.is_symlink() or not source.resolve().is_relative_to(root.resolve()):
                raise ValueError("coverage source cannot escape through symlinks")
            if source.is_file():
                files[source.relative_to(root).as_posix()] = source
    if not files:
        raise ValueError("coverage scope contains no Python source")
    tracked = set(git(root, "ls-files", "-z", "--", *roots).split("\0"))
    if set(files) - tracked:
        raise ValueError("coverage source contains uncommitted files")
    # Git's stat cache and assume-unchanged/skip-worktree flags are not proof
    # that the measured bytes are the committed candidate.
    for name, path in files.items():
        if path.read_bytes() != git_bytes(root, "show", "HEAD:" + name):
            raise ValueError("COVERAGE SOURCE BYTES DIFFER FROM THE CANDIDATE")
    return files

mutants_x_source_files__mutmut['_mutmut_orig'] = x_source_files__mutmut_orig # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_1'] = x_source_files__mutmut_1 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_2'] = x_source_files__mutmut_2 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_3'] = x_source_files__mutmut_3 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_4'] = x_source_files__mutmut_4 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_5'] = x_source_files__mutmut_5 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_6'] = x_source_files__mutmut_6 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_7'] = x_source_files__mutmut_7 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_8'] = x_source_files__mutmut_8 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_9'] = x_source_files__mutmut_9 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_10'] = x_source_files__mutmut_10 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_11'] = x_source_files__mutmut_11 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_12'] = x_source_files__mutmut_12 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_13'] = x_source_files__mutmut_13 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_14'] = x_source_files__mutmut_14 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_15'] = x_source_files__mutmut_15 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_16'] = x_source_files__mutmut_16 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_17'] = x_source_files__mutmut_17 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_18'] = x_source_files__mutmut_18 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_19'] = x_source_files__mutmut_19 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_20'] = x_source_files__mutmut_20 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_21'] = x_source_files__mutmut_21 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_22'] = x_source_files__mutmut_22 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_23'] = x_source_files__mutmut_23 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_24'] = x_source_files__mutmut_24 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_25'] = x_source_files__mutmut_25 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_26'] = x_source_files__mutmut_26 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_27'] = x_source_files__mutmut_27 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_28'] = x_source_files__mutmut_28 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_29'] = x_source_files__mutmut_29 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_30'] = x_source_files__mutmut_30 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_31'] = x_source_files__mutmut_31 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_32'] = x_source_files__mutmut_32 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_33'] = x_source_files__mutmut_33 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_34'] = x_source_files__mutmut_34 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_35'] = x_source_files__mutmut_35 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_36'] = x_source_files__mutmut_36 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_37'] = x_source_files__mutmut_37 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_38'] = x_source_files__mutmut_38 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_39'] = x_source_files__mutmut_39 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_40'] = x_source_files__mutmut_40 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_41'] = x_source_files__mutmut_41 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_42'] = x_source_files__mutmut_42 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_43'] = x_source_files__mutmut_43 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_44'] = x_source_files__mutmut_44 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_45'] = x_source_files__mutmut_45 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_46'] = x_source_files__mutmut_46 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_47'] = x_source_files__mutmut_47 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_48'] = x_source_files__mutmut_48 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_49'] = x_source_files__mutmut_49 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_50'] = x_source_files__mutmut_50 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_51'] = x_source_files__mutmut_51 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_52'] = x_source_files__mutmut_52 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_53'] = x_source_files__mutmut_53 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_54'] = x_source_files__mutmut_54 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_55'] = x_source_files__mutmut_55 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_56'] = x_source_files__mutmut_56 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_57'] = x_source_files__mutmut_57 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_58'] = x_source_files__mutmut_58 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_59'] = x_source_files__mutmut_59 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_60'] = x_source_files__mutmut_60 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_61'] = x_source_files__mutmut_61 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_62'] = x_source_files__mutmut_62 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_63'] = x_source_files__mutmut_63 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_64'] = x_source_files__mutmut_64 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_65'] = x_source_files__mutmut_65 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_66'] = x_source_files__mutmut_66 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_67'] = x_source_files__mutmut_67 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_68'] = x_source_files__mutmut_68 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_69'] = x_source_files__mutmut_69 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_70'] = x_source_files__mutmut_70 # type: ignore # mutmut generated
mutants_x_source_files__mutmut['x_source_files__mutmut_71'] = x_source_files__mutmut_71 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_complete_line_hits__mutmut)
def complete_line_hits(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_orig(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_1(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = None
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_2(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(None, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_3(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=None)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_4(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_5(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, )
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_6(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(None) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_7(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) == set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_8(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(None):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_9(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError(None)
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_10(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("XXcoverage report does not measure the complete source setXX")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_11(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("COVERAGE REPORT DOES NOT MEASURE THE COMPLETE SOURCE SET")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_12(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = None
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_13(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(None).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_14(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = None
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_15(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter(None) if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_16(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("XXsourceXX") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_17(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("SOURCE") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_18(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = None
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_19(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=None, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_20(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_21(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, )
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_22(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module(None).Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_23(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("XXcoverageXX").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_24(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("COVERAGE").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_25(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=True, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_26(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option(None, [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_27(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", None)
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_28(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option([])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_29(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", )
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_30(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("XXreport:exclude_linesXX", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_31(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("REPORT:EXCLUDE_LINES", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_32(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option(None, [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_33(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", None)
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_34(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option([])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_35(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", )
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_36(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("XXreport:partial_branchesXX", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_37(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("REPORT:PARTIAL_BRANCHES", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_38(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = None
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_39(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter(None):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_40(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("XXclassXX"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_41(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("CLASS"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_42(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = None
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_43(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(None, sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_44(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), None, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_45(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=None)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_46(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_47(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_48(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, )
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_49(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get(None), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_50(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("XXfilenameXX"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_51(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("FILENAME"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_52(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = None
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_53(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(None): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_54(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get(None)): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_55(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("XXnumberXX")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_56(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("NUMBER")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_57(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(None) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_58(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get(None)) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_59(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("XXhitsXX")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_60(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("HITS")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_61(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find(None)}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_62(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.rfind("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_63(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("XXlinesXX")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_64(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("LINES")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_65(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = None
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_66(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(None)
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_67(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(None)[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_68(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(None))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_69(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[2])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_70(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(None) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_71(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) == statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_72(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError(None)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_73(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " - relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_74(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("XXcoverage report omits or invents executable source lines: XX" + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_75(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("COVERAGE REPORT OMITS OR INVENTS EXECUTABLE SOURCE LINES: " + relative)
        result[relative] = hits
    return result


def x_complete_line_hits__mutmut_76(root: Path, report: Path, files: dict[str, Path]) -> dict[str, dict[int, int]]:
    """Validate all detail against the actual Coverage.py statement inventory."""
    from tc_fitness.core_checks.new_code_coverage import _resolve_element_tree

    details = parse_coverage_details(report, repo_root=root)
    if set(details) != set(files):
        raise ValueError("coverage report does not measure the complete source set")
    parsed = _resolve_element_tree().parse(report).getroot()
    sources = [element.text for element in parsed.iter("source") if element.text]
    analyzer = importlib.import_module("coverage").Coverage(config_file=False, data_file=None)
    analyzer.set_option("report:exclude_lines", [])
    analyzer.set_option("report:partial_branches", [])
    result: dict[str, dict[int, int]] = {}
    for element in parsed.iter("class"):
        relative = resolve_coverage_filename(element.get("filename"), sources, repo_root=root)
        hits = {int(line.get("number")): int(line.get("hits")) for line in element.find("lines")}
        statements = set(analyzer.analysis2(str(files[relative]))[1])
        if set(hits) != statements:
            raise ValueError("coverage report omits or invents executable source lines: " + relative)
        result[relative] = None
    return result

mutants_x_complete_line_hits__mutmut['_mutmut_orig'] = x_complete_line_hits__mutmut_orig # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_1'] = x_complete_line_hits__mutmut_1 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_2'] = x_complete_line_hits__mutmut_2 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_3'] = x_complete_line_hits__mutmut_3 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_4'] = x_complete_line_hits__mutmut_4 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_5'] = x_complete_line_hits__mutmut_5 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_6'] = x_complete_line_hits__mutmut_6 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_7'] = x_complete_line_hits__mutmut_7 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_8'] = x_complete_line_hits__mutmut_8 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_9'] = x_complete_line_hits__mutmut_9 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_10'] = x_complete_line_hits__mutmut_10 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_11'] = x_complete_line_hits__mutmut_11 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_12'] = x_complete_line_hits__mutmut_12 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_13'] = x_complete_line_hits__mutmut_13 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_14'] = x_complete_line_hits__mutmut_14 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_15'] = x_complete_line_hits__mutmut_15 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_16'] = x_complete_line_hits__mutmut_16 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_17'] = x_complete_line_hits__mutmut_17 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_18'] = x_complete_line_hits__mutmut_18 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_19'] = x_complete_line_hits__mutmut_19 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_20'] = x_complete_line_hits__mutmut_20 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_21'] = x_complete_line_hits__mutmut_21 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_22'] = x_complete_line_hits__mutmut_22 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_23'] = x_complete_line_hits__mutmut_23 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_24'] = x_complete_line_hits__mutmut_24 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_25'] = x_complete_line_hits__mutmut_25 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_26'] = x_complete_line_hits__mutmut_26 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_27'] = x_complete_line_hits__mutmut_27 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_28'] = x_complete_line_hits__mutmut_28 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_29'] = x_complete_line_hits__mutmut_29 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_30'] = x_complete_line_hits__mutmut_30 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_31'] = x_complete_line_hits__mutmut_31 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_32'] = x_complete_line_hits__mutmut_32 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_33'] = x_complete_line_hits__mutmut_33 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_34'] = x_complete_line_hits__mutmut_34 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_35'] = x_complete_line_hits__mutmut_35 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_36'] = x_complete_line_hits__mutmut_36 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_37'] = x_complete_line_hits__mutmut_37 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_38'] = x_complete_line_hits__mutmut_38 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_39'] = x_complete_line_hits__mutmut_39 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_40'] = x_complete_line_hits__mutmut_40 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_41'] = x_complete_line_hits__mutmut_41 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_42'] = x_complete_line_hits__mutmut_42 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_43'] = x_complete_line_hits__mutmut_43 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_44'] = x_complete_line_hits__mutmut_44 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_45'] = x_complete_line_hits__mutmut_45 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_46'] = x_complete_line_hits__mutmut_46 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_47'] = x_complete_line_hits__mutmut_47 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_48'] = x_complete_line_hits__mutmut_48 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_49'] = x_complete_line_hits__mutmut_49 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_50'] = x_complete_line_hits__mutmut_50 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_51'] = x_complete_line_hits__mutmut_51 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_52'] = x_complete_line_hits__mutmut_52 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_53'] = x_complete_line_hits__mutmut_53 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_54'] = x_complete_line_hits__mutmut_54 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_55'] = x_complete_line_hits__mutmut_55 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_56'] = x_complete_line_hits__mutmut_56 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_57'] = x_complete_line_hits__mutmut_57 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_58'] = x_complete_line_hits__mutmut_58 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_59'] = x_complete_line_hits__mutmut_59 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_60'] = x_complete_line_hits__mutmut_60 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_61'] = x_complete_line_hits__mutmut_61 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_62'] = x_complete_line_hits__mutmut_62 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_63'] = x_complete_line_hits__mutmut_63 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_64'] = x_complete_line_hits__mutmut_64 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_65'] = x_complete_line_hits__mutmut_65 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_66'] = x_complete_line_hits__mutmut_66 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_67'] = x_complete_line_hits__mutmut_67 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_68'] = x_complete_line_hits__mutmut_68 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_69'] = x_complete_line_hits__mutmut_69 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_70'] = x_complete_line_hits__mutmut_70 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_71'] = x_complete_line_hits__mutmut_71 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_72'] = x_complete_line_hits__mutmut_72 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_73'] = x_complete_line_hits__mutmut_73 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_74'] = x_complete_line_hits__mutmut_74 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_75'] = x_complete_line_hits__mutmut_75 # type: ignore # mutmut generated
mutants_x_complete_line_hits__mutmut['x_complete_line_hits__mutmut_76'] = x_complete_line_hits__mutmut_76 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_changed_line_failures__mutmut)
def changed_line_failures(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_orig(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_1(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 and config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_2(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get(None) != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_3(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("XXfloor_pctXX") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_4(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("FLOOR_PCT") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_5(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") == 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_6(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 101 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_7(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get(None, [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_8(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", None) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_9(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get([".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_10(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", ) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_11(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("XXextensionsXX", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_12(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("EXTENSIONS", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_13(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", ["XX.pyXX"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_14(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".PY"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_15(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) == [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_16(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != ["XX.pyXX"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_17(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".PY"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_18(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError(None)
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_19(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("XXexact-base coverage requires 100 percent Python coverageXX")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_20(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_21(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("EXACT-BASE COVERAGE REQUIRES 100 PERCENT PYTHON COVERAGE")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_22(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = None
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_23(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(None), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_24(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get(None)), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_25(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("XXexact_base_commitXX")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_26(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("EXACT_BASE_COMMIT")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_27(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(None)
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_28(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get(None))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_29(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("XXcandidate_commitXX"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_30(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("CANDIDATE_COMMIT"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_31(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(None, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_32(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, None, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_33(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, None)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_34(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_35(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_36(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, )
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_37(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = None
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_38(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(None, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_39(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, None)
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_40(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_41(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, )
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_42(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get(None, []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_43(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", None))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_44(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get([]))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_45(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", ))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_46(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("XXrootsXX", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_47(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("ROOTS", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_48(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = None
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_49(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(None, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_50(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, None, files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_51(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), None)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_52(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_53(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_54(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), )
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_55(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root * configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_56(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(None), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_57(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get(None, "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_58(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", None)), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_59(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_60(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", )), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_61(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("XXcoverage_reportXX", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_62(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("COVERAGE_REPORT", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_63(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "XXcoverage.xmlXX")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_64(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "COVERAGE.XML")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_65(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = None
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_66(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(None, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_67(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, None) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_68(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_69(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, ) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_70(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "XXcoverage_receiptXX" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_71(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "COVERAGE_RECEIPT" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_72(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" not in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_73(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = None
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_74(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            None,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_75(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            None,
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_76(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            None,
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_77(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            None,
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_78(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            None,
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_79(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            None,
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_80(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            None,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_81(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            None,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_82(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            None,
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_83(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            None,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_84(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_85(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_86(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_87(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_88(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_89(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_90(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_91(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_92(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_93(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_94(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "XXdiffXX",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_95(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "DIFF",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_96(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "XX--no-ext-diffXX",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_97(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--NO-EXT-DIFF",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_98(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "XX--no-textconvXX",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_99(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--NO-TEXTCONV",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_100(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "XX--no-renamesXX",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_101(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--NO-RENAMES",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_102(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "XX--unified=0XX",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_103(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--UNIFIED=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_104(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "XX--XX",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_105(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = None
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_106(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(None).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_107(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = None
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_108(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(None)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_109(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] or hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_110(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line not in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_111(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] != 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_112(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 1)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_113(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = None
    return failures


def x_changed_line_failures__mutmut_114(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " - ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_115(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "XXuncovered changed executable lines: XX" + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_116(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "UNCOVERED CHANGED EXECUTABLE LINES: " + ", ".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_117(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(None)
    return failures


def x_changed_line_failures__mutmut_118(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + "XX, XX".join(map(str, missing))
    return failures


def x_changed_line_failures__mutmut_119(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(None, missing))
    return failures


def x_changed_line_failures__mutmut_120(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, None))
    return failures


def x_changed_line_failures__mutmut_121(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(missing))
    return failures


def x_changed_line_failures__mutmut_122(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Score every exact-base changed executable line at a hard 100 percent."""
    from tc_fitness.core_checks.new_code_coverage import parse_added_lines

    if config.get("floor_pct") != 100 or config.get("extensions", [".py"]) != [".py"]:
        raise ValueError("exact-base coverage requires 100 percent Python coverage")
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    files = source_files(root, config.get("roots", []))
    hits = complete_line_hits(root, root / configured(config.get("coverage_report", "coverage.xml")), files)
    failures: dict[str, str] = receipt_failures(root, config) if "coverage_receipt" in config else {}
    for relative in files:
        diff = git(
            root,
            "diff",
            "--no-ext-diff",
            "--no-textconv",
            "--no-renames",
            "--unified=0",
            base,
            candidate,
            "--",
            relative,
        )
        changed = set().union(*parse_added_lines(diff).values())
        missing = sorted(line for line in changed if line in hits[relative] and hits[relative][line] == 0)
        if missing:
            failures[relative] = "uncovered changed executable lines: " + ", ".join(map(str, ))
    return failures

mutants_x_changed_line_failures__mutmut['_mutmut_orig'] = x_changed_line_failures__mutmut_orig # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_1'] = x_changed_line_failures__mutmut_1 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_2'] = x_changed_line_failures__mutmut_2 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_3'] = x_changed_line_failures__mutmut_3 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_4'] = x_changed_line_failures__mutmut_4 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_5'] = x_changed_line_failures__mutmut_5 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_6'] = x_changed_line_failures__mutmut_6 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_7'] = x_changed_line_failures__mutmut_7 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_8'] = x_changed_line_failures__mutmut_8 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_9'] = x_changed_line_failures__mutmut_9 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_10'] = x_changed_line_failures__mutmut_10 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_11'] = x_changed_line_failures__mutmut_11 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_12'] = x_changed_line_failures__mutmut_12 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_13'] = x_changed_line_failures__mutmut_13 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_14'] = x_changed_line_failures__mutmut_14 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_15'] = x_changed_line_failures__mutmut_15 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_16'] = x_changed_line_failures__mutmut_16 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_17'] = x_changed_line_failures__mutmut_17 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_18'] = x_changed_line_failures__mutmut_18 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_19'] = x_changed_line_failures__mutmut_19 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_20'] = x_changed_line_failures__mutmut_20 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_21'] = x_changed_line_failures__mutmut_21 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_22'] = x_changed_line_failures__mutmut_22 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_23'] = x_changed_line_failures__mutmut_23 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_24'] = x_changed_line_failures__mutmut_24 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_25'] = x_changed_line_failures__mutmut_25 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_26'] = x_changed_line_failures__mutmut_26 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_27'] = x_changed_line_failures__mutmut_27 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_28'] = x_changed_line_failures__mutmut_28 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_29'] = x_changed_line_failures__mutmut_29 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_30'] = x_changed_line_failures__mutmut_30 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_31'] = x_changed_line_failures__mutmut_31 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_32'] = x_changed_line_failures__mutmut_32 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_33'] = x_changed_line_failures__mutmut_33 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_34'] = x_changed_line_failures__mutmut_34 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_35'] = x_changed_line_failures__mutmut_35 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_36'] = x_changed_line_failures__mutmut_36 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_37'] = x_changed_line_failures__mutmut_37 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_38'] = x_changed_line_failures__mutmut_38 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_39'] = x_changed_line_failures__mutmut_39 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_40'] = x_changed_line_failures__mutmut_40 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_41'] = x_changed_line_failures__mutmut_41 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_42'] = x_changed_line_failures__mutmut_42 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_43'] = x_changed_line_failures__mutmut_43 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_44'] = x_changed_line_failures__mutmut_44 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_45'] = x_changed_line_failures__mutmut_45 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_46'] = x_changed_line_failures__mutmut_46 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_47'] = x_changed_line_failures__mutmut_47 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_48'] = x_changed_line_failures__mutmut_48 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_49'] = x_changed_line_failures__mutmut_49 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_50'] = x_changed_line_failures__mutmut_50 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_51'] = x_changed_line_failures__mutmut_51 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_52'] = x_changed_line_failures__mutmut_52 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_53'] = x_changed_line_failures__mutmut_53 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_54'] = x_changed_line_failures__mutmut_54 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_55'] = x_changed_line_failures__mutmut_55 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_56'] = x_changed_line_failures__mutmut_56 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_57'] = x_changed_line_failures__mutmut_57 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_58'] = x_changed_line_failures__mutmut_58 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_59'] = x_changed_line_failures__mutmut_59 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_60'] = x_changed_line_failures__mutmut_60 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_61'] = x_changed_line_failures__mutmut_61 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_62'] = x_changed_line_failures__mutmut_62 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_63'] = x_changed_line_failures__mutmut_63 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_64'] = x_changed_line_failures__mutmut_64 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_65'] = x_changed_line_failures__mutmut_65 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_66'] = x_changed_line_failures__mutmut_66 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_67'] = x_changed_line_failures__mutmut_67 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_68'] = x_changed_line_failures__mutmut_68 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_69'] = x_changed_line_failures__mutmut_69 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_70'] = x_changed_line_failures__mutmut_70 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_71'] = x_changed_line_failures__mutmut_71 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_72'] = x_changed_line_failures__mutmut_72 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_73'] = x_changed_line_failures__mutmut_73 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_74'] = x_changed_line_failures__mutmut_74 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_75'] = x_changed_line_failures__mutmut_75 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_76'] = x_changed_line_failures__mutmut_76 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_77'] = x_changed_line_failures__mutmut_77 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_78'] = x_changed_line_failures__mutmut_78 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_79'] = x_changed_line_failures__mutmut_79 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_80'] = x_changed_line_failures__mutmut_80 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_81'] = x_changed_line_failures__mutmut_81 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_82'] = x_changed_line_failures__mutmut_82 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_83'] = x_changed_line_failures__mutmut_83 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_84'] = x_changed_line_failures__mutmut_84 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_85'] = x_changed_line_failures__mutmut_85 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_86'] = x_changed_line_failures__mutmut_86 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_87'] = x_changed_line_failures__mutmut_87 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_88'] = x_changed_line_failures__mutmut_88 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_89'] = x_changed_line_failures__mutmut_89 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_90'] = x_changed_line_failures__mutmut_90 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_91'] = x_changed_line_failures__mutmut_91 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_92'] = x_changed_line_failures__mutmut_92 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_93'] = x_changed_line_failures__mutmut_93 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_94'] = x_changed_line_failures__mutmut_94 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_95'] = x_changed_line_failures__mutmut_95 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_96'] = x_changed_line_failures__mutmut_96 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_97'] = x_changed_line_failures__mutmut_97 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_98'] = x_changed_line_failures__mutmut_98 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_99'] = x_changed_line_failures__mutmut_99 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_100'] = x_changed_line_failures__mutmut_100 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_101'] = x_changed_line_failures__mutmut_101 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_102'] = x_changed_line_failures__mutmut_102 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_103'] = x_changed_line_failures__mutmut_103 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_104'] = x_changed_line_failures__mutmut_104 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_105'] = x_changed_line_failures__mutmut_105 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_106'] = x_changed_line_failures__mutmut_106 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_107'] = x_changed_line_failures__mutmut_107 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_108'] = x_changed_line_failures__mutmut_108 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_109'] = x_changed_line_failures__mutmut_109 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_110'] = x_changed_line_failures__mutmut_110 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_111'] = x_changed_line_failures__mutmut_111 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_112'] = x_changed_line_failures__mutmut_112 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_113'] = x_changed_line_failures__mutmut_113 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_114'] = x_changed_line_failures__mutmut_114 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_115'] = x_changed_line_failures__mutmut_115 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_116'] = x_changed_line_failures__mutmut_116 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_117'] = x_changed_line_failures__mutmut_117 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_118'] = x_changed_line_failures__mutmut_118 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_119'] = x_changed_line_failures__mutmut_119 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_120'] = x_changed_line_failures__mutmut_120 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_121'] = x_changed_line_failures__mutmut_121 # type: ignore # mutmut generated
mutants_x_changed_line_failures__mutmut['x_changed_line_failures__mutmut_122'] = x_changed_line_failures__mutmut_122 # type: ignore # mutmut generated
mutants_x_measure__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_measure__mutmut)
def measure(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_orig(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_1(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = None
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_2(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(None, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_3(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, None, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_4(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, None)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_5(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_6(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_7(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, )
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_8(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = None
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_9(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(None, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_10(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=None)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_11(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_12(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, )
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_13(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(None, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_14(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, None, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_15(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, None, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_16(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, None, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_17(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, None)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_18(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_19(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_20(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_21(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_22(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, )
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_23(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = None
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_24(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(None) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_25(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(None)}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_26(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "XXfilesXX": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_27(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "FILES": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_28(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "XXcountsXX": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_29(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "COUNTS": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_30(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(None)
            for key in ("lines", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_31(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("XXlinesXX", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_32(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("LINES", "covered_lines", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_33(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "XXcovered_linesXX", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_34(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "COVERED_LINES", "branches", "covered_branches")
        },
    }


def x_measure__mutmut_35(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "XXbranchesXX", "covered_branches")
        },
    }


def x_measure__mutmut_36(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "BRANCHES", "covered_branches")
        },
    }


def x_measure__mutmut_37(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "XXcovered_branchesXX")
        },
    }


def x_measure__mutmut_38(root: Path, report: Path, files: dict[str, Path]) -> dict[str, Any]:
    """Retain exact per-file and package counts after complete-source validation."""
    from tc_fitness.coverage_measurement import cross_check_branches

    hits = complete_line_hits(root, report, files)
    counts = parse_coverage_details(report, repo_root=root)
    cross_check_branches(root, report, files, hits, counts)
    per_file = {name: asdict(value) for name, value in sorted(counts.items())}
    return {
        "files": per_file,
        "counts": {
            key: sum(value[key] for value in per_file.values())
            for key in ("lines", "covered_lines", "branches", "COVERED_BRANCHES")
        },
    }

mutants_x_measure__mutmut['_mutmut_orig'] = x_measure__mutmut_orig # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_1'] = x_measure__mutmut_1 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_2'] = x_measure__mutmut_2 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_3'] = x_measure__mutmut_3 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_4'] = x_measure__mutmut_4 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_5'] = x_measure__mutmut_5 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_6'] = x_measure__mutmut_6 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_7'] = x_measure__mutmut_7 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_8'] = x_measure__mutmut_8 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_9'] = x_measure__mutmut_9 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_10'] = x_measure__mutmut_10 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_11'] = x_measure__mutmut_11 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_12'] = x_measure__mutmut_12 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_13'] = x_measure__mutmut_13 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_14'] = x_measure__mutmut_14 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_15'] = x_measure__mutmut_15 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_16'] = x_measure__mutmut_16 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_17'] = x_measure__mutmut_17 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_18'] = x_measure__mutmut_18 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_19'] = x_measure__mutmut_19 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_20'] = x_measure__mutmut_20 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_21'] = x_measure__mutmut_21 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_22'] = x_measure__mutmut_22 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_23'] = x_measure__mutmut_23 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_24'] = x_measure__mutmut_24 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_25'] = x_measure__mutmut_25 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_26'] = x_measure__mutmut_26 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_27'] = x_measure__mutmut_27 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_28'] = x_measure__mutmut_28 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_29'] = x_measure__mutmut_29 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_30'] = x_measure__mutmut_30 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_31'] = x_measure__mutmut_31 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_32'] = x_measure__mutmut_32 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_33'] = x_measure__mutmut_33 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_34'] = x_measure__mutmut_34 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_35'] = x_measure__mutmut_35 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_36'] = x_measure__mutmut_36 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_37'] = x_measure__mutmut_37 # type: ignore # mutmut generated
mutants_x_measure__mutmut['x_measure__mutmut_38'] = x_measure__mutmut_38 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_read_receipt__mutmut)
def read_receipt(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_orig(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_1(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = None
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_2(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(None)
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_3(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) and payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_4(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_5(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get(None) != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_6(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("XXschemaXX") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_7(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("SCHEMA") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_8(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") == SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_9(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError(None)
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_10(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("XXinvalid coverage receipt schemaXX")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_11(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("INVALID COVERAGE RECEIPT SCHEMA")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_12(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = None
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_13(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop(None, None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_14(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop(None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_15(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", )
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_16(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("XXdigestXX", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_17(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("DIGEST", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_18(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest and actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_19(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual == expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_20(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual == digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_21(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(None):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_22(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError(None)
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_23(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("XXcoverage receipt digest mismatchXX")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_24(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("COVERAGE RECEIPT DIGEST MISMATCH")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_25(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["XXreportsXX"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_26(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["REPORTS"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_27(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"} and bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_28(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_29(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"XXcoverage.xmlXX", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_30(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"COVERAGE.XML", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_31(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "XXcoverage.jsonXX"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_32(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "COVERAGE.JSON"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_33(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest(None) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_34(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent * name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_35(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) == expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_36(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError(None)
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_37(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("XXcoverage report digest mismatchXX")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_38(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("COVERAGE REPORT DIGEST MISMATCH")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_39(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(None) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_40(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["XXreportsXX"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_41(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["REPORTS"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_42(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) == {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_43(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"XXcoverage.xmlXX", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_44(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"COVERAGE.XML", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_45(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "XXcoverage.jsonXX"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_46(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "COVERAGE.JSON"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_47(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError(None)
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_48(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("XXcoverage receipt requires both producer reportsXX")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_49(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("COVERAGE RECEIPT REQUIRES BOTH PRODUCER REPORTS")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_50(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = None
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_51(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["XXcountsXX"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_52(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["COUNTS"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_53(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} and any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_54(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(None) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_55(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) == {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_56(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"XXlinesXX", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_57(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"LINES", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_58(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "XXcovered_linesXX", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_59(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "COVERED_LINES", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_60(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "XXbranchesXX", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_61(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "BRANCHES", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_62(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "XXcovered_branchesXX"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_63(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "COVERED_BRANCHES"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_64(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        None
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_65(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int and value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_66(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(None) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_67(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_68(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value <= 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_69(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 1 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_70(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError(None)
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_71(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("XXcoverage receipt requires exact non-negative countsXX")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_72(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("COVERAGE RECEIPT REQUIRES EXACT NON-NEGATIVE COUNTS")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_73(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"] and counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_74(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"] and counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_75(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_76(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["XXlinesXX"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_77(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["LINES"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_78(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["XXcovered_linesXX"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_79(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["COVERED_LINES"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_80(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] >= counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_81(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["XXlinesXX"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_82(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["LINES"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_83(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["XXcovered_branchesXX"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_84(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["COVERED_BRANCHES"] > counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_85(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] >= counts["branches"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_86(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["XXbranchesXX"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_87(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["BRANCHES"]
    ):
        raise ValueError("invalid coverage receipt opportunity counts")
    return payload


def x_read_receipt__mutmut_88(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError(None)
    return payload


def x_read_receipt__mutmut_89(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("XXinvalid coverage receipt opportunity countsXX")
    return payload


def x_read_receipt__mutmut_90(path: Path, expected_digest: str) -> dict[str, Any]:
    """Check integrity against an independently supplied trusted digest."""
    payload = json.loads(path.read_text())
    if not isinstance(payload, dict) or payload.get("schema") != SCHEMA:
        raise ValueError("invalid coverage receipt schema")
    actual = payload.pop("digest", None)
    if actual != expected_digest or actual != digest(payload):
        raise ValueError("coverage receipt digest mismatch")
    for name, expected in payload["reports"].items():
        if (
            name not in {"coverage.xml", "coverage.json"}
            or bytes_digest((path.parent / name).read_bytes()) != expected
        ):
            raise ValueError("coverage report digest mismatch")
    if set(payload["reports"]) != {"coverage.xml", "coverage.json"}:
        raise ValueError("coverage receipt requires both producer reports")
    counts = payload["counts"]
    if set(counts) != {"lines", "covered_lines", "branches", "covered_branches"} or any(
        type(value) is not int or value < 0 for value in counts.values()
    ):
        raise ValueError("coverage receipt requires exact non-negative counts")
    if (
        not counts["lines"]
        or counts["covered_lines"] > counts["lines"]
        or counts["covered_branches"] > counts["branches"]
    ):
        raise ValueError("INVALID COVERAGE RECEIPT OPPORTUNITY COUNTS")
    return payload

mutants_x_read_receipt__mutmut['_mutmut_orig'] = x_read_receipt__mutmut_orig # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_1'] = x_read_receipt__mutmut_1 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_2'] = x_read_receipt__mutmut_2 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_3'] = x_read_receipt__mutmut_3 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_4'] = x_read_receipt__mutmut_4 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_5'] = x_read_receipt__mutmut_5 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_6'] = x_read_receipt__mutmut_6 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_7'] = x_read_receipt__mutmut_7 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_8'] = x_read_receipt__mutmut_8 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_9'] = x_read_receipt__mutmut_9 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_10'] = x_read_receipt__mutmut_10 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_11'] = x_read_receipt__mutmut_11 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_12'] = x_read_receipt__mutmut_12 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_13'] = x_read_receipt__mutmut_13 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_14'] = x_read_receipt__mutmut_14 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_15'] = x_read_receipt__mutmut_15 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_16'] = x_read_receipt__mutmut_16 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_17'] = x_read_receipt__mutmut_17 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_18'] = x_read_receipt__mutmut_18 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_19'] = x_read_receipt__mutmut_19 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_20'] = x_read_receipt__mutmut_20 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_21'] = x_read_receipt__mutmut_21 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_22'] = x_read_receipt__mutmut_22 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_23'] = x_read_receipt__mutmut_23 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_24'] = x_read_receipt__mutmut_24 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_25'] = x_read_receipt__mutmut_25 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_26'] = x_read_receipt__mutmut_26 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_27'] = x_read_receipt__mutmut_27 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_28'] = x_read_receipt__mutmut_28 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_29'] = x_read_receipt__mutmut_29 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_30'] = x_read_receipt__mutmut_30 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_31'] = x_read_receipt__mutmut_31 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_32'] = x_read_receipt__mutmut_32 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_33'] = x_read_receipt__mutmut_33 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_34'] = x_read_receipt__mutmut_34 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_35'] = x_read_receipt__mutmut_35 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_36'] = x_read_receipt__mutmut_36 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_37'] = x_read_receipt__mutmut_37 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_38'] = x_read_receipt__mutmut_38 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_39'] = x_read_receipt__mutmut_39 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_40'] = x_read_receipt__mutmut_40 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_41'] = x_read_receipt__mutmut_41 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_42'] = x_read_receipt__mutmut_42 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_43'] = x_read_receipt__mutmut_43 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_44'] = x_read_receipt__mutmut_44 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_45'] = x_read_receipt__mutmut_45 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_46'] = x_read_receipt__mutmut_46 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_47'] = x_read_receipt__mutmut_47 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_48'] = x_read_receipt__mutmut_48 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_49'] = x_read_receipt__mutmut_49 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_50'] = x_read_receipt__mutmut_50 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_51'] = x_read_receipt__mutmut_51 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_52'] = x_read_receipt__mutmut_52 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_53'] = x_read_receipt__mutmut_53 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_54'] = x_read_receipt__mutmut_54 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_55'] = x_read_receipt__mutmut_55 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_56'] = x_read_receipt__mutmut_56 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_57'] = x_read_receipt__mutmut_57 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_58'] = x_read_receipt__mutmut_58 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_59'] = x_read_receipt__mutmut_59 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_60'] = x_read_receipt__mutmut_60 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_61'] = x_read_receipt__mutmut_61 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_62'] = x_read_receipt__mutmut_62 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_63'] = x_read_receipt__mutmut_63 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_64'] = x_read_receipt__mutmut_64 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_65'] = x_read_receipt__mutmut_65 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_66'] = x_read_receipt__mutmut_66 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_67'] = x_read_receipt__mutmut_67 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_68'] = x_read_receipt__mutmut_68 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_69'] = x_read_receipt__mutmut_69 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_70'] = x_read_receipt__mutmut_70 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_71'] = x_read_receipt__mutmut_71 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_72'] = x_read_receipt__mutmut_72 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_73'] = x_read_receipt__mutmut_73 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_74'] = x_read_receipt__mutmut_74 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_75'] = x_read_receipt__mutmut_75 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_76'] = x_read_receipt__mutmut_76 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_77'] = x_read_receipt__mutmut_77 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_78'] = x_read_receipt__mutmut_78 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_79'] = x_read_receipt__mutmut_79 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_80'] = x_read_receipt__mutmut_80 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_81'] = x_read_receipt__mutmut_81 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_82'] = x_read_receipt__mutmut_82 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_83'] = x_read_receipt__mutmut_83 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_84'] = x_read_receipt__mutmut_84 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_85'] = x_read_receipt__mutmut_85 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_86'] = x_read_receipt__mutmut_86 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_87'] = x_read_receipt__mutmut_87 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_88'] = x_read_receipt__mutmut_88 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_89'] = x_read_receipt__mutmut_89 # type: ignore # mutmut generated
mutants_x_read_receipt__mutmut['x_read_receipt__mutmut_90'] = x_read_receipt__mutmut_90 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_receipt_failures__mutmut)
def receipt_failures(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_orig(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_1(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = None
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_2(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(None), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_3(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get(None)), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_4(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("XXexact_base_commitXX")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_5(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("EXACT_BASE_COMMIT")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_6(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(None)
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_7(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get(None))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_8(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("XXcandidate_commitXX"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_9(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("CANDIDATE_COMMIT"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_10(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(None, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_11(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, None, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_12(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, None)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_13(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_14(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_15(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, )
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_16(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = None
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_17(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get(None, [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_18(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", None)
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_19(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get([])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_20(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", )
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_21(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("XXrootsXX", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_22(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("ROOTS", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_23(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = None
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_24(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(None, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_25(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, None)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_26(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_27(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, )
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_28(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = None
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_29(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root * configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_30(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(None)
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_31(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get(None))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_32(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("XXcoverage_receiptXX"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_33(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("COVERAGE_RECEIPT"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_34(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = None
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_35(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root * configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_36(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(None)
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_37(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get(None))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_38(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("XXaccepted_coverage_receiptXX"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_39(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("ACCEPTED_COVERAGE_RECEIPT"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_40(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = None
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_41(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(None, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_42(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, None)
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_43(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_44(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, )
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_45(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(None, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_46(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, None))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_47(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_48(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, ))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_49(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get(None)))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_50(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("XXcoverage_receipt_digestXX")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_51(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("COVERAGE_RECEIPT_DIGEST")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_52(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = None
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_53(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(None, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_54(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, None)
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_55(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_56(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, )
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_57(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(None, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_58(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, None))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_59(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_60(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, ))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_61(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get(None)))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_62(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("XXaccepted_coverage_digestXX")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_63(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("ACCEPTED_COVERAGE_DIGEST")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_64(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate and accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_65(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base and current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_66(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["XXbase_commitXX"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_67(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["BASE_COMMIT"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_68(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] == base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_69(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["XXcandidate_commitXX"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_70(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["CANDIDATE_COMMIT"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_71(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] == candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_72(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["XXcandidate_commitXX"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_73(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["CANDIDATE_COMMIT"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_74(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] == base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_75(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError(None)
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_76(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("XXcoverage receipt does not bind the exact base and candidateXX")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_77(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("COVERAGE RECEIPT DOES NOT BIND THE EXACT BASE AND CANDIDATE")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_78(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) and accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_79(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["XXrootsXX"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_80(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["ROOTS"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_81(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] == sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_82(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(None) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_83(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["XXrootsXX"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_84(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["ROOTS"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_85(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] == sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_86(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(None):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_87(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError(None)
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_88(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("XXcoverage receipt changes the measured source scopeXX")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_89(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("COVERAGE RECEIPT CHANGES THE MEASURED SOURCE SCOPE")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_90(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = None
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_91(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(None)
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_92(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get(None))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_93(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("XXcoverage_configXX"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_94(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("COVERAGE_CONFIG"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_95(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() and ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_96(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(None).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_97(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or "XX..XX" in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_98(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." not in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_99(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(None).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_100(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError(None)
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_101(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("XXcoverage configuration must be repository-relativeXX")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_102(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("COVERAGE CONFIGURATION MUST BE REPOSITORY-RELATIVE")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_103(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name and receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_104(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["XXconfig_pathXX"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_105(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["CONFIG_PATH"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_106(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] == config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_107(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["XXconfig_digestXX"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_108(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["CONFIG_DIGEST"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_109(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] == bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_110(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            None
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_111(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(None, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_112(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, None, commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_113(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", None)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_114(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes("show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_115(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_116(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", )
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_117(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "XXshowXX", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_118(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "SHOW", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_119(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" - config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_120(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit - ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_121(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + "XX:XX" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_122(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError(None)
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_123(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("XXcoverage receipt configuration identity mismatchXX")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_124(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("COVERAGE RECEIPT CONFIGURATION IDENTITY MISMATCH")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_125(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = None
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_126(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split(None)
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_127(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(None, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_128(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, None, "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_129(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", None, "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_130(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", None, "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_131(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", None, commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_132(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", None, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_133(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, None, *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_134(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git("ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_135(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_136(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_137(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_138(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_139(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_140(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_141(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", ).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_142(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "XXls-treeXX", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_143(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "LS-TREE", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_144(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "XX-rXX", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_145(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-R", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_146(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "XX--name-onlyXX", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_147(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--NAME-ONLY", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_148(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "XX-zXX", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_149(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-Z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_150(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "XX--XX", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_151(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("XX\0XX")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_152(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(None)
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_153(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith("XX.pyXX")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_154(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".PY")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_155(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = None
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_156(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            None
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_157(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(None) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_158(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(None, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_159(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, None, commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_160(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", None)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_161(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes("show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_162(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_163(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", )) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_164(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "XXshowXX", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_165(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "SHOW", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_166(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" - name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_167(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit - ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_168(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + "XX:XX" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_169(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(None)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_170(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["XXsource_digestXX"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_171(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["SOURCE_DIGEST"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_172(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] == source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_173(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError(None)
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_174(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("XXcoverage receipt source identity mismatchXX")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_175(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("COVERAGE RECEIPT SOURCE IDENTITY MISMATCH")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_176(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = None
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_177(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["XXexecutionXX"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_178(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["EXECUTION"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_179(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(None):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_180(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] == configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_181(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(None) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_182(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(None)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_183(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("XXrun_idXX", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_184(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("RUN_ID", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_185(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "XXattempt_idXX")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_186(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "ATTEMPT_ID")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_187(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError(None)
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_188(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("XXcoverage receipt belongs to a stale run or attemptXX")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_189(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("COVERAGE RECEIPT BELONGS TO A STALE RUN OR ATTEMPT")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_190(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = None
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_191(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(None) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_192(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("XXstarted_atXX", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_193(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("STARTED_AT", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_194(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "XXfinished_atXX"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_195(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "FINISHED_AT"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_196(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = None
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_197(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(None)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_198(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = None
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_199(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get(None, 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_200(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", None)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_201(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get(3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_202(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", )
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_203(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("XXmax_age_secondsXX", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_204(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("MAX_AGE_SECONDS", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_205(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3601)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_206(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) and age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_207(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) and not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_208(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) and not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_209(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_210(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_211(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(None) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_212(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age < 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_213(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 1:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_214(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError(None)
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_215(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("XXcoverage freshness requires a finite positive age limitXX")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_216(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("COVERAGE FRESHNESS REQUIRES A FINITE POSITIVE AGE LIMIT")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_217(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now and (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_218(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None and not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_219(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None and finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_220(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is not None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_221(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is not None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_222(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_223(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started < finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_224(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished < now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_225(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now + started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_226(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() >= age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_227(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError(None)
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_228(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("XXcoverage receipt is expired, future-dated or invalidXX")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_229(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("COVERAGE RECEIPT IS EXPIRED, FUTURE-DATED OR INVALID")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_230(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = None
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_231(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root * configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_232(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(None)
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_233(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get(None, "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_234(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", None))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_235(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_236(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", ))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_237(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("XXcoverage_reportXX", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_238(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("COVERAGE_REPORT", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_239(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "XXcoverage.xmlXX"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_240(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "COVERAGE.XML"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_241(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() == (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_242(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent * "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_243(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "XXcoverage.xmlXX").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_244(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "COVERAGE.XML").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_245(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError(None)
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_246(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("XXcoverage admission report is not the bound producer reportXX")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_247(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("COVERAGE ADMISSION REPORT IS NOT THE BOUND PRODUCER REPORT")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_248(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = None
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_249(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(None, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_250(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, None, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_251(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, None)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_252(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_253(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_254(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, )
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_255(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(None):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_256(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] == current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_257(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("XXfilesXX", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_258(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("FILES", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_259(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "XXcountsXX")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_260(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "COUNTS")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_261(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError(None)
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_262(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("XXcoverage receipt counts disagree with actual source detailXX")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_263(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("COVERAGE RECEIPT COUNTS DISAGREE WITH ACTUAL SOURCE DETAIL")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_264(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = None
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_265(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("XXlinesXX", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_266(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("LINES", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_267(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "XXbranchesXX"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_268(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "BRANCHES"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_269(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = None
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_270(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["XXcountsXX"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_271(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["COUNTS"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_272(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["XXcountsXX"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_273(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["COUNTS"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_274(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = None
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_275(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = None
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_276(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" - kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_277(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["XXcovered_XX" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_278(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["COVERED_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_279(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 2
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_280(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = None
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_281(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" - kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_282(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["XXcovered_XX" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_283(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["COVERED_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_284(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 2
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_285(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit / (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_286(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total and 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_287(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 2) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_288(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) <= before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_289(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit / (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_290(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total and 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_291(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 2):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_292(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                None
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_293(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'XXlineXX' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_294(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'LINE' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_295(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind != 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_296(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'XXlinesXX' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_297(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'LINES' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_298(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'XXbranchXX'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_299(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'BRANCH'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_300(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {"XX.XX": "; ".join(messages)} if messages else {}


def x_receipt_failures__mutmut_301(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "; ".join(None)} if messages else {}


def x_receipt_failures__mutmut_302(root: Path, config: dict[str, Any]) -> dict[str, str]:
    """Validate current execution, exact accepted base and monotonic counts.

    Digest anchors MUST come from the trusted producer/admission handoff, not
    be copied from the files being checked. Selecting the latest accepted
    digest is the caller's storage/CI responsibility; no receipt accepts itself.
    """
    base, candidate = identity(config.get("exact_base_commit")), identity(config.get("candidate_commit"))
    exact_checkout(root, base, candidate)
    roots = config.get("roots", [])
    files = source_files(root, roots)
    current_path = root / configured(config.get("coverage_receipt"))
    accepted_path = root / configured(config.get("accepted_coverage_receipt"))
    current = read_receipt(current_path, trusted_digest(root, config.get("coverage_receipt_digest")))
    accepted = read_receipt(accepted_path, trusted_digest(root, config.get("accepted_coverage_digest")))
    if (
        current["base_commit"] != base
        or current["candidate_commit"] != candidate
        or accepted["candidate_commit"] != base
    ):
        raise ValueError("coverage receipt does not bind the exact base and candidate")
    if current["roots"] != sorted(roots) or accepted["roots"] != sorted(roots):
        raise ValueError("coverage receipt changes the measured source scope")
    config_name = configured(config.get("coverage_config"))
    if Path(config_name).is_absolute() or ".." in Path(config_name).parts:
        raise ValueError("coverage configuration must be repository-relative")
    for receipt, commit in ((current, candidate), (accepted, base)):
        if receipt["config_path"] != config_name or receipt["config_digest"] != bytes_digest(
            git_bytes(root, "show", commit + ":" + config_name)
        ):
            raise ValueError("coverage receipt configuration identity mismatch")
        names = [
            name
            for name in git(root, "ls-tree", "-r", "--name-only", "-z", commit, "--", *roots).split("\0")
            if name.endswith(".py")
        ]
        source_hash = digest(
            {name: bytes_digest(git_bytes(root, "show", commit + ":" + name)) for name in sorted(names)}
        )
        if receipt["source_digest"] != source_hash:
            raise ValueError("coverage receipt source identity mismatch")
    execution = current["execution"]
    if any(execution[key] != configured(config.get(key)) for key in ("run_id", "attempt_id")):
        raise ValueError("coverage receipt belongs to a stale run or attempt")
    started, finished = (datetime.fromisoformat(execution[key]) for key in ("started_at", "finished_at"))
    now = datetime.now(UTC)
    age = config.get("max_age_seconds", 3600)
    if isinstance(age, bool) or not isinstance(age, int | float) or not math.isfinite(age) or age <= 0:
        raise ValueError("coverage freshness requires a finite positive age limit")
    if (
        started.tzinfo is None
        or finished.tzinfo is None
        or not started <= finished <= now
        or (now - started).total_seconds() > age
    ):
        raise ValueError("coverage receipt is expired, future-dated or invalid")
    report = root / configured(config.get("coverage_report", "coverage.xml"))
    if report.resolve() != (current_path.parent / "coverage.xml").resolve():
        raise ValueError("coverage admission report is not the bound producer report")
    measured = measure(root, report, files)
    if any(measured[key] != current[key] for key in ("files", "counts")):
        raise ValueError("coverage receipt counts disagree with actual source detail")
    messages = []
    for kind in ("lines", "branches"):
        before, after = accepted["counts"], current["counts"]
        before_total, after_total = before[kind], after[kind]
        # Zero opportunities are fully covered, not an undefined percentage.
        before_hit = before["covered_" + kind] if before_total else 1
        after_hit = after["covered_" + kind] if after_total else 1
        if after_hit * (before_total or 1) < before_hit * (after_total or 1):
            messages.append(
                f"{'line' if kind == 'lines' else 'branch'} coverage decreased from accepted exact base"
            )
    return {".": "XX; XX".join(messages)} if messages else {}

mutants_x_receipt_failures__mutmut['_mutmut_orig'] = x_receipt_failures__mutmut_orig # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_1'] = x_receipt_failures__mutmut_1 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_2'] = x_receipt_failures__mutmut_2 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_3'] = x_receipt_failures__mutmut_3 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_4'] = x_receipt_failures__mutmut_4 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_5'] = x_receipt_failures__mutmut_5 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_6'] = x_receipt_failures__mutmut_6 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_7'] = x_receipt_failures__mutmut_7 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_8'] = x_receipt_failures__mutmut_8 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_9'] = x_receipt_failures__mutmut_9 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_10'] = x_receipt_failures__mutmut_10 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_11'] = x_receipt_failures__mutmut_11 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_12'] = x_receipt_failures__mutmut_12 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_13'] = x_receipt_failures__mutmut_13 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_14'] = x_receipt_failures__mutmut_14 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_15'] = x_receipt_failures__mutmut_15 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_16'] = x_receipt_failures__mutmut_16 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_17'] = x_receipt_failures__mutmut_17 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_18'] = x_receipt_failures__mutmut_18 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_19'] = x_receipt_failures__mutmut_19 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_20'] = x_receipt_failures__mutmut_20 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_21'] = x_receipt_failures__mutmut_21 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_22'] = x_receipt_failures__mutmut_22 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_23'] = x_receipt_failures__mutmut_23 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_24'] = x_receipt_failures__mutmut_24 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_25'] = x_receipt_failures__mutmut_25 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_26'] = x_receipt_failures__mutmut_26 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_27'] = x_receipt_failures__mutmut_27 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_28'] = x_receipt_failures__mutmut_28 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_29'] = x_receipt_failures__mutmut_29 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_30'] = x_receipt_failures__mutmut_30 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_31'] = x_receipt_failures__mutmut_31 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_32'] = x_receipt_failures__mutmut_32 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_33'] = x_receipt_failures__mutmut_33 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_34'] = x_receipt_failures__mutmut_34 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_35'] = x_receipt_failures__mutmut_35 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_36'] = x_receipt_failures__mutmut_36 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_37'] = x_receipt_failures__mutmut_37 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_38'] = x_receipt_failures__mutmut_38 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_39'] = x_receipt_failures__mutmut_39 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_40'] = x_receipt_failures__mutmut_40 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_41'] = x_receipt_failures__mutmut_41 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_42'] = x_receipt_failures__mutmut_42 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_43'] = x_receipt_failures__mutmut_43 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_44'] = x_receipt_failures__mutmut_44 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_45'] = x_receipt_failures__mutmut_45 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_46'] = x_receipt_failures__mutmut_46 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_47'] = x_receipt_failures__mutmut_47 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_48'] = x_receipt_failures__mutmut_48 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_49'] = x_receipt_failures__mutmut_49 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_50'] = x_receipt_failures__mutmut_50 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_51'] = x_receipt_failures__mutmut_51 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_52'] = x_receipt_failures__mutmut_52 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_53'] = x_receipt_failures__mutmut_53 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_54'] = x_receipt_failures__mutmut_54 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_55'] = x_receipt_failures__mutmut_55 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_56'] = x_receipt_failures__mutmut_56 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_57'] = x_receipt_failures__mutmut_57 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_58'] = x_receipt_failures__mutmut_58 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_59'] = x_receipt_failures__mutmut_59 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_60'] = x_receipt_failures__mutmut_60 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_61'] = x_receipt_failures__mutmut_61 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_62'] = x_receipt_failures__mutmut_62 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_63'] = x_receipt_failures__mutmut_63 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_64'] = x_receipt_failures__mutmut_64 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_65'] = x_receipt_failures__mutmut_65 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_66'] = x_receipt_failures__mutmut_66 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_67'] = x_receipt_failures__mutmut_67 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_68'] = x_receipt_failures__mutmut_68 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_69'] = x_receipt_failures__mutmut_69 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_70'] = x_receipt_failures__mutmut_70 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_71'] = x_receipt_failures__mutmut_71 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_72'] = x_receipt_failures__mutmut_72 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_73'] = x_receipt_failures__mutmut_73 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_74'] = x_receipt_failures__mutmut_74 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_75'] = x_receipt_failures__mutmut_75 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_76'] = x_receipt_failures__mutmut_76 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_77'] = x_receipt_failures__mutmut_77 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_78'] = x_receipt_failures__mutmut_78 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_79'] = x_receipt_failures__mutmut_79 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_80'] = x_receipt_failures__mutmut_80 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_81'] = x_receipt_failures__mutmut_81 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_82'] = x_receipt_failures__mutmut_82 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_83'] = x_receipt_failures__mutmut_83 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_84'] = x_receipt_failures__mutmut_84 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_85'] = x_receipt_failures__mutmut_85 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_86'] = x_receipt_failures__mutmut_86 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_87'] = x_receipt_failures__mutmut_87 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_88'] = x_receipt_failures__mutmut_88 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_89'] = x_receipt_failures__mutmut_89 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_90'] = x_receipt_failures__mutmut_90 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_91'] = x_receipt_failures__mutmut_91 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_92'] = x_receipt_failures__mutmut_92 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_93'] = x_receipt_failures__mutmut_93 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_94'] = x_receipt_failures__mutmut_94 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_95'] = x_receipt_failures__mutmut_95 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_96'] = x_receipt_failures__mutmut_96 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_97'] = x_receipt_failures__mutmut_97 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_98'] = x_receipt_failures__mutmut_98 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_99'] = x_receipt_failures__mutmut_99 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_100'] = x_receipt_failures__mutmut_100 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_101'] = x_receipt_failures__mutmut_101 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_102'] = x_receipt_failures__mutmut_102 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_103'] = x_receipt_failures__mutmut_103 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_104'] = x_receipt_failures__mutmut_104 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_105'] = x_receipt_failures__mutmut_105 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_106'] = x_receipt_failures__mutmut_106 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_107'] = x_receipt_failures__mutmut_107 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_108'] = x_receipt_failures__mutmut_108 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_109'] = x_receipt_failures__mutmut_109 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_110'] = x_receipt_failures__mutmut_110 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_111'] = x_receipt_failures__mutmut_111 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_112'] = x_receipt_failures__mutmut_112 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_113'] = x_receipt_failures__mutmut_113 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_114'] = x_receipt_failures__mutmut_114 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_115'] = x_receipt_failures__mutmut_115 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_116'] = x_receipt_failures__mutmut_116 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_117'] = x_receipt_failures__mutmut_117 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_118'] = x_receipt_failures__mutmut_118 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_119'] = x_receipt_failures__mutmut_119 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_120'] = x_receipt_failures__mutmut_120 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_121'] = x_receipt_failures__mutmut_121 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_122'] = x_receipt_failures__mutmut_122 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_123'] = x_receipt_failures__mutmut_123 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_124'] = x_receipt_failures__mutmut_124 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_125'] = x_receipt_failures__mutmut_125 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_126'] = x_receipt_failures__mutmut_126 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_127'] = x_receipt_failures__mutmut_127 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_128'] = x_receipt_failures__mutmut_128 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_129'] = x_receipt_failures__mutmut_129 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_130'] = x_receipt_failures__mutmut_130 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_131'] = x_receipt_failures__mutmut_131 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_132'] = x_receipt_failures__mutmut_132 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_133'] = x_receipt_failures__mutmut_133 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_134'] = x_receipt_failures__mutmut_134 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_135'] = x_receipt_failures__mutmut_135 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_136'] = x_receipt_failures__mutmut_136 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_137'] = x_receipt_failures__mutmut_137 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_138'] = x_receipt_failures__mutmut_138 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_139'] = x_receipt_failures__mutmut_139 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_140'] = x_receipt_failures__mutmut_140 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_141'] = x_receipt_failures__mutmut_141 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_142'] = x_receipt_failures__mutmut_142 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_143'] = x_receipt_failures__mutmut_143 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_144'] = x_receipt_failures__mutmut_144 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_145'] = x_receipt_failures__mutmut_145 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_146'] = x_receipt_failures__mutmut_146 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_147'] = x_receipt_failures__mutmut_147 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_148'] = x_receipt_failures__mutmut_148 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_149'] = x_receipt_failures__mutmut_149 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_150'] = x_receipt_failures__mutmut_150 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_151'] = x_receipt_failures__mutmut_151 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_152'] = x_receipt_failures__mutmut_152 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_153'] = x_receipt_failures__mutmut_153 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_154'] = x_receipt_failures__mutmut_154 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_155'] = x_receipt_failures__mutmut_155 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_156'] = x_receipt_failures__mutmut_156 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_157'] = x_receipt_failures__mutmut_157 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_158'] = x_receipt_failures__mutmut_158 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_159'] = x_receipt_failures__mutmut_159 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_160'] = x_receipt_failures__mutmut_160 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_161'] = x_receipt_failures__mutmut_161 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_162'] = x_receipt_failures__mutmut_162 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_163'] = x_receipt_failures__mutmut_163 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_164'] = x_receipt_failures__mutmut_164 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_165'] = x_receipt_failures__mutmut_165 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_166'] = x_receipt_failures__mutmut_166 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_167'] = x_receipt_failures__mutmut_167 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_168'] = x_receipt_failures__mutmut_168 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_169'] = x_receipt_failures__mutmut_169 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_170'] = x_receipt_failures__mutmut_170 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_171'] = x_receipt_failures__mutmut_171 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_172'] = x_receipt_failures__mutmut_172 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_173'] = x_receipt_failures__mutmut_173 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_174'] = x_receipt_failures__mutmut_174 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_175'] = x_receipt_failures__mutmut_175 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_176'] = x_receipt_failures__mutmut_176 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_177'] = x_receipt_failures__mutmut_177 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_178'] = x_receipt_failures__mutmut_178 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_179'] = x_receipt_failures__mutmut_179 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_180'] = x_receipt_failures__mutmut_180 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_181'] = x_receipt_failures__mutmut_181 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_182'] = x_receipt_failures__mutmut_182 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_183'] = x_receipt_failures__mutmut_183 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_184'] = x_receipt_failures__mutmut_184 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_185'] = x_receipt_failures__mutmut_185 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_186'] = x_receipt_failures__mutmut_186 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_187'] = x_receipt_failures__mutmut_187 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_188'] = x_receipt_failures__mutmut_188 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_189'] = x_receipt_failures__mutmut_189 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_190'] = x_receipt_failures__mutmut_190 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_191'] = x_receipt_failures__mutmut_191 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_192'] = x_receipt_failures__mutmut_192 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_193'] = x_receipt_failures__mutmut_193 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_194'] = x_receipt_failures__mutmut_194 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_195'] = x_receipt_failures__mutmut_195 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_196'] = x_receipt_failures__mutmut_196 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_197'] = x_receipt_failures__mutmut_197 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_198'] = x_receipt_failures__mutmut_198 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_199'] = x_receipt_failures__mutmut_199 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_200'] = x_receipt_failures__mutmut_200 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_201'] = x_receipt_failures__mutmut_201 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_202'] = x_receipt_failures__mutmut_202 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_203'] = x_receipt_failures__mutmut_203 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_204'] = x_receipt_failures__mutmut_204 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_205'] = x_receipt_failures__mutmut_205 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_206'] = x_receipt_failures__mutmut_206 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_207'] = x_receipt_failures__mutmut_207 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_208'] = x_receipt_failures__mutmut_208 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_209'] = x_receipt_failures__mutmut_209 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_210'] = x_receipt_failures__mutmut_210 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_211'] = x_receipt_failures__mutmut_211 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_212'] = x_receipt_failures__mutmut_212 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_213'] = x_receipt_failures__mutmut_213 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_214'] = x_receipt_failures__mutmut_214 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_215'] = x_receipt_failures__mutmut_215 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_216'] = x_receipt_failures__mutmut_216 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_217'] = x_receipt_failures__mutmut_217 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_218'] = x_receipt_failures__mutmut_218 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_219'] = x_receipt_failures__mutmut_219 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_220'] = x_receipt_failures__mutmut_220 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_221'] = x_receipt_failures__mutmut_221 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_222'] = x_receipt_failures__mutmut_222 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_223'] = x_receipt_failures__mutmut_223 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_224'] = x_receipt_failures__mutmut_224 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_225'] = x_receipt_failures__mutmut_225 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_226'] = x_receipt_failures__mutmut_226 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_227'] = x_receipt_failures__mutmut_227 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_228'] = x_receipt_failures__mutmut_228 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_229'] = x_receipt_failures__mutmut_229 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_230'] = x_receipt_failures__mutmut_230 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_231'] = x_receipt_failures__mutmut_231 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_232'] = x_receipt_failures__mutmut_232 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_233'] = x_receipt_failures__mutmut_233 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_234'] = x_receipt_failures__mutmut_234 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_235'] = x_receipt_failures__mutmut_235 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_236'] = x_receipt_failures__mutmut_236 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_237'] = x_receipt_failures__mutmut_237 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_238'] = x_receipt_failures__mutmut_238 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_239'] = x_receipt_failures__mutmut_239 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_240'] = x_receipt_failures__mutmut_240 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_241'] = x_receipt_failures__mutmut_241 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_242'] = x_receipt_failures__mutmut_242 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_243'] = x_receipt_failures__mutmut_243 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_244'] = x_receipt_failures__mutmut_244 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_245'] = x_receipt_failures__mutmut_245 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_246'] = x_receipt_failures__mutmut_246 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_247'] = x_receipt_failures__mutmut_247 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_248'] = x_receipt_failures__mutmut_248 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_249'] = x_receipt_failures__mutmut_249 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_250'] = x_receipt_failures__mutmut_250 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_251'] = x_receipt_failures__mutmut_251 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_252'] = x_receipt_failures__mutmut_252 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_253'] = x_receipt_failures__mutmut_253 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_254'] = x_receipt_failures__mutmut_254 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_255'] = x_receipt_failures__mutmut_255 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_256'] = x_receipt_failures__mutmut_256 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_257'] = x_receipt_failures__mutmut_257 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_258'] = x_receipt_failures__mutmut_258 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_259'] = x_receipt_failures__mutmut_259 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_260'] = x_receipt_failures__mutmut_260 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_261'] = x_receipt_failures__mutmut_261 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_262'] = x_receipt_failures__mutmut_262 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_263'] = x_receipt_failures__mutmut_263 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_264'] = x_receipt_failures__mutmut_264 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_265'] = x_receipt_failures__mutmut_265 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_266'] = x_receipt_failures__mutmut_266 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_267'] = x_receipt_failures__mutmut_267 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_268'] = x_receipt_failures__mutmut_268 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_269'] = x_receipt_failures__mutmut_269 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_270'] = x_receipt_failures__mutmut_270 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_271'] = x_receipt_failures__mutmut_271 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_272'] = x_receipt_failures__mutmut_272 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_273'] = x_receipt_failures__mutmut_273 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_274'] = x_receipt_failures__mutmut_274 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_275'] = x_receipt_failures__mutmut_275 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_276'] = x_receipt_failures__mutmut_276 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_277'] = x_receipt_failures__mutmut_277 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_278'] = x_receipt_failures__mutmut_278 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_279'] = x_receipt_failures__mutmut_279 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_280'] = x_receipt_failures__mutmut_280 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_281'] = x_receipt_failures__mutmut_281 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_282'] = x_receipt_failures__mutmut_282 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_283'] = x_receipt_failures__mutmut_283 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_284'] = x_receipt_failures__mutmut_284 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_285'] = x_receipt_failures__mutmut_285 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_286'] = x_receipt_failures__mutmut_286 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_287'] = x_receipt_failures__mutmut_287 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_288'] = x_receipt_failures__mutmut_288 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_289'] = x_receipt_failures__mutmut_289 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_290'] = x_receipt_failures__mutmut_290 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_291'] = x_receipt_failures__mutmut_291 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_292'] = x_receipt_failures__mutmut_292 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_293'] = x_receipt_failures__mutmut_293 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_294'] = x_receipt_failures__mutmut_294 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_295'] = x_receipt_failures__mutmut_295 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_296'] = x_receipt_failures__mutmut_296 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_297'] = x_receipt_failures__mutmut_297 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_298'] = x_receipt_failures__mutmut_298 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_299'] = x_receipt_failures__mutmut_299 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_300'] = x_receipt_failures__mutmut_300 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_301'] = x_receipt_failures__mutmut_301 # type: ignore # mutmut generated
mutants_x_receipt_failures__mutmut['x_receipt_failures__mutmut_302'] = x_receipt_failures__mutmut_302 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut: MutantDict = {}  # type: ignore


class CoverageExecutionError(ValueError):
    """A terminal native measurement failure with retained structured diagnostics."""

    @_mutmut_mutated(mutants_xǁCoverageExecutionErrorǁ__init____mutmut)
    def __init__(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_orig(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_1(self, phase: str, code: int, output: Path) -> None:
        super().__init__(None)
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_2(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = None
        self.exit_code = code
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_3(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = None
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_4(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = None
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_5(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output * (phase + ".stdout.log")
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_6(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase - ".stdout.log")
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_7(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + "XX.stdout.logXX")
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_8(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + ".STDOUT.LOG")
        self.stderr_log = output / (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_9(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = None

    def xǁCoverageExecutionErrorǁ__init____mutmut_10(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = output * (phase + ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_11(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = output / (phase - ".stderr.log")

    def xǁCoverageExecutionErrorǁ__init____mutmut_12(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = output / (phase + "XX.stderr.logXX")

    def xǁCoverageExecutionErrorǁ__init____mutmut_13(self, phase: str, code: int, output: Path) -> None:
        super().__init__(f"coverage producer {phase} command failed (exit {code}); no receipt emitted")
        self.phase = phase
        self.exit_code = code
        self.stdout_log = output / (phase + ".stdout.log")
        self.stderr_log = output / (phase + ".STDERR.LOG")

mutants_xǁCoverageExecutionErrorǁ__init____mutmut['_mutmut_orig'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_1'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_2'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_3'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_4'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_5'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_6'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_7'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_8'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_9'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_10'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_11'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_12'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁCoverageExecutionErrorǁ__init____mutmut['xǁCoverageExecutionErrorǁ__init____mutmut_13'] = CoverageExecutionError.xǁCoverageExecutionErrorǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_produce_coverage__mutmut)
def produce_coverage(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_orig(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_1(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = None
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_2(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = None
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_3(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(None), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_4(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(None)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_5(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = None
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_6(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(None), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_7(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(None)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_8(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(None, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_9(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, None, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_10(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, None)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_11(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_12(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_13(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, )
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_14(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = None
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_15(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(None, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_16(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, None)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_17(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_18(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, )
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_19(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = None
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_20(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root * config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_21(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() and ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_22(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(None).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_23(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or "XX..XX" in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_24(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." not in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_25(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(None).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_26(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError(None)
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_27(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("XXcoverage configuration must be a tracked repository fileXX")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_28(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("COVERAGE CONFIGURATION MUST BE A TRACKED REPOSITORY FILE")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_29(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = None
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_30(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(None)
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_31(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(None) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_32(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(None, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_33(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, None, candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_34(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", None)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_35(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes("show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_36(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_37(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", )) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_38(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "XXshowXX", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_39(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "SHOW", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_40(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" - config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_41(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate - ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_42(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + "XX:XX" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_43(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) == config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_44(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError(None)
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_45(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("XXcoverage configuration differs from the candidateXX")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_46(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("COVERAGE CONFIGURATION DIFFERS FROM THE CANDIDATE")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_47(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = None
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_48(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest(None)
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_49(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(None) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_50(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(None)})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_51(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_52(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError(None)
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_53(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("XXcoverage producer requires a Python test commandXX")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_54(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_55(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("COVERAGE PRODUCER REQUIRES A PYTHON TEST COMMAND")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_56(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = None
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_57(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_58(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(None)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_59(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() and not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_60(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_61(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_62(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError(None)
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_63(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("XXcoverage producer requires an absolute installed Python interpreterXX")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_64(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_65(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("COVERAGE PRODUCER REQUIRES AN ABSOLUTE INSTALLED PYTHON INTERPRETER")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_66(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=None, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_67(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=None)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_68(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_69(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, )
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_70(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=False, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_71(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=True)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_72(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = None
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_73(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        None,
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_74(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=None,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_75(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=None,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_76(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=None,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_77(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_78(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_79(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_80(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_81(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(None),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_82(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "XX-IXX",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_83(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-i",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_84(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "XX-cXX",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_85(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-C",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_86(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "XXimport sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,XX"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_87(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "IMPORT SYS,JSON,IMPORTLIB.METADATA AS M; PRINT(JSON.DUMPS({'PYTHON':SYS.VERSION,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_88(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "XX'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))XX",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_89(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'PYTHON_EXECUTABLE':SYS.EXECUTABLE,'COVERAGE':M.VERSION('COVERAGE'),'PYTEST':M.VERSION('PYTEST')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_90(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=31,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_91(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(None)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_92(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output * "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_93(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "XXidentity.stdout.logXX").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_94(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "IDENTITY.STDOUT.LOG").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_95(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(None)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_96(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output * "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_97(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "XXidentity.stderr.logXX").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_98(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "IDENTITY.STDERR.LOG").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_99(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError(None, runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_100(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", None, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_101(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, None)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_102(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError(runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_103(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_104(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, )
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_105(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("XXidentityXX", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_106(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("IDENTITY", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_107(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = None
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_108(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(None)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_109(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None or (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_110(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_111(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root) and digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_112(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute() and digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_113(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_114(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(None)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_115(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError(None)
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_116(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("XXcoverage digest output must be a new external handoff fileXX")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_117(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("COVERAGE DIGEST OUTPUT MUST BE A NEW EXTERNAL HANDOFF FILE")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_118(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = None
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_119(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output * "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_120(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "XXcoverage.iniXX"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_121(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "COVERAGE.INI"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_122(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        None
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_123(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots) - f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_124(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n" - "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_125(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "XX[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\nXX"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_126(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[RUN]\nBRANCH = TRUE\nPARALLEL = TRUE\nPATCH = SUBPROCESS\nSOURCE =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_127(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(None)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_128(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "XXXX".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_129(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root * name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_130(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output * 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_131(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'XXcoverage.dataXX'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_132(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'COVERAGE.DATA'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_133(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = None
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_134(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(None).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_135(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("XXrunXX", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_136(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("RUN", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_137(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("XXxmlXX", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_138(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("XML", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_139(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "XX-oXX", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_140(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-O", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_141(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(None)),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_142(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output * "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_143(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "XXcoverage.xmlXX")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_144(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "COVERAGE.XML")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_145(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("XXjsonXX", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_146(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("JSON", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_147(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "XX-oXX", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_148(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-O", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_149(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(None)),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_150(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output * "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_151(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "XXcoverage.jsonXX")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_152(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "COVERAGE.JSON")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_153(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = None
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_154(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            None,
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_155(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=None,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_156(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=None,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_157(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=None,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_158(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=None,
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_159(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=None,
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_160(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_161(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_162(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_163(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_164(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_165(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_166(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(None), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_167(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "XX-IXX", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_168(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-i", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_169(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "XX-mXX", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_170(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-M", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_171(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "XXcoverageXX", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_172(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "COVERAGE", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_173(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[1], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_174(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "XX--rcfileXX", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_175(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--RCFILE", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_176(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(None), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_177(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[2:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_178(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=601,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_179(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output * (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_180(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] - ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_181(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[1] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_182(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + "XX.stdout.logXX"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_183(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".STDOUT.LOG"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_184(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output * (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_185(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] - ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_186(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[1] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_187(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + "XX.stderr.logXX"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_188(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".STDERR.LOG"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_189(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(None, result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_190(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], None, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_191(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, None)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_192(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_193(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_194(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, )
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_195(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[1], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_196(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(None, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_197(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, None, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_198(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, None)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_199(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_200(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_201(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, )
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_202(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) and config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_203(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash == digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_204(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        None
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_205(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(None) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_206(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(None)}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_207(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(None, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_208(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, None).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_209(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_210(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, ).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_211(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash == bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_212(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(None):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_213(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError(None)
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_214(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("XXcoverage source or configuration changed during executionXX")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_215(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("COVERAGE SOURCE OR CONFIGURATION CHANGED DURING EXECUTION")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_216(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = None
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_217(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "XXschemaXX": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_218(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "SCHEMA": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_219(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "XXbase_commitXX": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_220(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "BASE_COMMIT": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_221(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "XXcandidate_commitXX": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_222(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "CANDIDATE_COMMIT": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_223(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "XXrootsXX": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_224(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "ROOTS": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_225(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(None),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_226(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "XXsource_digestXX": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_227(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "SOURCE_DIGEST": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_228(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "XXconfig_pathXX": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_229(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "CONFIG_PATH": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_230(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "XXconfig_digestXX": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_231(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "CONFIG_DIGEST": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_232(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "XXreportsXX": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_233(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "REPORTS": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_234(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest(None) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_235(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output * name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_236(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("XXcoverage.xmlXX", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_237(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("COVERAGE.XML", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_238(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "XXcoverage.jsonXX")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_239(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "COVERAGE.JSON")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_240(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "XXexecutionXX": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_241(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "EXECUTION": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_242(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "XXrun_idXX": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_243(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "RUN_ID": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_244(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "XXattempt_idXX": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_245(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "ATTEMPT_ID": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_246(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "XXstarted_atXX": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_247(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "STARTED_AT": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_248(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "XXfinished_atXX": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_249(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "FINISHED_AT": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_250(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(None).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_251(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "XXcommandXX": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_252(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "COMMAND": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_253(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "XXcoverage_versionXX": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_254(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "COVERAGE_VERSION": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_255(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["XXcoverageXX"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_256(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["COVERAGE"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_257(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "XXpython_versionXX": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_258(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "PYTHON_VERSION": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_259(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["XXpythonXX"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_260(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["PYTHON"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_261(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "XXpytest_versionXX": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_262(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "PYTEST_VERSION": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_263(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["XXpytestXX"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_264(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["PYTEST"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_265(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "XXpython_executableXX": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_266(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "PYTHON_EXECUTABLE": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_267(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["XXpython_executableXX"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_268(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["PYTHON_EXECUTABLE"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_269(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(None, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_270(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, None, files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_271(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", None),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_272(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_273(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_274(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", ),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_275(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output * "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_276(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "XXcoverage.xmlXX", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_277(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "COVERAGE.XML", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_278(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = None
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_279(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["XXdigestXX"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_280(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["DIGEST"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_281(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(None)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_282(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(None)
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_283(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output * "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_284(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "XXreceipt.jsonXX").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_285(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "RECEIPT.JSON").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_286(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) - "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_287(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(None, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_288(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=None, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_289(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=None) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_290(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_291(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_292(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, ) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_293(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=3, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_294(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=False) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_295(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "XX\nXX")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_296(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_297(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open(None) as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_298(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("XXxXX") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_299(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("X") as stream:
            stream.write(payload["digest"] + "\n")
    return payload


def x_produce_coverage__mutmut_300(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(None)
    return payload


def x_produce_coverage__mutmut_301(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] - "\n")
    return payload


def x_produce_coverage__mutmut_302(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["XXdigestXX"] + "\n")
    return payload


def x_produce_coverage__mutmut_303(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["DIGEST"] + "\n")
    return payload


def x_produce_coverage__mutmut_304(
    *,
    root: Path,
    base: str,
    candidate: str,
    roots: list[str],
    config: str,
    run_id: str,
    attempt_id: str,
    output: Path,
    command: list[str],
    digest_output: Path | None = None,
    environment: dict[str, str] | None = None,
    python_executable: Path | None = None,
) -> dict[str, Any]:
    """Run Coverage.py from empty data and retain a new digest-bound measurement.

    The caller owns command selection and the independent digest handoff. This
    producer never marks its own receipt accepted and never replaces evidence.
    """
    root, output = root.resolve(), output.resolve()
    base, candidate = identity(base), identity(candidate)
    run_id, attempt_id = configured(run_id), configured(attempt_id)
    exact_checkout(root, base, candidate)
    files = source_files(root, roots)
    config_path = root / config
    if Path(config).is_absolute() or ".." in Path(config).parts:
        raise ValueError("coverage configuration must be a tracked repository file")
    config_hash = bytes_digest(config_path.read_bytes())
    if bytes_digest(git_bytes(root, "show", candidate + ":" + config)) != config_hash:
        raise ValueError("coverage configuration differs from the candidate")
    source_hash = digest({name: bytes_digest(path.read_bytes()) for name, path in sorted(files.items())})
    if not command:
        raise ValueError("coverage producer requires a Python test command")
    interpreter = python_executable if python_executable is not None else Path(sys.executable)
    if not interpreter.is_absolute() or not interpreter.is_file():
        raise ValueError("coverage producer requires an absolute installed Python interpreter")
    output.mkdir(parents=True, exist_ok=False)
    runtime = run_bounded_process(
        [
            str(interpreter),
            "-I",
            "-c",
            "import sys,json,importlib.metadata as m; print(json.dumps({'python':sys.version,"
            "'python_executable':sys.executable,'coverage':m.version('coverage'),'pytest':m.version('pytest')}))",
        ],
        cwd=root,
        env=environment,
        timeout=30,
    )
    (output / "identity.stdout.log").write_bytes(runtime.stdout)
    (output / "identity.stderr.log").write_bytes(runtime.stderr)
    if runtime.returncode:
        raise CoverageExecutionError("identity", runtime.returncode, output)
    versions = json.loads(runtime.stdout)
    if digest_output is not None and (
        not digest_output.is_absolute()
        or digest_output.resolve().is_relative_to(root)
        or digest_output.exists()
    ):
        raise ValueError("coverage digest output must be a new external handoff file")
    settings = output / "coverage.ini"
    settings.write_text(
        "[run]\nbranch = true\nparallel = true\npatch = subprocess\nsource =\n"
        + "".join(f"    {root / name}\n" for name in roots)
        + f"data_file = {output / 'coverage.data'}\n[report]\nexclude_lines =\npartial_branches =\n"
    )
    started = datetime.now(UTC).isoformat()
    for args in (
        ("run", *command),
        ("xml", "-o", str(output / "coverage.xml")),
        ("json", "-o", str(output / "coverage.json")),
    ):
        # The producer deliberately executes caller-selected Python tests with
        # a fixed interpreter and argv, never a shell or executable from PATH.
        result = run_bounded_process(
            [str(interpreter), "-I", "-m", "coverage", args[0], "--rcfile", str(settings), *args[1:]],
            cwd=root,
            env=environment,
            timeout=600,
            stdout_path=output / (args[0] + ".stdout.log"),
            stderr_path=output / (args[0] + ".stderr.log"),
        )
        if result.returncode:
            raise CoverageExecutionError(args[0], result.returncode, output)
    exact_checkout(root, base, candidate)
    if source_hash != digest(
        {name: bytes_digest(path.read_bytes()) for name, path in sorted(source_files(root, roots).items())}
    ) or config_hash != bytes_digest(config_path.read_bytes()):
        raise ValueError("coverage source or configuration changed during execution")
    payload: dict[str, Any] = {
        "schema": SCHEMA,
        "base_commit": base,
        "candidate_commit": candidate,
        "roots": sorted(roots),
        "source_digest": source_hash,
        "config_path": config,
        "config_digest": config_hash,
        "reports": {
            name: bytes_digest((output / name).read_bytes()) for name in ("coverage.xml", "coverage.json")
        },
        "execution": {
            "run_id": run_id,
            "attempt_id": attempt_id,
            "started_at": started,
            "finished_at": datetime.now(UTC).isoformat(),
            "command": command,
            "coverage_version": versions["coverage"],
            "python_version": versions["python"],
            "pytest_version": versions["pytest"],
            "python_executable": versions["python_executable"],
        },
        **measure(root, output / "coverage.xml", files),
    }
    payload["digest"] = digest(payload)
    (output / "receipt.json").write_text(json.dumps(payload, indent=2, sort_keys=True) + "\n")
    if digest_output is not None:
        with digest_output.open("x") as stream:
            stream.write(payload["digest"] + "XX\nXX")
    return payload

mutants_x_produce_coverage__mutmut['_mutmut_orig'] = x_produce_coverage__mutmut_orig # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_1'] = x_produce_coverage__mutmut_1 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_2'] = x_produce_coverage__mutmut_2 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_3'] = x_produce_coverage__mutmut_3 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_4'] = x_produce_coverage__mutmut_4 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_5'] = x_produce_coverage__mutmut_5 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_6'] = x_produce_coverage__mutmut_6 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_7'] = x_produce_coverage__mutmut_7 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_8'] = x_produce_coverage__mutmut_8 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_9'] = x_produce_coverage__mutmut_9 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_10'] = x_produce_coverage__mutmut_10 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_11'] = x_produce_coverage__mutmut_11 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_12'] = x_produce_coverage__mutmut_12 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_13'] = x_produce_coverage__mutmut_13 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_14'] = x_produce_coverage__mutmut_14 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_15'] = x_produce_coverage__mutmut_15 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_16'] = x_produce_coverage__mutmut_16 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_17'] = x_produce_coverage__mutmut_17 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_18'] = x_produce_coverage__mutmut_18 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_19'] = x_produce_coverage__mutmut_19 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_20'] = x_produce_coverage__mutmut_20 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_21'] = x_produce_coverage__mutmut_21 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_22'] = x_produce_coverage__mutmut_22 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_23'] = x_produce_coverage__mutmut_23 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_24'] = x_produce_coverage__mutmut_24 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_25'] = x_produce_coverage__mutmut_25 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_26'] = x_produce_coverage__mutmut_26 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_27'] = x_produce_coverage__mutmut_27 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_28'] = x_produce_coverage__mutmut_28 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_29'] = x_produce_coverage__mutmut_29 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_30'] = x_produce_coverage__mutmut_30 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_31'] = x_produce_coverage__mutmut_31 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_32'] = x_produce_coverage__mutmut_32 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_33'] = x_produce_coverage__mutmut_33 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_34'] = x_produce_coverage__mutmut_34 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_35'] = x_produce_coverage__mutmut_35 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_36'] = x_produce_coverage__mutmut_36 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_37'] = x_produce_coverage__mutmut_37 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_38'] = x_produce_coverage__mutmut_38 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_39'] = x_produce_coverage__mutmut_39 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_40'] = x_produce_coverage__mutmut_40 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_41'] = x_produce_coverage__mutmut_41 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_42'] = x_produce_coverage__mutmut_42 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_43'] = x_produce_coverage__mutmut_43 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_44'] = x_produce_coverage__mutmut_44 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_45'] = x_produce_coverage__mutmut_45 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_46'] = x_produce_coverage__mutmut_46 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_47'] = x_produce_coverage__mutmut_47 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_48'] = x_produce_coverage__mutmut_48 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_49'] = x_produce_coverage__mutmut_49 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_50'] = x_produce_coverage__mutmut_50 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_51'] = x_produce_coverage__mutmut_51 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_52'] = x_produce_coverage__mutmut_52 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_53'] = x_produce_coverage__mutmut_53 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_54'] = x_produce_coverage__mutmut_54 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_55'] = x_produce_coverage__mutmut_55 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_56'] = x_produce_coverage__mutmut_56 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_57'] = x_produce_coverage__mutmut_57 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_58'] = x_produce_coverage__mutmut_58 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_59'] = x_produce_coverage__mutmut_59 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_60'] = x_produce_coverage__mutmut_60 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_61'] = x_produce_coverage__mutmut_61 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_62'] = x_produce_coverage__mutmut_62 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_63'] = x_produce_coverage__mutmut_63 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_64'] = x_produce_coverage__mutmut_64 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_65'] = x_produce_coverage__mutmut_65 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_66'] = x_produce_coverage__mutmut_66 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_67'] = x_produce_coverage__mutmut_67 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_68'] = x_produce_coverage__mutmut_68 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_69'] = x_produce_coverage__mutmut_69 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_70'] = x_produce_coverage__mutmut_70 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_71'] = x_produce_coverage__mutmut_71 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_72'] = x_produce_coverage__mutmut_72 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_73'] = x_produce_coverage__mutmut_73 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_74'] = x_produce_coverage__mutmut_74 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_75'] = x_produce_coverage__mutmut_75 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_76'] = x_produce_coverage__mutmut_76 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_77'] = x_produce_coverage__mutmut_77 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_78'] = x_produce_coverage__mutmut_78 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_79'] = x_produce_coverage__mutmut_79 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_80'] = x_produce_coverage__mutmut_80 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_81'] = x_produce_coverage__mutmut_81 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_82'] = x_produce_coverage__mutmut_82 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_83'] = x_produce_coverage__mutmut_83 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_84'] = x_produce_coverage__mutmut_84 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_85'] = x_produce_coverage__mutmut_85 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_86'] = x_produce_coverage__mutmut_86 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_87'] = x_produce_coverage__mutmut_87 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_88'] = x_produce_coverage__mutmut_88 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_89'] = x_produce_coverage__mutmut_89 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_90'] = x_produce_coverage__mutmut_90 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_91'] = x_produce_coverage__mutmut_91 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_92'] = x_produce_coverage__mutmut_92 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_93'] = x_produce_coverage__mutmut_93 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_94'] = x_produce_coverage__mutmut_94 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_95'] = x_produce_coverage__mutmut_95 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_96'] = x_produce_coverage__mutmut_96 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_97'] = x_produce_coverage__mutmut_97 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_98'] = x_produce_coverage__mutmut_98 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_99'] = x_produce_coverage__mutmut_99 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_100'] = x_produce_coverage__mutmut_100 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_101'] = x_produce_coverage__mutmut_101 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_102'] = x_produce_coverage__mutmut_102 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_103'] = x_produce_coverage__mutmut_103 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_104'] = x_produce_coverage__mutmut_104 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_105'] = x_produce_coverage__mutmut_105 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_106'] = x_produce_coverage__mutmut_106 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_107'] = x_produce_coverage__mutmut_107 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_108'] = x_produce_coverage__mutmut_108 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_109'] = x_produce_coverage__mutmut_109 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_110'] = x_produce_coverage__mutmut_110 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_111'] = x_produce_coverage__mutmut_111 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_112'] = x_produce_coverage__mutmut_112 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_113'] = x_produce_coverage__mutmut_113 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_114'] = x_produce_coverage__mutmut_114 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_115'] = x_produce_coverage__mutmut_115 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_116'] = x_produce_coverage__mutmut_116 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_117'] = x_produce_coverage__mutmut_117 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_118'] = x_produce_coverage__mutmut_118 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_119'] = x_produce_coverage__mutmut_119 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_120'] = x_produce_coverage__mutmut_120 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_121'] = x_produce_coverage__mutmut_121 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_122'] = x_produce_coverage__mutmut_122 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_123'] = x_produce_coverage__mutmut_123 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_124'] = x_produce_coverage__mutmut_124 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_125'] = x_produce_coverage__mutmut_125 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_126'] = x_produce_coverage__mutmut_126 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_127'] = x_produce_coverage__mutmut_127 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_128'] = x_produce_coverage__mutmut_128 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_129'] = x_produce_coverage__mutmut_129 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_130'] = x_produce_coverage__mutmut_130 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_131'] = x_produce_coverage__mutmut_131 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_132'] = x_produce_coverage__mutmut_132 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_133'] = x_produce_coverage__mutmut_133 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_134'] = x_produce_coverage__mutmut_134 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_135'] = x_produce_coverage__mutmut_135 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_136'] = x_produce_coverage__mutmut_136 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_137'] = x_produce_coverage__mutmut_137 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_138'] = x_produce_coverage__mutmut_138 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_139'] = x_produce_coverage__mutmut_139 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_140'] = x_produce_coverage__mutmut_140 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_141'] = x_produce_coverage__mutmut_141 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_142'] = x_produce_coverage__mutmut_142 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_143'] = x_produce_coverage__mutmut_143 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_144'] = x_produce_coverage__mutmut_144 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_145'] = x_produce_coverage__mutmut_145 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_146'] = x_produce_coverage__mutmut_146 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_147'] = x_produce_coverage__mutmut_147 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_148'] = x_produce_coverage__mutmut_148 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_149'] = x_produce_coverage__mutmut_149 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_150'] = x_produce_coverage__mutmut_150 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_151'] = x_produce_coverage__mutmut_151 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_152'] = x_produce_coverage__mutmut_152 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_153'] = x_produce_coverage__mutmut_153 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_154'] = x_produce_coverage__mutmut_154 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_155'] = x_produce_coverage__mutmut_155 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_156'] = x_produce_coverage__mutmut_156 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_157'] = x_produce_coverage__mutmut_157 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_158'] = x_produce_coverage__mutmut_158 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_159'] = x_produce_coverage__mutmut_159 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_160'] = x_produce_coverage__mutmut_160 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_161'] = x_produce_coverage__mutmut_161 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_162'] = x_produce_coverage__mutmut_162 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_163'] = x_produce_coverage__mutmut_163 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_164'] = x_produce_coverage__mutmut_164 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_165'] = x_produce_coverage__mutmut_165 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_166'] = x_produce_coverage__mutmut_166 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_167'] = x_produce_coverage__mutmut_167 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_168'] = x_produce_coverage__mutmut_168 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_169'] = x_produce_coverage__mutmut_169 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_170'] = x_produce_coverage__mutmut_170 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_171'] = x_produce_coverage__mutmut_171 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_172'] = x_produce_coverage__mutmut_172 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_173'] = x_produce_coverage__mutmut_173 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_174'] = x_produce_coverage__mutmut_174 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_175'] = x_produce_coverage__mutmut_175 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_176'] = x_produce_coverage__mutmut_176 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_177'] = x_produce_coverage__mutmut_177 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_178'] = x_produce_coverage__mutmut_178 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_179'] = x_produce_coverage__mutmut_179 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_180'] = x_produce_coverage__mutmut_180 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_181'] = x_produce_coverage__mutmut_181 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_182'] = x_produce_coverage__mutmut_182 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_183'] = x_produce_coverage__mutmut_183 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_184'] = x_produce_coverage__mutmut_184 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_185'] = x_produce_coverage__mutmut_185 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_186'] = x_produce_coverage__mutmut_186 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_187'] = x_produce_coverage__mutmut_187 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_188'] = x_produce_coverage__mutmut_188 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_189'] = x_produce_coverage__mutmut_189 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_190'] = x_produce_coverage__mutmut_190 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_191'] = x_produce_coverage__mutmut_191 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_192'] = x_produce_coverage__mutmut_192 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_193'] = x_produce_coverage__mutmut_193 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_194'] = x_produce_coverage__mutmut_194 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_195'] = x_produce_coverage__mutmut_195 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_196'] = x_produce_coverage__mutmut_196 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_197'] = x_produce_coverage__mutmut_197 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_198'] = x_produce_coverage__mutmut_198 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_199'] = x_produce_coverage__mutmut_199 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_200'] = x_produce_coverage__mutmut_200 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_201'] = x_produce_coverage__mutmut_201 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_202'] = x_produce_coverage__mutmut_202 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_203'] = x_produce_coverage__mutmut_203 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_204'] = x_produce_coverage__mutmut_204 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_205'] = x_produce_coverage__mutmut_205 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_206'] = x_produce_coverage__mutmut_206 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_207'] = x_produce_coverage__mutmut_207 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_208'] = x_produce_coverage__mutmut_208 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_209'] = x_produce_coverage__mutmut_209 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_210'] = x_produce_coverage__mutmut_210 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_211'] = x_produce_coverage__mutmut_211 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_212'] = x_produce_coverage__mutmut_212 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_213'] = x_produce_coverage__mutmut_213 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_214'] = x_produce_coverage__mutmut_214 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_215'] = x_produce_coverage__mutmut_215 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_216'] = x_produce_coverage__mutmut_216 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_217'] = x_produce_coverage__mutmut_217 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_218'] = x_produce_coverage__mutmut_218 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_219'] = x_produce_coverage__mutmut_219 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_220'] = x_produce_coverage__mutmut_220 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_221'] = x_produce_coverage__mutmut_221 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_222'] = x_produce_coverage__mutmut_222 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_223'] = x_produce_coverage__mutmut_223 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_224'] = x_produce_coverage__mutmut_224 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_225'] = x_produce_coverage__mutmut_225 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_226'] = x_produce_coverage__mutmut_226 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_227'] = x_produce_coverage__mutmut_227 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_228'] = x_produce_coverage__mutmut_228 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_229'] = x_produce_coverage__mutmut_229 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_230'] = x_produce_coverage__mutmut_230 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_231'] = x_produce_coverage__mutmut_231 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_232'] = x_produce_coverage__mutmut_232 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_233'] = x_produce_coverage__mutmut_233 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_234'] = x_produce_coverage__mutmut_234 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_235'] = x_produce_coverage__mutmut_235 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_236'] = x_produce_coverage__mutmut_236 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_237'] = x_produce_coverage__mutmut_237 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_238'] = x_produce_coverage__mutmut_238 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_239'] = x_produce_coverage__mutmut_239 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_240'] = x_produce_coverage__mutmut_240 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_241'] = x_produce_coverage__mutmut_241 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_242'] = x_produce_coverage__mutmut_242 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_243'] = x_produce_coverage__mutmut_243 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_244'] = x_produce_coverage__mutmut_244 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_245'] = x_produce_coverage__mutmut_245 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_246'] = x_produce_coverage__mutmut_246 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_247'] = x_produce_coverage__mutmut_247 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_248'] = x_produce_coverage__mutmut_248 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_249'] = x_produce_coverage__mutmut_249 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_250'] = x_produce_coverage__mutmut_250 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_251'] = x_produce_coverage__mutmut_251 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_252'] = x_produce_coverage__mutmut_252 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_253'] = x_produce_coverage__mutmut_253 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_254'] = x_produce_coverage__mutmut_254 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_255'] = x_produce_coverage__mutmut_255 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_256'] = x_produce_coverage__mutmut_256 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_257'] = x_produce_coverage__mutmut_257 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_258'] = x_produce_coverage__mutmut_258 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_259'] = x_produce_coverage__mutmut_259 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_260'] = x_produce_coverage__mutmut_260 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_261'] = x_produce_coverage__mutmut_261 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_262'] = x_produce_coverage__mutmut_262 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_263'] = x_produce_coverage__mutmut_263 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_264'] = x_produce_coverage__mutmut_264 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_265'] = x_produce_coverage__mutmut_265 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_266'] = x_produce_coverage__mutmut_266 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_267'] = x_produce_coverage__mutmut_267 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_268'] = x_produce_coverage__mutmut_268 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_269'] = x_produce_coverage__mutmut_269 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_270'] = x_produce_coverage__mutmut_270 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_271'] = x_produce_coverage__mutmut_271 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_272'] = x_produce_coverage__mutmut_272 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_273'] = x_produce_coverage__mutmut_273 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_274'] = x_produce_coverage__mutmut_274 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_275'] = x_produce_coverage__mutmut_275 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_276'] = x_produce_coverage__mutmut_276 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_277'] = x_produce_coverage__mutmut_277 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_278'] = x_produce_coverage__mutmut_278 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_279'] = x_produce_coverage__mutmut_279 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_280'] = x_produce_coverage__mutmut_280 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_281'] = x_produce_coverage__mutmut_281 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_282'] = x_produce_coverage__mutmut_282 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_283'] = x_produce_coverage__mutmut_283 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_284'] = x_produce_coverage__mutmut_284 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_285'] = x_produce_coverage__mutmut_285 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_286'] = x_produce_coverage__mutmut_286 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_287'] = x_produce_coverage__mutmut_287 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_288'] = x_produce_coverage__mutmut_288 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_289'] = x_produce_coverage__mutmut_289 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_290'] = x_produce_coverage__mutmut_290 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_291'] = x_produce_coverage__mutmut_291 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_292'] = x_produce_coverage__mutmut_292 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_293'] = x_produce_coverage__mutmut_293 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_294'] = x_produce_coverage__mutmut_294 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_295'] = x_produce_coverage__mutmut_295 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_296'] = x_produce_coverage__mutmut_296 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_297'] = x_produce_coverage__mutmut_297 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_298'] = x_produce_coverage__mutmut_298 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_299'] = x_produce_coverage__mutmut_299 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_300'] = x_produce_coverage__mutmut_300 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_301'] = x_produce_coverage__mutmut_301 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_302'] = x_produce_coverage__mutmut_302 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_303'] = x_produce_coverage__mutmut_303 # type: ignore # mutmut generated
mutants_x_produce_coverage__mutmut['x_produce_coverage__mutmut_304'] = x_produce_coverage__mutmut_304 # type: ignore # mutmut generated
mutants_x_main__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_main__mutmut)
def main(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_orig(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_1(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = None
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_2(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=None)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_3(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument(None, choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_4(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=None)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_5(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument(choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_6(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", )
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_7(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("XXactionXX", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_8(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("ACTION", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_9(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["XXproduceXX"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_10(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["PRODUCE"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_11(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument(None, type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_12(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=None, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_13(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=None)
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_14(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument(type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_15(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_16(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, )
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_17(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("XX--repo-rootXX", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_18(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--REPO-ROOT", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_19(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument(None, required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_20(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=None)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_21(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument(required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_22(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", )
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_23(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("XX--base-commitXX", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_24(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--BASE-COMMIT", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_25(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=False)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_26(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument(None, required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_27(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=None)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_28(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument(required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_29(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", )
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_30(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("XX--candidate-commitXX", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_31(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--CANDIDATE-COMMIT", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_32(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=False)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_33(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument(None, action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_34(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action=None, required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_35(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=None)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_36(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument(action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_37(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_38(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", )
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_39(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("XX--sourceXX", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_40(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--SOURCE", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_41(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="XXappendXX", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_42(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="APPEND", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_43(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=False)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_44(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument(None, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_45(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=None)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_46(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument(required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_47(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", )
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_48(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("XX--configXX", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_49(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--CONFIG", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_50(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=False)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_51(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument(None, required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_52(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=None)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_53(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument(required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_54(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", )
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_55(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("XX--run-idXX", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_56(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--RUN-ID", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_57(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=False)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_58(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument(None, required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_59(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=None)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_60(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument(required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_61(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", )
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_62(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("XX--attempt-idXX", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_63(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--ATTEMPT-ID", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_64(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=False)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_65(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument(None, required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_66(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=None)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_67(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument(required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_68(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", )
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_69(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("XX--outputXX", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_70(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--OUTPUT", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_71(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=False)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_72(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument(None)
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_73(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("XX--digest-outputXX")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_74(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--DIGEST-OUTPUT")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_75(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = None
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_76(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[2:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_77(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is not None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_78(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = None
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_79(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index(None) if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_80(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.rindex("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_81(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("XX--XX") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_82(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "XX--XX" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_83(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" not in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_84(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = None
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_85(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(None)
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_86(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = None
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_87(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split - 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_88(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 2 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_89(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = None
    print(payload["digest"])
    return 0


def x_main__mutmut_90(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=None,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_91(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=None,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_92(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=None,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_93(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=None,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_94(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=None,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_95(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=None,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_96(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=None,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_97(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=None,
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_98(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=None,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_99(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_100(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_101(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_102(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_103(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_104(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_105(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_106(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_107(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_108(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_109(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        )
    print(payload["digest"])
    return 0


def x_main__mutmut_110(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(None),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_111(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(None)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_112(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(None) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_113(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(None)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 0


def x_main__mutmut_114(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(None)
    return 0


def x_main__mutmut_115(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["XXdigestXX"])
    return 0


def x_main__mutmut_116(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["DIGEST"])
    return 0


def x_main__mutmut_117(argv: list[str] | None = None) -> int:
    """Public evidence producer; admission remains in the existing CORE checks."""
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("action", choices=["produce"])
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--source", action="append", required=True)
    parser.add_argument("--config", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt-id", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--digest-output")
    tokens = sys.argv[1:] if argv is None else argv
    split = tokens.index("--") if "--" in tokens else len(tokens)
    args = parser.parse_args(tokens[:split])
    command = tokens[split + 1 :]
    payload = produce_coverage(
        root=args.repo_root,
        base=args.base_commit,
        candidate=args.candidate_commit,
        roots=args.source,
        config=args.config,
        run_id=args.run_id,
        attempt_id=args.attempt_id,
        output=Path(configured(args.output)),
        command=command,
        digest_output=Path(configured(args.digest_output)) if args.digest_output else None,
    )
    print(payload["digest"])
    return 1

mutants_x_main__mutmut['_mutmut_orig'] = x_main__mutmut_orig # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_1'] = x_main__mutmut_1 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_2'] = x_main__mutmut_2 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_3'] = x_main__mutmut_3 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_4'] = x_main__mutmut_4 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_5'] = x_main__mutmut_5 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_6'] = x_main__mutmut_6 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_7'] = x_main__mutmut_7 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_8'] = x_main__mutmut_8 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_9'] = x_main__mutmut_9 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_10'] = x_main__mutmut_10 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_11'] = x_main__mutmut_11 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_12'] = x_main__mutmut_12 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_13'] = x_main__mutmut_13 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_14'] = x_main__mutmut_14 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_15'] = x_main__mutmut_15 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_16'] = x_main__mutmut_16 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_17'] = x_main__mutmut_17 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_18'] = x_main__mutmut_18 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_19'] = x_main__mutmut_19 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_20'] = x_main__mutmut_20 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_21'] = x_main__mutmut_21 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_22'] = x_main__mutmut_22 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_23'] = x_main__mutmut_23 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_24'] = x_main__mutmut_24 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_25'] = x_main__mutmut_25 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_26'] = x_main__mutmut_26 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_27'] = x_main__mutmut_27 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_28'] = x_main__mutmut_28 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_29'] = x_main__mutmut_29 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_30'] = x_main__mutmut_30 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_31'] = x_main__mutmut_31 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_32'] = x_main__mutmut_32 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_33'] = x_main__mutmut_33 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_34'] = x_main__mutmut_34 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_35'] = x_main__mutmut_35 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_36'] = x_main__mutmut_36 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_37'] = x_main__mutmut_37 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_38'] = x_main__mutmut_38 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_39'] = x_main__mutmut_39 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_40'] = x_main__mutmut_40 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_41'] = x_main__mutmut_41 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_42'] = x_main__mutmut_42 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_43'] = x_main__mutmut_43 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_44'] = x_main__mutmut_44 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_45'] = x_main__mutmut_45 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_46'] = x_main__mutmut_46 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_47'] = x_main__mutmut_47 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_48'] = x_main__mutmut_48 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_49'] = x_main__mutmut_49 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_50'] = x_main__mutmut_50 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_51'] = x_main__mutmut_51 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_52'] = x_main__mutmut_52 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_53'] = x_main__mutmut_53 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_54'] = x_main__mutmut_54 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_55'] = x_main__mutmut_55 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_56'] = x_main__mutmut_56 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_57'] = x_main__mutmut_57 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_58'] = x_main__mutmut_58 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_59'] = x_main__mutmut_59 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_60'] = x_main__mutmut_60 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_61'] = x_main__mutmut_61 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_62'] = x_main__mutmut_62 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_63'] = x_main__mutmut_63 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_64'] = x_main__mutmut_64 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_65'] = x_main__mutmut_65 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_66'] = x_main__mutmut_66 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_67'] = x_main__mutmut_67 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_68'] = x_main__mutmut_68 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_69'] = x_main__mutmut_69 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_70'] = x_main__mutmut_70 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_71'] = x_main__mutmut_71 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_72'] = x_main__mutmut_72 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_73'] = x_main__mutmut_73 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_74'] = x_main__mutmut_74 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_75'] = x_main__mutmut_75 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_76'] = x_main__mutmut_76 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_77'] = x_main__mutmut_77 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_78'] = x_main__mutmut_78 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_79'] = x_main__mutmut_79 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_80'] = x_main__mutmut_80 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_81'] = x_main__mutmut_81 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_82'] = x_main__mutmut_82 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_83'] = x_main__mutmut_83 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_84'] = x_main__mutmut_84 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_85'] = x_main__mutmut_85 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_86'] = x_main__mutmut_86 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_87'] = x_main__mutmut_87 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_88'] = x_main__mutmut_88 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_89'] = x_main__mutmut_89 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_90'] = x_main__mutmut_90 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_91'] = x_main__mutmut_91 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_92'] = x_main__mutmut_92 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_93'] = x_main__mutmut_93 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_94'] = x_main__mutmut_94 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_95'] = x_main__mutmut_95 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_96'] = x_main__mutmut_96 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_97'] = x_main__mutmut_97 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_98'] = x_main__mutmut_98 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_99'] = x_main__mutmut_99 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_100'] = x_main__mutmut_100 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_101'] = x_main__mutmut_101 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_102'] = x_main__mutmut_102 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_103'] = x_main__mutmut_103 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_104'] = x_main__mutmut_104 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_105'] = x_main__mutmut_105 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_106'] = x_main__mutmut_106 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_107'] = x_main__mutmut_107 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_108'] = x_main__mutmut_108 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_109'] = x_main__mutmut_109 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_110'] = x_main__mutmut_110 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_111'] = x_main__mutmut_111 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_112'] = x_main__mutmut_112 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_113'] = x_main__mutmut_113 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_114'] = x_main__mutmut_114 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_115'] = x_main__mutmut_115 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_116'] = x_main__mutmut_116 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_117'] = x_main__mutmut_117 # type: ignore # mutmut generated


if __name__ == "__main__":
    raise SystemExit(main())
