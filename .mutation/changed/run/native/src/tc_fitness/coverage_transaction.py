"""Fresh exact-base/candidate self-coverage assurance; no accepted evidence input."""

from __future__ import annotations

import argparse
import concurrent.futures
import json
import os
import re
import shutil
import tempfile
import uuid
from dataclasses import asdict, dataclass
from pathlib import Path
from typing import Any

from tc_fitness.check_contracts import registered_contract_directory
from tc_fitness.check_evidence import capture_check_evidence
from tc_fitness.core_checks import CORE_CHECKS
from tc_fitness.core_checks._coverage_evidence import CoverageCounts
from tc_fitness.core_checks.coverage_floor import build as coverage_floor
from tc_fitness.coverage_admission import (
    CoverageExecutionError,
    bytes_digest,
    changed_line_failures,
    exact_checkout,
    git,
    produce_coverage,
)
from tc_fitness.runner import run_bounded_process

_ROOTS = ("src/tc_fitness",)
_CRITICAL = tuple(
    "src/tc_fitness/" + name + ".py" for name in ("gate", "runner", "gate_config", "runtime_contract")
)
_PYTEST_CONFIG = """[pytest]
addopts = --strict-markers -p no:cacheprovider
markers =
    unit: in-process behaviour
    contract: public input/output contract
    integration: real local collaborators
    e2e: supported installed entrypoint
"""


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict


@dataclass(frozen=True)
class Toolchain:
    python: str
    python_executable: str
    coverage: str
    pytest: str
    uv: str
    lock_digest: str


@dataclass(frozen=True)
class Measurement:
    """Source identity and fresh counts are immutable within the transaction."""

    commit: str
    source_digest: str
    config_digest: str
    counts: CoverageCounts
    files: tuple[tuple[str, CoverageCounts], ...]
    toolchain: Toolchain


@dataclass(frozen=True)
class CoverageTransaction:
    execution_id: str
    base: Measurement
    candidate: Measurement
    failures: tuple[str, ...]

    def as_payload(self) -> dict[str, Any]:
        """Export is output-only; admission never reads a serialised transaction."""
        return {
            "schema": "tc.fitness/coverage-transaction/v1",
            "status": "fail" if self.failures else "pass",
            **asdict(self),
        }
mutants_x__measurement__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__measurement__mutmut)
def _measurement(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_orig(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_1(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = None
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_2(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["XXexecutionXX"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_3(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["EXECUTION"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_4(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=None,
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_5(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=None,
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_6(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=None,
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_7(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=None,
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_8(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=None,
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_9(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=None,
    )


def x__measurement__mutmut_10(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_11(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_12(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_13(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_14(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_15(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        )


def x__measurement__mutmut_16(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["XXcandidate_commitXX"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_17(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["CANDIDATE_COMMIT"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_18(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["XXsource_digestXX"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_19(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["SOURCE_DIGEST"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_20(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["XXconfig_digestXX"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_21(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["CONFIG_DIGEST"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_22(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["XXcountsXX"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_23(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["COUNTS"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_24(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple(None),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_25(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(None)),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_26(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["XXfilesXX"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_27(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["FILES"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_28(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=None,
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_29(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=None,
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_30(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=None,
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_31(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=None,
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_32(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=None,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_33(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=None,
        ),
    )


def x__measurement__mutmut_34(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_35(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_36(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_37(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_38(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_39(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            ),
    )


def x__measurement__mutmut_40(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["XXpython_versionXX"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_41(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["PYTHON_VERSION"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_42(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["XXpython_executableXX"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_43(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["PYTHON_EXECUTABLE"],
            coverage=execution["coverage_version"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_44(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["XXcoverage_versionXX"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_45(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["COVERAGE_VERSION"],
            pytest=execution["pytest_version"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_46(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["XXpytest_versionXX"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )


def x__measurement__mutmut_47(payload: dict[str, Any], uv_version: str, lock_digest: str) -> Measurement:
    execution = payload["execution"]
    return Measurement(
        commit=payload["candidate_commit"],
        source_digest=payload["source_digest"],
        config_digest=payload["config_digest"],
        counts=CoverageCounts(**payload["counts"]),
        files=tuple((name, CoverageCounts(**counts)) for name, counts in sorted(payload["files"].items())),
        toolchain=Toolchain(
            python=execution["python_version"],
            python_executable=execution["python_executable"],
            coverage=execution["coverage_version"],
            pytest=execution["PYTEST_VERSION"],
            uv=uv_version,
            lock_digest=lock_digest,
        ),
    )

mutants_x__measurement__mutmut['_mutmut_orig'] = x__measurement__mutmut_orig # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_1'] = x__measurement__mutmut_1 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_2'] = x__measurement__mutmut_2 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_3'] = x__measurement__mutmut_3 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_4'] = x__measurement__mutmut_4 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_5'] = x__measurement__mutmut_5 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_6'] = x__measurement__mutmut_6 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_7'] = x__measurement__mutmut_7 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_8'] = x__measurement__mutmut_8 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_9'] = x__measurement__mutmut_9 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_10'] = x__measurement__mutmut_10 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_11'] = x__measurement__mutmut_11 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_12'] = x__measurement__mutmut_12 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_13'] = x__measurement__mutmut_13 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_14'] = x__measurement__mutmut_14 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_15'] = x__measurement__mutmut_15 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_16'] = x__measurement__mutmut_16 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_17'] = x__measurement__mutmut_17 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_18'] = x__measurement__mutmut_18 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_19'] = x__measurement__mutmut_19 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_20'] = x__measurement__mutmut_20 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_21'] = x__measurement__mutmut_21 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_22'] = x__measurement__mutmut_22 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_23'] = x__measurement__mutmut_23 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_24'] = x__measurement__mutmut_24 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_25'] = x__measurement__mutmut_25 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_26'] = x__measurement__mutmut_26 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_27'] = x__measurement__mutmut_27 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_28'] = x__measurement__mutmut_28 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_29'] = x__measurement__mutmut_29 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_30'] = x__measurement__mutmut_30 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_31'] = x__measurement__mutmut_31 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_32'] = x__measurement__mutmut_32 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_33'] = x__measurement__mutmut_33 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_34'] = x__measurement__mutmut_34 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_35'] = x__measurement__mutmut_35 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_36'] = x__measurement__mutmut_36 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_37'] = x__measurement__mutmut_37 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_38'] = x__measurement__mutmut_38 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_39'] = x__measurement__mutmut_39 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_40'] = x__measurement__mutmut_40 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_41'] = x__measurement__mutmut_41 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_42'] = x__measurement__mutmut_42 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_43'] = x__measurement__mutmut_43 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_44'] = x__measurement__mutmut_44 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_45'] = x__measurement__mutmut_45 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_46'] = x__measurement__mutmut_46 # type: ignore # mutmut generated
mutants_x__measurement__mutmut['x__measurement__mutmut_47'] = x__measurement__mutmut_47 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut: MutantDict = {}  # type: ignore


class TransactionError(ValueError):
    """Phase/side diagnostics are structured by the controller, not parsed logs."""

    @_mutmut_mutated(mutants_xǁTransactionErrorǁ__init____mutmut)
    def __init__(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phase=phase, stdout_log=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_orig(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phase=phase, stdout_log=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_1(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(None)
        self.details = dict(side=side, phase=phase, stdout_log=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_2(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = None

    def xǁTransactionErrorǁ__init____mutmut_3(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(sideXX=side, phase=phase, stdout_log=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_4(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phaseXX=phase, stdout_log=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_5(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phase=phase, stdout_logXX=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_6(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phase=phase, stdout_log=stdout_log, stderr_logXX=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_7(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=None, phase=phase, stdout_log=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_8(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phase=None, stdout_log=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_9(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phase=phase, stdout_log=None, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_10(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phase=phase, stdout_log=stdout_log, stderr_log=None)

    def xǁTransactionErrorǁ__init____mutmut_11(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(phase=phase, stdout_log=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_12(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, stdout_log=stdout_log, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_13(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phase=phase, stderr_log=stderr_log)

    def xǁTransactionErrorǁ__init____mutmut_14(self, message: str, *, side: str, phase: str, stdout_log: str, stderr_log: str) -> None:
        super().__init__(message)
        self.details = dict(side=side, phase=phase, stdout_log=stdout_log, )

mutants_xǁTransactionErrorǁ__init____mutmut['_mutmut_orig'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_1'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_2'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_3'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_4'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_5'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_6'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_7'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_8'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_9'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_10'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_11'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_12'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_13'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁTransactionErrorǁ__init____mutmut['xǁTransactionErrorǁ__init____mutmut_14'] = TransactionError.xǁTransactionErrorǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__error_payload__mutmut)
def _error_payload(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_orig(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_1(exc: Exception) -> dict[str, Any]:
    return {
        "XXschemaXX": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_2(exc: Exception) -> dict[str, Any]:
    return {
        "SCHEMA": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_3(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "XXtc.fitness/coverage-transaction/v1XX",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_4(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "TC.FITNESS/COVERAGE-TRANSACTION/V1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_5(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "XXstatusXX": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_6(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "STATUS": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_7(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "XXerrorXX",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_8(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "ERROR",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_9(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "XXerrorXX": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_10(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "ERROR": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_11(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(None),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "side": None}),
    }


def x__error_payload__mutmut_12(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"XXphaseXX": "transaction", "side": None}),
    }


def x__error_payload__mutmut_13(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"PHASE": "transaction", "side": None}),
    }


def x__error_payload__mutmut_14(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "XXtransactionXX", "side": None}),
    }


def x__error_payload__mutmut_15(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "TRANSACTION", "side": None}),
    }


def x__error_payload__mutmut_16(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "XXsideXX": None}),
    }


def x__error_payload__mutmut_17(exc: Exception) -> dict[str, Any]:
    return {
        "schema": "tc.fitness/coverage-transaction/v1",
        "status": "error",
        "error": str(exc),
        **(exc.details if isinstance(exc, TransactionError) else {"phase": "transaction", "SIDE": None}),
    }

mutants_x__error_payload__mutmut['_mutmut_orig'] = x__error_payload__mutmut_orig # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_1'] = x__error_payload__mutmut_1 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_2'] = x__error_payload__mutmut_2 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_3'] = x__error_payload__mutmut_3 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_4'] = x__error_payload__mutmut_4 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_5'] = x__error_payload__mutmut_5 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_6'] = x__error_payload__mutmut_6 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_7'] = x__error_payload__mutmut_7 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_8'] = x__error_payload__mutmut_8 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_9'] = x__error_payload__mutmut_9 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_10'] = x__error_payload__mutmut_10 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_11'] = x__error_payload__mutmut_11 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_12'] = x__error_payload__mutmut_12 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_13'] = x__error_payload__mutmut_13 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_14'] = x__error_payload__mutmut_14 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_15'] = x__error_payload__mutmut_15 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_16'] = x__error_payload__mutmut_16 # type: ignore # mutmut generated
mutants_x__error_payload__mutmut['x__error_payload__mutmut_17'] = x__error_payload__mutmut_17 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fresh_measurement__mutmut)
def _fresh_measurement(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_orig(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_1(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = None
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_2(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = None
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_3(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch * "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_4(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "XXpytest.iniXX"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_5(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "PYTEST.INI"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_6(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(None)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_7(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = None
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_8(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which(None)
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_9(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("XXuvXX")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_10(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("UV")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_11(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is not None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_12(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = None
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_13(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "XXlocked coverage environments require the trusted uv executableXX"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_14(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "LOCKED COVERAGE ENVIRONMENTS REQUIRE THE TRUSTED UV EXECUTABLE"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_15(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text(None)
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_16(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch * "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_17(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "XXuv.stdout.logXX").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_18(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "UV.STDOUT.LOG").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_19(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("XXXX")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_20(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(None)
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_21(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch * "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_22(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "XXuv.stderr.logXX").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_23(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "UV.STDERR.LOG").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_24(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message - "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_25(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "XX\nXX")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_26(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            None,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_27(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=None,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_28(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase=None,
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_29(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=None,
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_30(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=None,
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_31(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_32(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_33(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_34(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_35(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_36(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="XXprovisionXX",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_37(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="PROVISION",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_38(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name - "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_39(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "XX/uv.stdout.logXX",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_40(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/UV.STDOUT.LOG",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_41(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name - "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_42(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "XX/uv.stderr.logXX",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_43(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/UV.STDERR.LOG",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_44(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = None
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_45(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(None)
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_46(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(None).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_47(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = None
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_48(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process(None, cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_49(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=None, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_50(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=None)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_51(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process(cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_52(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_53(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, )
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_54(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "XX--versionXX"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_55(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--VERSION"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_56(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=31)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_57(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(None)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_58(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch * "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_59(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "XXuv-identity.stdout.logXX").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_60(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "UV-IDENTITY.STDOUT.LOG").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_61(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(None)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_62(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch * "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_63(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "XXuv-identity.stderr.logXX").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_64(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "UV-IDENTITY.STDERR.LOG").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_65(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError(None)
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_66(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("XXtrusted uv identity is unavailableXX")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_67(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("TRUSTED UV IDENTITY IS UNAVAILABLE")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_68(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = None
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_69(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = None
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_70(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest(None)
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_71(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot * "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_72(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "XXuv.lockXX").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_73(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "UV.LOCK").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_74(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = None
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_75(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_76(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch * "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_77(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "XXvenvXX"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_78(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "VENV"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_79(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = None
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_80(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_")) or key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_81(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_82(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(None)
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_83(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("XXPYTEST_XX", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_84(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("pytest_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_85(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "XXCOVERAGE_XX", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_86(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "coverage_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_87(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "XXCOV_CORE_XX"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_88(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "cov_core_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_89(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_90(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"XXPYTHONPATHXX", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_91(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"pythonpath", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_92(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "XXVIRTUAL_ENVXX"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_93(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "virtual_env"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_94(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE=None,
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_95(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD=None,
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_96(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=None,
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_97(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_98(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_99(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_100(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="XX1XX",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_101(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="XX1XX",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_102(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(None),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_103(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = None
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_104(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        None,
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_105(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=None,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_106(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=None,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_107(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=None,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_108(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=None,
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_109(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=None,
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_110(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_111(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_112(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_113(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_114(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_115(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_116(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "XXsyncXX", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_117(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "SYNC", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_118(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "XX--lockedXX", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_119(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--LOCKED", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_120(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "XX--all-extrasXX"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_121(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--ALL-EXTRAS"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_122(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=601,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_123(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch * "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_124(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "XXuv.stdout.logXX",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_125(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "UV.STDOUT.LOG",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_126(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch * "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_127(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "XXuv.stderr.logXX",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_128(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "UV.STDERR.LOG",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_129(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            None,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_130(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=None,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_131(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase=None,
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_132(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=None,
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_133(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=None,
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_134(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_135(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_136(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_137(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_138(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_139(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "XXexact-worktree locked environment provisioning failedXX",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_140(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "EXACT-WORKTREE LOCKED ENVIRONMENT PROVISIONING FAILED",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_141(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="XXprovisionXX",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_142(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="PROVISION",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_143(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name - "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_144(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "XX/uv.stdout.logXX",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_145(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/UV.STDOUT.LOG",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_146(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name - "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_147(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "XX/uv.stderr.logXX",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_148(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/UV.STDERR.LOG",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_149(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest(None) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_150(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot * "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_151(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "XXuv.lockXX").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_152(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "UV.LOCK").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_153(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) == lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_154(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError(None)
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_155(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("XXlocked environment provisioning changed the bound lockfileXX")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_156(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("LOCKED ENVIRONMENT PROVISIONING CHANGED THE BOUND LOCKFILE")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_157(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = None
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_158(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["XX-mXX", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_159(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-M", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_160(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "XXpytestXX", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_161(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "PYTEST", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_162(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "XX-qXX", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_163(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-Q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_164(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "XX-cXX", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_165(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-C", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_166(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(None), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_167(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "XX--rootdirXX", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_168(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--ROOTDIR", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_169(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(None), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_170(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "XXtestsXX"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_171(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "TESTS"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_172(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = None
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_173(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot * "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_174(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "XXtests/check_contractsXX"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_175(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "TESTS/CHECK_CONTRACTS"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_176(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() or not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_177(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_178(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(None):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_179(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(None, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_180(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, None) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_181(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_182(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, ) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_183(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_184(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(None)
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_185(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["XX--ignoreXX", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_186(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--IGNORE", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_187(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(None)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_188(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = None
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_189(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=None,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_190(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=None,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_191(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=None,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_192(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=None,
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_193(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config=None,
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_194(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=None,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_195(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id=None,
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_196(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=None,
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_197(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=None,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_198(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=None,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_199(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=None,
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_200(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_201(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_202(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_203(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_204(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_205(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_206(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_207(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_208(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_209(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_210(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_211(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(None),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_212(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="XXpyproject.tomlXX",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_213(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="PYPROJECT.TOML",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_214(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="XX1XX",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_215(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch * "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_216(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "XXmeasurementXX",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_217(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "MEASUREMENT",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_218(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv * ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_219(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("XXScripts/python.exeXX" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_220(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_221(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("SCRIPTS/PYTHON.EXE" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_222(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name != "nt" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_223(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "XXntXX" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_224(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "NT" else "bin/python"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_225(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "XXbin/pythonXX"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_226(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "BIN/PYTHON"),
    )
    return _measurement(payload, version, lock)


def x__fresh_measurement__mutmut_227(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(None, version, lock)


def x__fresh_measurement__mutmut_228(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, None, lock)


def x__fresh_measurement__mutmut_229(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, None)


def x__fresh_measurement__mutmut_230(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(version, lock)


def x__fresh_measurement__mutmut_231(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, lock)


def x__fresh_measurement__mutmut_232(
    snapshot: Path, commit: str, scratch: Path, execution: str, *, environment_dir: Path | None = None
) -> Measurement:
    snapshot = snapshot.resolve()
    settings = scratch / "pytest.ini"
    settings.write_text(_PYTEST_CONFIG)
    uv = shutil.which("uv")
    if uv is None:
        message = "locked coverage environments require the trusted uv executable"
        (scratch / "uv.stdout.log").write_text("")
        (scratch / "uv.stderr.log").write_text(message + "\n")
        raise TransactionError(
            message,
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    uv = str(Path(uv).resolve())
    identity = run_bounded_process([uv, "--version"], cwd=snapshot, timeout=30)
    (scratch / "uv-identity.stdout.log").write_bytes(identity.stdout)
    (scratch / "uv-identity.stderr.log").write_bytes(identity.stderr)
    if identity.returncode:
        raise ValueError("trusted uv identity is unavailable")
    version = identity.stdout.decode().strip()
    lock = bytes_digest((snapshot / "uv.lock").read_bytes())
    venv = environment_dir if environment_dir is not None else scratch / "venv"
    environment = {
        key: value
        for key, value in os.environ.items()
        if not key.startswith(("PYTEST_", "COVERAGE_", "COV_CORE_"))
        and key not in {"PYTHONPATH", "VIRTUAL_ENV"}
    }
    environment.update(
        PYTHONDONTWRITEBYTECODE="1",
        PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        UV_PROJECT_ENVIRONMENT=str(venv),
    )
    provisioned = run_bounded_process(
        [uv, "sync", "--locked", "--all-extras"],
        cwd=snapshot,
        env=environment,
        timeout=600,
        stdout_path=scratch / "uv.stdout.log",
        stderr_path=scratch / "uv.stderr.log",
    )
    if provisioned.returncode:
        raise TransactionError(
            "exact-worktree locked environment provisioning failed",
            side=scratch.name,
            phase="provision",
            stdout_log=scratch.name + "/uv.stdout.log",
            stderr_log=scratch.name + "/uv.stderr.log",
        )
    if bytes_digest((snapshot / "uv.lock").read_bytes()) != lock:
        raise ValueError("locked environment provisioning changed the bound lockfile")
    command = ["-m", "pytest", "-q", "-c", str(settings), "--rootdir", str(snapshot), "tests"]
    registry = snapshot / "tests/check_contracts"
    if registry.is_dir() and not registry.is_symlink():
        for path in sorted(registry.iterdir()):
            # Fixture cases run through their public contract tests, not as
            # outer pytest modules. Classification uses this trusted engine's
            # existing manifest boundary, never a candidate-loaded plugin.
            if registered_contract_directory(path, CORE_CHECKS) is not None:
                command.extend(["--ignore", str(path)])
    payload = produce_coverage(
        root=snapshot,
        base=commit,
        candidate=commit,
        roots=list(_ROOTS),
        config="pyproject.toml",
        run_id=execution,
        attempt_id="1",
        output=scratch / "measurement",
        command=command,
        environment=environment,
        python_executable=venv / ("Scripts/python.exe" if os.name == "nt" else "bin/python"),
    )
    return _measurement(payload, version, )

mutants_x__fresh_measurement__mutmut['_mutmut_orig'] = x__fresh_measurement__mutmut_orig # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_1'] = x__fresh_measurement__mutmut_1 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_2'] = x__fresh_measurement__mutmut_2 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_3'] = x__fresh_measurement__mutmut_3 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_4'] = x__fresh_measurement__mutmut_4 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_5'] = x__fresh_measurement__mutmut_5 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_6'] = x__fresh_measurement__mutmut_6 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_7'] = x__fresh_measurement__mutmut_7 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_8'] = x__fresh_measurement__mutmut_8 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_9'] = x__fresh_measurement__mutmut_9 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_10'] = x__fresh_measurement__mutmut_10 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_11'] = x__fresh_measurement__mutmut_11 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_12'] = x__fresh_measurement__mutmut_12 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_13'] = x__fresh_measurement__mutmut_13 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_14'] = x__fresh_measurement__mutmut_14 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_15'] = x__fresh_measurement__mutmut_15 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_16'] = x__fresh_measurement__mutmut_16 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_17'] = x__fresh_measurement__mutmut_17 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_18'] = x__fresh_measurement__mutmut_18 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_19'] = x__fresh_measurement__mutmut_19 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_20'] = x__fresh_measurement__mutmut_20 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_21'] = x__fresh_measurement__mutmut_21 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_22'] = x__fresh_measurement__mutmut_22 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_23'] = x__fresh_measurement__mutmut_23 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_24'] = x__fresh_measurement__mutmut_24 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_25'] = x__fresh_measurement__mutmut_25 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_26'] = x__fresh_measurement__mutmut_26 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_27'] = x__fresh_measurement__mutmut_27 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_28'] = x__fresh_measurement__mutmut_28 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_29'] = x__fresh_measurement__mutmut_29 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_30'] = x__fresh_measurement__mutmut_30 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_31'] = x__fresh_measurement__mutmut_31 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_32'] = x__fresh_measurement__mutmut_32 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_33'] = x__fresh_measurement__mutmut_33 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_34'] = x__fresh_measurement__mutmut_34 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_35'] = x__fresh_measurement__mutmut_35 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_36'] = x__fresh_measurement__mutmut_36 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_37'] = x__fresh_measurement__mutmut_37 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_38'] = x__fresh_measurement__mutmut_38 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_39'] = x__fresh_measurement__mutmut_39 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_40'] = x__fresh_measurement__mutmut_40 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_41'] = x__fresh_measurement__mutmut_41 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_42'] = x__fresh_measurement__mutmut_42 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_43'] = x__fresh_measurement__mutmut_43 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_44'] = x__fresh_measurement__mutmut_44 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_45'] = x__fresh_measurement__mutmut_45 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_46'] = x__fresh_measurement__mutmut_46 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_47'] = x__fresh_measurement__mutmut_47 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_48'] = x__fresh_measurement__mutmut_48 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_49'] = x__fresh_measurement__mutmut_49 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_50'] = x__fresh_measurement__mutmut_50 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_51'] = x__fresh_measurement__mutmut_51 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_52'] = x__fresh_measurement__mutmut_52 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_53'] = x__fresh_measurement__mutmut_53 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_54'] = x__fresh_measurement__mutmut_54 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_55'] = x__fresh_measurement__mutmut_55 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_56'] = x__fresh_measurement__mutmut_56 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_57'] = x__fresh_measurement__mutmut_57 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_58'] = x__fresh_measurement__mutmut_58 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_59'] = x__fresh_measurement__mutmut_59 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_60'] = x__fresh_measurement__mutmut_60 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_61'] = x__fresh_measurement__mutmut_61 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_62'] = x__fresh_measurement__mutmut_62 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_63'] = x__fresh_measurement__mutmut_63 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_64'] = x__fresh_measurement__mutmut_64 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_65'] = x__fresh_measurement__mutmut_65 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_66'] = x__fresh_measurement__mutmut_66 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_67'] = x__fresh_measurement__mutmut_67 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_68'] = x__fresh_measurement__mutmut_68 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_69'] = x__fresh_measurement__mutmut_69 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_70'] = x__fresh_measurement__mutmut_70 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_71'] = x__fresh_measurement__mutmut_71 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_72'] = x__fresh_measurement__mutmut_72 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_73'] = x__fresh_measurement__mutmut_73 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_74'] = x__fresh_measurement__mutmut_74 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_75'] = x__fresh_measurement__mutmut_75 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_76'] = x__fresh_measurement__mutmut_76 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_77'] = x__fresh_measurement__mutmut_77 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_78'] = x__fresh_measurement__mutmut_78 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_79'] = x__fresh_measurement__mutmut_79 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_80'] = x__fresh_measurement__mutmut_80 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_81'] = x__fresh_measurement__mutmut_81 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_82'] = x__fresh_measurement__mutmut_82 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_83'] = x__fresh_measurement__mutmut_83 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_84'] = x__fresh_measurement__mutmut_84 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_85'] = x__fresh_measurement__mutmut_85 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_86'] = x__fresh_measurement__mutmut_86 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_87'] = x__fresh_measurement__mutmut_87 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_88'] = x__fresh_measurement__mutmut_88 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_89'] = x__fresh_measurement__mutmut_89 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_90'] = x__fresh_measurement__mutmut_90 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_91'] = x__fresh_measurement__mutmut_91 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_92'] = x__fresh_measurement__mutmut_92 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_93'] = x__fresh_measurement__mutmut_93 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_94'] = x__fresh_measurement__mutmut_94 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_95'] = x__fresh_measurement__mutmut_95 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_96'] = x__fresh_measurement__mutmut_96 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_97'] = x__fresh_measurement__mutmut_97 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_98'] = x__fresh_measurement__mutmut_98 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_99'] = x__fresh_measurement__mutmut_99 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_100'] = x__fresh_measurement__mutmut_100 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_101'] = x__fresh_measurement__mutmut_101 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_102'] = x__fresh_measurement__mutmut_102 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_103'] = x__fresh_measurement__mutmut_103 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_104'] = x__fresh_measurement__mutmut_104 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_105'] = x__fresh_measurement__mutmut_105 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_106'] = x__fresh_measurement__mutmut_106 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_107'] = x__fresh_measurement__mutmut_107 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_108'] = x__fresh_measurement__mutmut_108 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_109'] = x__fresh_measurement__mutmut_109 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_110'] = x__fresh_measurement__mutmut_110 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_111'] = x__fresh_measurement__mutmut_111 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_112'] = x__fresh_measurement__mutmut_112 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_113'] = x__fresh_measurement__mutmut_113 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_114'] = x__fresh_measurement__mutmut_114 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_115'] = x__fresh_measurement__mutmut_115 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_116'] = x__fresh_measurement__mutmut_116 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_117'] = x__fresh_measurement__mutmut_117 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_118'] = x__fresh_measurement__mutmut_118 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_119'] = x__fresh_measurement__mutmut_119 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_120'] = x__fresh_measurement__mutmut_120 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_121'] = x__fresh_measurement__mutmut_121 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_122'] = x__fresh_measurement__mutmut_122 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_123'] = x__fresh_measurement__mutmut_123 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_124'] = x__fresh_measurement__mutmut_124 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_125'] = x__fresh_measurement__mutmut_125 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_126'] = x__fresh_measurement__mutmut_126 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_127'] = x__fresh_measurement__mutmut_127 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_128'] = x__fresh_measurement__mutmut_128 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_129'] = x__fresh_measurement__mutmut_129 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_130'] = x__fresh_measurement__mutmut_130 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_131'] = x__fresh_measurement__mutmut_131 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_132'] = x__fresh_measurement__mutmut_132 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_133'] = x__fresh_measurement__mutmut_133 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_134'] = x__fresh_measurement__mutmut_134 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_135'] = x__fresh_measurement__mutmut_135 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_136'] = x__fresh_measurement__mutmut_136 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_137'] = x__fresh_measurement__mutmut_137 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_138'] = x__fresh_measurement__mutmut_138 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_139'] = x__fresh_measurement__mutmut_139 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_140'] = x__fresh_measurement__mutmut_140 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_141'] = x__fresh_measurement__mutmut_141 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_142'] = x__fresh_measurement__mutmut_142 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_143'] = x__fresh_measurement__mutmut_143 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_144'] = x__fresh_measurement__mutmut_144 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_145'] = x__fresh_measurement__mutmut_145 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_146'] = x__fresh_measurement__mutmut_146 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_147'] = x__fresh_measurement__mutmut_147 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_148'] = x__fresh_measurement__mutmut_148 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_149'] = x__fresh_measurement__mutmut_149 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_150'] = x__fresh_measurement__mutmut_150 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_151'] = x__fresh_measurement__mutmut_151 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_152'] = x__fresh_measurement__mutmut_152 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_153'] = x__fresh_measurement__mutmut_153 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_154'] = x__fresh_measurement__mutmut_154 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_155'] = x__fresh_measurement__mutmut_155 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_156'] = x__fresh_measurement__mutmut_156 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_157'] = x__fresh_measurement__mutmut_157 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_158'] = x__fresh_measurement__mutmut_158 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_159'] = x__fresh_measurement__mutmut_159 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_160'] = x__fresh_measurement__mutmut_160 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_161'] = x__fresh_measurement__mutmut_161 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_162'] = x__fresh_measurement__mutmut_162 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_163'] = x__fresh_measurement__mutmut_163 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_164'] = x__fresh_measurement__mutmut_164 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_165'] = x__fresh_measurement__mutmut_165 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_166'] = x__fresh_measurement__mutmut_166 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_167'] = x__fresh_measurement__mutmut_167 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_168'] = x__fresh_measurement__mutmut_168 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_169'] = x__fresh_measurement__mutmut_169 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_170'] = x__fresh_measurement__mutmut_170 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_171'] = x__fresh_measurement__mutmut_171 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_172'] = x__fresh_measurement__mutmut_172 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_173'] = x__fresh_measurement__mutmut_173 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_174'] = x__fresh_measurement__mutmut_174 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_175'] = x__fresh_measurement__mutmut_175 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_176'] = x__fresh_measurement__mutmut_176 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_177'] = x__fresh_measurement__mutmut_177 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_178'] = x__fresh_measurement__mutmut_178 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_179'] = x__fresh_measurement__mutmut_179 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_180'] = x__fresh_measurement__mutmut_180 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_181'] = x__fresh_measurement__mutmut_181 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_182'] = x__fresh_measurement__mutmut_182 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_183'] = x__fresh_measurement__mutmut_183 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_184'] = x__fresh_measurement__mutmut_184 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_185'] = x__fresh_measurement__mutmut_185 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_186'] = x__fresh_measurement__mutmut_186 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_187'] = x__fresh_measurement__mutmut_187 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_188'] = x__fresh_measurement__mutmut_188 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_189'] = x__fresh_measurement__mutmut_189 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_190'] = x__fresh_measurement__mutmut_190 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_191'] = x__fresh_measurement__mutmut_191 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_192'] = x__fresh_measurement__mutmut_192 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_193'] = x__fresh_measurement__mutmut_193 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_194'] = x__fresh_measurement__mutmut_194 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_195'] = x__fresh_measurement__mutmut_195 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_196'] = x__fresh_measurement__mutmut_196 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_197'] = x__fresh_measurement__mutmut_197 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_198'] = x__fresh_measurement__mutmut_198 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_199'] = x__fresh_measurement__mutmut_199 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_200'] = x__fresh_measurement__mutmut_200 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_201'] = x__fresh_measurement__mutmut_201 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_202'] = x__fresh_measurement__mutmut_202 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_203'] = x__fresh_measurement__mutmut_203 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_204'] = x__fresh_measurement__mutmut_204 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_205'] = x__fresh_measurement__mutmut_205 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_206'] = x__fresh_measurement__mutmut_206 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_207'] = x__fresh_measurement__mutmut_207 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_208'] = x__fresh_measurement__mutmut_208 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_209'] = x__fresh_measurement__mutmut_209 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_210'] = x__fresh_measurement__mutmut_210 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_211'] = x__fresh_measurement__mutmut_211 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_212'] = x__fresh_measurement__mutmut_212 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_213'] = x__fresh_measurement__mutmut_213 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_214'] = x__fresh_measurement__mutmut_214 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_215'] = x__fresh_measurement__mutmut_215 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_216'] = x__fresh_measurement__mutmut_216 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_217'] = x__fresh_measurement__mutmut_217 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_218'] = x__fresh_measurement__mutmut_218 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_219'] = x__fresh_measurement__mutmut_219 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_220'] = x__fresh_measurement__mutmut_220 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_221'] = x__fresh_measurement__mutmut_221 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_222'] = x__fresh_measurement__mutmut_222 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_223'] = x__fresh_measurement__mutmut_223 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_224'] = x__fresh_measurement__mutmut_224 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_225'] = x__fresh_measurement__mutmut_225 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_226'] = x__fresh_measurement__mutmut_226 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_227'] = x__fresh_measurement__mutmut_227 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_228'] = x__fresh_measurement__mutmut_228 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_229'] = x__fresh_measurement__mutmut_229 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_230'] = x__fresh_measurement__mutmut_230 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_231'] = x__fresh_measurement__mutmut_231 # type: ignore # mutmut generated
mutants_x__fresh_measurement__mutmut['x__fresh_measurement__mutmut_232'] = x__fresh_measurement__mutmut_232 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__candidate_failures__mutmut)
def _candidate_failures(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_orig(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_1(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            None,
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_2(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=None,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_3(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_4(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_5(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "XXrootsXX": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_6(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "ROOTS": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_7(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(None),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_8(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "XXfloor_pctXX": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_9(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "FLOOR_PCT": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_10(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 96,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_11(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "XXbranch_floor_pctXX": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_12(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "BRANCH_FLOOR_PCT": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_13(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 96,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_14(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "XXcritical_branch_filesXX": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_15(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "CRITICAL_BRANCH_FILES": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_16(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(None),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_17(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "XXcoverage_reportXX": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_18(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "COVERAGE_REPORT": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_19(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(None),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_20(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = None
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_21(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " - finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_22(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path - ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_23(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + "XX: XX" + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_24(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = None
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_25(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        None,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_26(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        None,
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_27(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_28(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_29(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "XXrootsXX": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_30(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "ROOTS": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_31(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(None),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_32(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "XXfloor_pctXX": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_33(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "FLOOR_PCT": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_34(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 101,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_35(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "XXexact_base_commitXX": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_36(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "EXACT_BASE_COMMIT": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_37(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "XXcandidate_commitXX": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_38(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "CANDIDATE_COMMIT": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_39(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "XXcoverage_reportXX": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_40(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "COVERAGE_REPORT": str(report),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_41(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(None),
        },
    )
    failures.extend(name + ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_42(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(None)
    return failures


def x__candidate_failures__mutmut_43(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + ": " - message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_44(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name - ": " + message for name, message in changed.items())
    return failures


def x__candidate_failures__mutmut_45(snapshot: Path, report: Path, base: str, candidate: str) -> list[str]:
    with capture_check_evidence() as evidence:
        coverage_floor(
            {
                "roots": list(_ROOTS),
                "floor_pct": 95,
                "branch_floor_pct": 95,
                "critical_branch_files": list(_CRITICAL),
                "coverage_report": str(report),
            },
            repo_root=snapshot,
        ).run()
    failures = [finding.path + ": " + finding.message for finding in evidence.findings]
    changed = changed_line_failures(
        snapshot,
        {
            "roots": list(_ROOTS),
            "floor_pct": 100,
            "exact_base_commit": base,
            "candidate_commit": candidate,
            "coverage_report": str(report),
        },
    )
    failures.extend(name + "XX: XX" + message for name, message in changed.items())
    return failures

mutants_x__candidate_failures__mutmut['_mutmut_orig'] = x__candidate_failures__mutmut_orig # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_1'] = x__candidate_failures__mutmut_1 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_2'] = x__candidate_failures__mutmut_2 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_3'] = x__candidate_failures__mutmut_3 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_4'] = x__candidate_failures__mutmut_4 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_5'] = x__candidate_failures__mutmut_5 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_6'] = x__candidate_failures__mutmut_6 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_7'] = x__candidate_failures__mutmut_7 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_8'] = x__candidate_failures__mutmut_8 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_9'] = x__candidate_failures__mutmut_9 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_10'] = x__candidate_failures__mutmut_10 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_11'] = x__candidate_failures__mutmut_11 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_12'] = x__candidate_failures__mutmut_12 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_13'] = x__candidate_failures__mutmut_13 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_14'] = x__candidate_failures__mutmut_14 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_15'] = x__candidate_failures__mutmut_15 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_16'] = x__candidate_failures__mutmut_16 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_17'] = x__candidate_failures__mutmut_17 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_18'] = x__candidate_failures__mutmut_18 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_19'] = x__candidate_failures__mutmut_19 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_20'] = x__candidate_failures__mutmut_20 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_21'] = x__candidate_failures__mutmut_21 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_22'] = x__candidate_failures__mutmut_22 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_23'] = x__candidate_failures__mutmut_23 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_24'] = x__candidate_failures__mutmut_24 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_25'] = x__candidate_failures__mutmut_25 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_26'] = x__candidate_failures__mutmut_26 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_27'] = x__candidate_failures__mutmut_27 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_28'] = x__candidate_failures__mutmut_28 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_29'] = x__candidate_failures__mutmut_29 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_30'] = x__candidate_failures__mutmut_30 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_31'] = x__candidate_failures__mutmut_31 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_32'] = x__candidate_failures__mutmut_32 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_33'] = x__candidate_failures__mutmut_33 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_34'] = x__candidate_failures__mutmut_34 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_35'] = x__candidate_failures__mutmut_35 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_36'] = x__candidate_failures__mutmut_36 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_37'] = x__candidate_failures__mutmut_37 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_38'] = x__candidate_failures__mutmut_38 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_39'] = x__candidate_failures__mutmut_39 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_40'] = x__candidate_failures__mutmut_40 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_41'] = x__candidate_failures__mutmut_41 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_42'] = x__candidate_failures__mutmut_42 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_43'] = x__candidate_failures__mutmut_43 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_44'] = x__candidate_failures__mutmut_44 # type: ignore # mutmut generated
mutants_x__candidate_failures__mutmut['x__candidate_failures__mutmut_45'] = x__candidate_failures__mutmut_45 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__measure_side__mutmut)
def _measure_side(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_orig(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_1(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(None, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_2(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, None, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_3(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, None, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_4(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, None, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_5(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=None),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_6(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_7(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_8(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_9(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_10(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, ),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_11(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                None,
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_12(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=None,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_13(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=None,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_14(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=None,
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_15(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=None,
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_16(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_17(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_18(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_19(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_20(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_21(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(None),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_22(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(None).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_23(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(None).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_24(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text(None)
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_25(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output * "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_26(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "XXcontroller.stdout.logXX").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_27(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "CONTROLLER.STDOUT.LOG").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_28(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("XXXX")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_29(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(None)
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_30(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output * "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_31(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "XXcontroller.stderr.logXX").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_32(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "CONTROLLER.STDERR.LOG").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_33(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) - "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_34(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(None) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_35(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "XX\nXX")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_36(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                None,
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_37(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=None,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_38(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=None,
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_39(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=None,
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_40(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=None,
            ),
        )


def x__measure_side__mutmut_41(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_42(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_43(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_44(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_45(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                ),
        )


def x__measure_side__mutmut_46(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(None),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_47(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="XXmeasurementXX",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_48(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="MEASUREMENT",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_49(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name - "/controller.stdout.log",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_50(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "XX/controller.stdout.logXX",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_51(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/CONTROLLER.STDOUT.LOG",
                stderr_log=output.name + "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_52(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name - "/controller.stderr.log",
            ),
        )


def x__measure_side__mutmut_53(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "XX/controller.stderr.logXX",
            ),
        )


def x__measure_side__mutmut_54(
    snapshot: Path,
    commit: str,
    output: Path,
    execution: str,
    evidence_dir: Path,
    *,
    environment_dir: Path,
) -> tuple[Measurement | None, TransactionError | None]:
    """Run one isolated side and return its error without hiding the other side's result."""
    try:
        return (
            _fresh_measurement(snapshot, commit, output, execution, environment_dir=environment_dir),
            None,
        )
    except CoverageExecutionError as exc:
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase=exc.phase,
                stdout_log=exc.stdout_log.relative_to(evidence_dir).as_posix(),
                stderr_log=exc.stderr_log.relative_to(evidence_dir).as_posix(),
            ),
        )
    except TransactionError as exc:
        return None, exc
    except Exception as exc:
        (output / "controller.stdout.log").write_text("")
        (output / "controller.stderr.log").write_text(str(exc) + "\n")
        return (
            None,
            TransactionError(
                str(exc),
                side=output.name,
                phase="measurement",
                stdout_log=output.name + "/controller.stdout.log",
                stderr_log=output.name + "/CONTROLLER.STDERR.LOG",
            ),
        )

mutants_x__measure_side__mutmut['_mutmut_orig'] = x__measure_side__mutmut_orig # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_1'] = x__measure_side__mutmut_1 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_2'] = x__measure_side__mutmut_2 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_3'] = x__measure_side__mutmut_3 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_4'] = x__measure_side__mutmut_4 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_5'] = x__measure_side__mutmut_5 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_6'] = x__measure_side__mutmut_6 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_7'] = x__measure_side__mutmut_7 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_8'] = x__measure_side__mutmut_8 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_9'] = x__measure_side__mutmut_9 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_10'] = x__measure_side__mutmut_10 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_11'] = x__measure_side__mutmut_11 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_12'] = x__measure_side__mutmut_12 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_13'] = x__measure_side__mutmut_13 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_14'] = x__measure_side__mutmut_14 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_15'] = x__measure_side__mutmut_15 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_16'] = x__measure_side__mutmut_16 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_17'] = x__measure_side__mutmut_17 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_18'] = x__measure_side__mutmut_18 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_19'] = x__measure_side__mutmut_19 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_20'] = x__measure_side__mutmut_20 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_21'] = x__measure_side__mutmut_21 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_22'] = x__measure_side__mutmut_22 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_23'] = x__measure_side__mutmut_23 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_24'] = x__measure_side__mutmut_24 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_25'] = x__measure_side__mutmut_25 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_26'] = x__measure_side__mutmut_26 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_27'] = x__measure_side__mutmut_27 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_28'] = x__measure_side__mutmut_28 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_29'] = x__measure_side__mutmut_29 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_30'] = x__measure_side__mutmut_30 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_31'] = x__measure_side__mutmut_31 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_32'] = x__measure_side__mutmut_32 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_33'] = x__measure_side__mutmut_33 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_34'] = x__measure_side__mutmut_34 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_35'] = x__measure_side__mutmut_35 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_36'] = x__measure_side__mutmut_36 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_37'] = x__measure_side__mutmut_37 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_38'] = x__measure_side__mutmut_38 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_39'] = x__measure_side__mutmut_39 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_40'] = x__measure_side__mutmut_40 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_41'] = x__measure_side__mutmut_41 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_42'] = x__measure_side__mutmut_42 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_43'] = x__measure_side__mutmut_43 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_44'] = x__measure_side__mutmut_44 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_45'] = x__measure_side__mutmut_45 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_46'] = x__measure_side__mutmut_46 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_47'] = x__measure_side__mutmut_47 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_48'] = x__measure_side__mutmut_48 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_49'] = x__measure_side__mutmut_49 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_50'] = x__measure_side__mutmut_50 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_51'] = x__measure_side__mutmut_51 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_52'] = x__measure_side__mutmut_52 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_53'] = x__measure_side__mutmut_53 # type: ignore # mutmut generated
mutants_x__measure_side__mutmut['x__measure_side__mutmut_54'] = x__measure_side__mutmut_54 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_assure_coverage__mutmut)
def assure_coverage(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_orig(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_1(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = None
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_2(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root) and (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_3(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink() and evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_4(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(None)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_5(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() or (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_6(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() and any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_7(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_8(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(None)))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_9(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError(None)
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_10(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("XXevidence directory must be new or empty, external and not a symlinkXX")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_11(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("EVIDENCE DIRECTORY MUST BE NEW OR EMPTY, EXTERNAL AND NOT A SYMLINK")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_12(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = None
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_13(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=None, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_14(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=None)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_15(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_16(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, )
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_17(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=False, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_18(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=False)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_19(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = None
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_20(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(None, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_21(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=None, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_22(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=None, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_23(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=None)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_24(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_25(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_26(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_27(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, )
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_28(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(None)
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_29(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir * "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_30(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "XXtransaction.jsonXX").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_31(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "TRANSACTION.JSON").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_32(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) - "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_33(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(None, sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_34(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=None) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_35(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_36(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), ) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_37(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(None), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_38(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=False) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_39(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "XX\nXX")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_40(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        None
    )
    return transaction


def x_assure_coverage__mutmut_41(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir * "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_42(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "XXtransaction.jsonXX").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_43(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "TRANSACTION.JSON").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_44(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) - "\n"
    )
    return transaction


def x_assure_coverage__mutmut_45(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(None, sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_46(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=None) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_47(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(sort_keys=True) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_48(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), ) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_49(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=False) + "\n"
    )
    return transaction


def x_assure_coverage__mutmut_50(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Measure both immutable commits now under the engine-owned self profile.

    Only the trusted invocation supplies identities. This engine never resolves
    moving refs, reads candidate selectors or imports an accepted measurement.
    """
    root = root.resolve()
    if (
        evidence_dir.is_symlink()
        or evidence_dir.resolve().is_relative_to(root)
        or (evidence_dir.exists() and (not evidence_dir.is_dir() or any(evidence_dir.iterdir())))
    ):
        raise ValueError("evidence directory must be new or empty, external and not a symlink")
    evidence_dir = evidence_dir.resolve()
    evidence_dir.mkdir(parents=True, exist_ok=True)
    try:
        transaction = _execute_coverage(root, base=base, candidate=candidate, evidence_dir=evidence_dir)
    except Exception as exc:
        (evidence_dir / "transaction.json").write_text(json.dumps(_error_payload(exc), sort_keys=True) + "\n")
        raise
    (evidence_dir / "transaction.json").write_text(
        json.dumps(transaction.as_payload(), sort_keys=True) + "XX\nXX"
    )
    return transaction

mutants_x_assure_coverage__mutmut['_mutmut_orig'] = x_assure_coverage__mutmut_orig # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_1'] = x_assure_coverage__mutmut_1 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_2'] = x_assure_coverage__mutmut_2 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_3'] = x_assure_coverage__mutmut_3 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_4'] = x_assure_coverage__mutmut_4 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_5'] = x_assure_coverage__mutmut_5 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_6'] = x_assure_coverage__mutmut_6 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_7'] = x_assure_coverage__mutmut_7 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_8'] = x_assure_coverage__mutmut_8 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_9'] = x_assure_coverage__mutmut_9 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_10'] = x_assure_coverage__mutmut_10 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_11'] = x_assure_coverage__mutmut_11 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_12'] = x_assure_coverage__mutmut_12 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_13'] = x_assure_coverage__mutmut_13 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_14'] = x_assure_coverage__mutmut_14 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_15'] = x_assure_coverage__mutmut_15 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_16'] = x_assure_coverage__mutmut_16 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_17'] = x_assure_coverage__mutmut_17 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_18'] = x_assure_coverage__mutmut_18 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_19'] = x_assure_coverage__mutmut_19 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_20'] = x_assure_coverage__mutmut_20 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_21'] = x_assure_coverage__mutmut_21 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_22'] = x_assure_coverage__mutmut_22 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_23'] = x_assure_coverage__mutmut_23 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_24'] = x_assure_coverage__mutmut_24 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_25'] = x_assure_coverage__mutmut_25 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_26'] = x_assure_coverage__mutmut_26 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_27'] = x_assure_coverage__mutmut_27 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_28'] = x_assure_coverage__mutmut_28 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_29'] = x_assure_coverage__mutmut_29 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_30'] = x_assure_coverage__mutmut_30 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_31'] = x_assure_coverage__mutmut_31 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_32'] = x_assure_coverage__mutmut_32 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_33'] = x_assure_coverage__mutmut_33 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_34'] = x_assure_coverage__mutmut_34 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_35'] = x_assure_coverage__mutmut_35 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_36'] = x_assure_coverage__mutmut_36 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_37'] = x_assure_coverage__mutmut_37 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_38'] = x_assure_coverage__mutmut_38 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_39'] = x_assure_coverage__mutmut_39 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_40'] = x_assure_coverage__mutmut_40 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_41'] = x_assure_coverage__mutmut_41 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_42'] = x_assure_coverage__mutmut_42 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_43'] = x_assure_coverage__mutmut_43 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_44'] = x_assure_coverage__mutmut_44 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_45'] = x_assure_coverage__mutmut_45 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_46'] = x_assure_coverage__mutmut_46 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_47'] = x_assure_coverage__mutmut_47 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_48'] = x_assure_coverage__mutmut_48 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_49'] = x_assure_coverage__mutmut_49 # type: ignore # mutmut generated
mutants_x_assure_coverage__mutmut['x_assure_coverage__mutmut_50'] = x_assure_coverage__mutmut_50 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__execute_coverage__mutmut)
def _execute_coverage(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_orig(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_1(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = None
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_2(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(None)
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_3(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix=None) as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_4(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="XXtc-fitness-coverage-transaction-XX") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_5(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="TC-FITNESS-COVERAGE-TRANSACTION-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_6(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = None
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_7(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(None)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_8(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = None
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_9(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(None):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_10(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(None, value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_11(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", None) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_12(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_13(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", ) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_14(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"XX[0-9a-f]{40}|[0-9a-f]{64}XX", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_15(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9A-F]{40}|[0-9A-F]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_16(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is not None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_17(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError(None)
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_18(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("XXcoverage transaction requires explicit full immutable commit IDsXX")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_19(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit ids")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_20(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("COVERAGE TRANSACTION REQUIRES EXPLICIT FULL IMMUTABLE COMMIT IDS")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_21(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(None, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_22(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, None, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_23(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, None)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_24(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_25(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_26(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, )
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_27(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = None
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_28(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("XXbaseXX", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_29(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("BASE", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_30(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("XXcandidateXX", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_31(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("CANDIDATE", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_32(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = None
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_33(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch * label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_34(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(None, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_35(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, None, "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_36(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", None, "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_37(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", None, str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_38(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", None, commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_39(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), None)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_40(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git("worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_41(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_42(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_43(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_44(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_45(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), )
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_46(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "XXworktreeXX", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_47(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "WORKTREE", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_48(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "XXaddXX", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_49(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "ADD", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_50(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "XX--detachXX", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_51(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--DETACH", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_52(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(None), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_53(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(None)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_54(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = None
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_55(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir * label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_56(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append(None)

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_57(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch * (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_58(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label - "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_59(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "XX-venvXX")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_60(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-VENV")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_61(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=None, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_62(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix=None
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_63(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_64(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_65(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=3, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_66(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="XXtc-fitness-coverageXX"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_67(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="TC-FITNESS-COVERAGE"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_68(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = None
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_69(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        None,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_70(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        None,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_71(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        None,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_72(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        None,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_73(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        None,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_74(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        None,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_75(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=None,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_76(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_77(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_78(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_79(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_80(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_81(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_82(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_83(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = None
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_84(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = None
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_85(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_86(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[1]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_87(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = None
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_88(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_89(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = None
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_90(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = None
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_91(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                None, evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_92(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], None, base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_93(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", None, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_94(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, None
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_95(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_96(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_97(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_98(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_99(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[2], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_100(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir * "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_101(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "XXcandidate/measurement/coverage.xmlXX", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_102(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "CANDIDATE/MEASUREMENT/COVERAGE.XML", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_103(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("XXlinesXX", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_104(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("LINES", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_105(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "XXbranchesXX"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_106(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "BRANCHES"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_107(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = None
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_108(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(None, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_109(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, None), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_110(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_111(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, ), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_112(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(None, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_113(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, None)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_114(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_115(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, )
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_116(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = None
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_117(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(None, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_118(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, None) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_119(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr("covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_120(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, ) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_121(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" - kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_122(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "XXcovered_XX" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_123(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "COVERED_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_124(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 2
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_125(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = None
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_126(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(None, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_127(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, None) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_128(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr("covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_129(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, ) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_130(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" - kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_131(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "XXcovered_XX" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_132(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "COVERED_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_133(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 2
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_134(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit / (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_135(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total and 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_136(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 2) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_137(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) <= prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_138(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit / (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_139(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total and 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_140(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 2):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_141(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = None
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_142(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "XXlineXX" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_143(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "LINE" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_144(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind != "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_145(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "XXlinesXX" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_146(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "LINES" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_147(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "XXbranchXX"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_148(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "BRANCH"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_149(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(None)
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_150(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label - " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_151(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + "XX coverage decreased from fresh exact baseXX")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_152(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " COVERAGE DECREASED FROM FRESH EXACT BASE")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_153(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(None, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_154(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, None, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_155(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, None)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_156(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_157(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_158(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, )
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_159(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(None, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_160(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, None, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_161(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, None, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_162(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, None)
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_163(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_164(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_165(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_166(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, )
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_167(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(None))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_168(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(None)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_169(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(None):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_170(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(None, "worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_171(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, None, "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_172(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", None, "--force", str(snapshot))


def x__execute_coverage__mutmut_173(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", None, str(snapshot))


def x__execute_coverage__mutmut_174(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", None)


def x__execute_coverage__mutmut_175(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git("worktree", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_176(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_177(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "--force", str(snapshot))


def x__execute_coverage__mutmut_178(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", str(snapshot))


def x__execute_coverage__mutmut_179(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", )


def x__execute_coverage__mutmut_180(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "XXworktreeXX", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_181(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "WORKTREE", "remove", "--force", str(snapshot))


def x__execute_coverage__mutmut_182(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "XXremoveXX", "--force", str(snapshot))


def x__execute_coverage__mutmut_183(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "REMOVE", "--force", str(snapshot))


def x__execute_coverage__mutmut_184(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "XX--forceXX", str(snapshot))


def x__execute_coverage__mutmut_185(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--FORCE", str(snapshot))


def x__execute_coverage__mutmut_186(root: Path, *, base: str, candidate: str, evidence_dir: Path) -> CoverageTransaction:
    """Cleanup is part of execution and precedes publishing terminal evidence."""
    execution = str(uuid.uuid4())
    with tempfile.TemporaryDirectory(prefix="tc-fitness-coverage-transaction-") as directory:
        scratch = Path(directory)
        snapshots: list[Path] = []
        try:
            if any(re.fullmatch(r"[0-9a-f]{40}|[0-9a-f]{64}", value) is None for value in (base, candidate)):
                raise ValueError("coverage transaction requires explicit full immutable commit IDs")
            exact_checkout(root, base, candidate)
            sides = []
            for label, commit in (("base", base), ("candidate", candidate)):
                snapshot = scratch / label
                git(root, "worktree", "add", "--detach", str(snapshot), commit)
                snapshots.append(snapshot)
                output = evidence_dir / label
                output.mkdir()
                sides.append((snapshot, commit, output, scratch / (label + "-venv")))

            with concurrent.futures.ThreadPoolExecutor(
                max_workers=2, thread_name_prefix="tc-fitness-coverage"
            ) as executor:
                futures = [
                    executor.submit(
                        _measure_side,
                        snapshot,
                        commit,
                        output,
                        execution,
                        evidence_dir,
                        environment_dir=environment_dir,
                    )
                    for snapshot, commit, output, environment_dir in sides
                ]
                # Collect in base/candidate order even when candidate finishes first.
                outcomes = [future.result() for future in futures]
            errors = [error for _, error in outcomes if error is not None]
            if errors:
                raise errors[0]
            measurements = [measurement for measurement, _ in outcomes if measurement is not None]
            before, after = measurements
            failures = _candidate_failures(
                snapshots[1], evidence_dir / "candidate/measurement/coverage.xml", base, candidate
            )
            for kind in ("lines", "branches"):
                prior_total, current_total = getattr(before.counts, kind), getattr(after.counts, kind)
                prior_hit = getattr(before.counts, "covered_" + kind) if prior_total else 1
                current_hit = getattr(after.counts, "covered_" + kind) if current_total else 1
                if current_hit * (prior_total or 1) < prior_hit * (current_total or 1):
                    label = "line" if kind == "lines" else "branch"
                    failures.append(label + " coverage decreased from fresh exact base")
            exact_checkout(root, base, candidate)
            return CoverageTransaction(execution, before, after, tuple(sorted(failures)))
        finally:
            for snapshot in reversed(snapshots):
                # These are solely this transaction's detached temporary worktrees.
                git(root, "worktree", "remove", "--force", str(None))

mutants_x__execute_coverage__mutmut['_mutmut_orig'] = x__execute_coverage__mutmut_orig # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_1'] = x__execute_coverage__mutmut_1 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_2'] = x__execute_coverage__mutmut_2 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_3'] = x__execute_coverage__mutmut_3 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_4'] = x__execute_coverage__mutmut_4 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_5'] = x__execute_coverage__mutmut_5 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_6'] = x__execute_coverage__mutmut_6 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_7'] = x__execute_coverage__mutmut_7 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_8'] = x__execute_coverage__mutmut_8 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_9'] = x__execute_coverage__mutmut_9 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_10'] = x__execute_coverage__mutmut_10 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_11'] = x__execute_coverage__mutmut_11 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_12'] = x__execute_coverage__mutmut_12 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_13'] = x__execute_coverage__mutmut_13 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_14'] = x__execute_coverage__mutmut_14 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_15'] = x__execute_coverage__mutmut_15 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_16'] = x__execute_coverage__mutmut_16 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_17'] = x__execute_coverage__mutmut_17 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_18'] = x__execute_coverage__mutmut_18 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_19'] = x__execute_coverage__mutmut_19 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_20'] = x__execute_coverage__mutmut_20 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_21'] = x__execute_coverage__mutmut_21 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_22'] = x__execute_coverage__mutmut_22 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_23'] = x__execute_coverage__mutmut_23 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_24'] = x__execute_coverage__mutmut_24 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_25'] = x__execute_coverage__mutmut_25 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_26'] = x__execute_coverage__mutmut_26 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_27'] = x__execute_coverage__mutmut_27 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_28'] = x__execute_coverage__mutmut_28 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_29'] = x__execute_coverage__mutmut_29 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_30'] = x__execute_coverage__mutmut_30 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_31'] = x__execute_coverage__mutmut_31 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_32'] = x__execute_coverage__mutmut_32 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_33'] = x__execute_coverage__mutmut_33 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_34'] = x__execute_coverage__mutmut_34 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_35'] = x__execute_coverage__mutmut_35 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_36'] = x__execute_coverage__mutmut_36 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_37'] = x__execute_coverage__mutmut_37 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_38'] = x__execute_coverage__mutmut_38 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_39'] = x__execute_coverage__mutmut_39 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_40'] = x__execute_coverage__mutmut_40 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_41'] = x__execute_coverage__mutmut_41 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_42'] = x__execute_coverage__mutmut_42 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_43'] = x__execute_coverage__mutmut_43 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_44'] = x__execute_coverage__mutmut_44 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_45'] = x__execute_coverage__mutmut_45 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_46'] = x__execute_coverage__mutmut_46 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_47'] = x__execute_coverage__mutmut_47 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_48'] = x__execute_coverage__mutmut_48 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_49'] = x__execute_coverage__mutmut_49 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_50'] = x__execute_coverage__mutmut_50 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_51'] = x__execute_coverage__mutmut_51 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_52'] = x__execute_coverage__mutmut_52 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_53'] = x__execute_coverage__mutmut_53 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_54'] = x__execute_coverage__mutmut_54 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_55'] = x__execute_coverage__mutmut_55 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_56'] = x__execute_coverage__mutmut_56 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_57'] = x__execute_coverage__mutmut_57 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_58'] = x__execute_coverage__mutmut_58 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_59'] = x__execute_coverage__mutmut_59 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_60'] = x__execute_coverage__mutmut_60 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_61'] = x__execute_coverage__mutmut_61 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_62'] = x__execute_coverage__mutmut_62 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_63'] = x__execute_coverage__mutmut_63 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_64'] = x__execute_coverage__mutmut_64 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_65'] = x__execute_coverage__mutmut_65 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_66'] = x__execute_coverage__mutmut_66 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_67'] = x__execute_coverage__mutmut_67 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_68'] = x__execute_coverage__mutmut_68 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_69'] = x__execute_coverage__mutmut_69 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_70'] = x__execute_coverage__mutmut_70 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_71'] = x__execute_coverage__mutmut_71 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_72'] = x__execute_coverage__mutmut_72 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_73'] = x__execute_coverage__mutmut_73 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_74'] = x__execute_coverage__mutmut_74 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_75'] = x__execute_coverage__mutmut_75 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_76'] = x__execute_coverage__mutmut_76 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_77'] = x__execute_coverage__mutmut_77 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_78'] = x__execute_coverage__mutmut_78 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_79'] = x__execute_coverage__mutmut_79 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_80'] = x__execute_coverage__mutmut_80 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_81'] = x__execute_coverage__mutmut_81 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_82'] = x__execute_coverage__mutmut_82 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_83'] = x__execute_coverage__mutmut_83 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_84'] = x__execute_coverage__mutmut_84 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_85'] = x__execute_coverage__mutmut_85 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_86'] = x__execute_coverage__mutmut_86 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_87'] = x__execute_coverage__mutmut_87 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_88'] = x__execute_coverage__mutmut_88 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_89'] = x__execute_coverage__mutmut_89 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_90'] = x__execute_coverage__mutmut_90 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_91'] = x__execute_coverage__mutmut_91 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_92'] = x__execute_coverage__mutmut_92 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_93'] = x__execute_coverage__mutmut_93 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_94'] = x__execute_coverage__mutmut_94 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_95'] = x__execute_coverage__mutmut_95 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_96'] = x__execute_coverage__mutmut_96 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_97'] = x__execute_coverage__mutmut_97 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_98'] = x__execute_coverage__mutmut_98 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_99'] = x__execute_coverage__mutmut_99 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_100'] = x__execute_coverage__mutmut_100 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_101'] = x__execute_coverage__mutmut_101 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_102'] = x__execute_coverage__mutmut_102 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_103'] = x__execute_coverage__mutmut_103 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_104'] = x__execute_coverage__mutmut_104 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_105'] = x__execute_coverage__mutmut_105 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_106'] = x__execute_coverage__mutmut_106 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_107'] = x__execute_coverage__mutmut_107 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_108'] = x__execute_coverage__mutmut_108 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_109'] = x__execute_coverage__mutmut_109 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_110'] = x__execute_coverage__mutmut_110 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_111'] = x__execute_coverage__mutmut_111 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_112'] = x__execute_coverage__mutmut_112 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_113'] = x__execute_coverage__mutmut_113 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_114'] = x__execute_coverage__mutmut_114 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_115'] = x__execute_coverage__mutmut_115 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_116'] = x__execute_coverage__mutmut_116 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_117'] = x__execute_coverage__mutmut_117 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_118'] = x__execute_coverage__mutmut_118 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_119'] = x__execute_coverage__mutmut_119 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_120'] = x__execute_coverage__mutmut_120 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_121'] = x__execute_coverage__mutmut_121 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_122'] = x__execute_coverage__mutmut_122 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_123'] = x__execute_coverage__mutmut_123 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_124'] = x__execute_coverage__mutmut_124 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_125'] = x__execute_coverage__mutmut_125 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_126'] = x__execute_coverage__mutmut_126 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_127'] = x__execute_coverage__mutmut_127 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_128'] = x__execute_coverage__mutmut_128 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_129'] = x__execute_coverage__mutmut_129 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_130'] = x__execute_coverage__mutmut_130 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_131'] = x__execute_coverage__mutmut_131 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_132'] = x__execute_coverage__mutmut_132 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_133'] = x__execute_coverage__mutmut_133 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_134'] = x__execute_coverage__mutmut_134 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_135'] = x__execute_coverage__mutmut_135 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_136'] = x__execute_coverage__mutmut_136 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_137'] = x__execute_coverage__mutmut_137 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_138'] = x__execute_coverage__mutmut_138 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_139'] = x__execute_coverage__mutmut_139 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_140'] = x__execute_coverage__mutmut_140 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_141'] = x__execute_coverage__mutmut_141 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_142'] = x__execute_coverage__mutmut_142 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_143'] = x__execute_coverage__mutmut_143 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_144'] = x__execute_coverage__mutmut_144 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_145'] = x__execute_coverage__mutmut_145 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_146'] = x__execute_coverage__mutmut_146 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_147'] = x__execute_coverage__mutmut_147 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_148'] = x__execute_coverage__mutmut_148 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_149'] = x__execute_coverage__mutmut_149 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_150'] = x__execute_coverage__mutmut_150 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_151'] = x__execute_coverage__mutmut_151 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_152'] = x__execute_coverage__mutmut_152 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_153'] = x__execute_coverage__mutmut_153 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_154'] = x__execute_coverage__mutmut_154 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_155'] = x__execute_coverage__mutmut_155 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_156'] = x__execute_coverage__mutmut_156 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_157'] = x__execute_coverage__mutmut_157 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_158'] = x__execute_coverage__mutmut_158 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_159'] = x__execute_coverage__mutmut_159 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_160'] = x__execute_coverage__mutmut_160 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_161'] = x__execute_coverage__mutmut_161 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_162'] = x__execute_coverage__mutmut_162 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_163'] = x__execute_coverage__mutmut_163 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_164'] = x__execute_coverage__mutmut_164 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_165'] = x__execute_coverage__mutmut_165 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_166'] = x__execute_coverage__mutmut_166 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_167'] = x__execute_coverage__mutmut_167 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_168'] = x__execute_coverage__mutmut_168 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_169'] = x__execute_coverage__mutmut_169 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_170'] = x__execute_coverage__mutmut_170 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_171'] = x__execute_coverage__mutmut_171 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_172'] = x__execute_coverage__mutmut_172 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_173'] = x__execute_coverage__mutmut_173 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_174'] = x__execute_coverage__mutmut_174 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_175'] = x__execute_coverage__mutmut_175 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_176'] = x__execute_coverage__mutmut_176 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_177'] = x__execute_coverage__mutmut_177 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_178'] = x__execute_coverage__mutmut_178 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_179'] = x__execute_coverage__mutmut_179 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_180'] = x__execute_coverage__mutmut_180 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_181'] = x__execute_coverage__mutmut_181 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_182'] = x__execute_coverage__mutmut_182 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_183'] = x__execute_coverage__mutmut_183 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_184'] = x__execute_coverage__mutmut_184 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_185'] = x__execute_coverage__mutmut_185 # type: ignore # mutmut generated
mutants_x__execute_coverage__mutmut['x__execute_coverage__mutmut_186'] = x__execute_coverage__mutmut_186 # type: ignore # mutmut generated
mutants_x_main__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_main__mutmut)
def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_orig(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_1(argv: list[str] | None = None) -> int:
    parser = None
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_2(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=None)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_3(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument(None, type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_4(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=None, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_5(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=None)
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_6(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument(type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_7(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_8(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, )
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_9(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("XX--repo-rootXX", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_10(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--REPO-ROOT", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_11(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument(None, required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_12(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=None)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_13(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument(required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_14(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", )
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_15(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("XX--base-commitXX", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_16(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--BASE-COMMIT", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_17(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=False)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_18(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument(None, required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_19(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=None)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_20(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument(required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_21(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", )
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_22(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("XX--candidate-commitXX", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_23(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--CANDIDATE-COMMIT", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_24(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=False)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_25(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument(None, type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_26(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=None, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_27(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=None)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_28(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument(type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_29(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_30(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, )
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_31(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("XX--evidence-dirXX", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_32(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--EVIDENCE-DIR", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_33(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=False)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_34(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument(None, type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_35(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=None)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_36(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument(type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_37(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", )
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_38(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("XX--outputXX", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_39(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--OUTPUT", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_40(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = None
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_41(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(None)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_42(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = None
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_43(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None or (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_44(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_45(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve()) and args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_46(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists() and args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_47(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(None)
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_48(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(None)
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_49(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = ""
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_50(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError(None)
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_51(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("XXtransaction output must be a new path outside the checkoutXX")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_52(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("TRANSACTION OUTPUT MUST BE A NEW PATH OUTSIDE THE CHECKOUT")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_53(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = None
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_54(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            None,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_55(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=None,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_56(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=None,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_57(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=None,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_58(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_59(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_60(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_61(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_62(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = None
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_63(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = None
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_64(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(None)
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_65(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(None))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_66(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_67(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open(None) as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_68(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("XXxXX") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_69(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("X") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_70(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(None)
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_71(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) - "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_72(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(None, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_73(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=None) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_74(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_75(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, ) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_76(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=False) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_77(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "XX\nXX")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_78(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = None
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_79(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(None)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_80(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = None
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_81(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 3
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_82(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None or not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_83(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_84(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_85(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open(None) as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_86(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("XXxXX") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_87(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("X") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_88(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(None)
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_89(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) - "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_90(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(None, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_91(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=None) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_92(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_93(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, ) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_94(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=False) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_95(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "XX\nXX")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=True))
    return code


def x_main__mutmut_96(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(None)
    return code


def x_main__mutmut_97(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(None, sort_keys=True))
    return code


def x_main__mutmut_98(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=None))
    return code


def x_main__mutmut_99(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(sort_keys=True))
    return code


def x_main__mutmut_100(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, ))
    return code


def x_main__mutmut_101(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base-commit", required=True)
    parser.add_argument("--candidate-commit", required=True)
    parser.add_argument("--evidence-dir", type=Path, required=True)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args(argv)
    output = args.output
    try:
        if args.output is not None and (
            args.output.exists()
            or args.output.resolve().is_relative_to(args.repo_root.resolve())
            or args.output.resolve().is_relative_to(args.evidence_dir.resolve())
        ):
            output = None
            raise ValueError("transaction output must be a new path outside the checkout")
        result = assure_coverage(
            args.repo_root,
            base=args.base_commit,
            candidate=args.candidate_commit,
            evidence_dir=args.evidence_dir,
        )
        payload = result.as_payload()
        code = int(bool(result.failures))
        if output is not None:
            with output.open("x") as stream:
                stream.write(json.dumps(payload, sort_keys=True) + "\n")
            return code
    except Exception as exc:
        payload = _error_payload(exc)
        code = 2
        if output is not None and not output.exists():
            try:
                with output.open("x") as stream:
                    stream.write(json.dumps(payload, sort_keys=True) + "\n")
                return code
            except OSError:
                pass
    print(json.dumps(payload, sort_keys=False))
    return code

mutants_x_main__mutmut['_mutmut_orig'] = x_main__mutmut_orig # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_1'] = x_main__mutmut_1 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_2'] = x_main__mutmut_2 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_3'] = x_main__mutmut_3 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_4'] = x_main__mutmut_4 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_5'] = x_main__mutmut_5 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_6'] = x_main__mutmut_6 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_7'] = x_main__mutmut_7 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_8'] = x_main__mutmut_8 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_9'] = x_main__mutmut_9 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_10'] = x_main__mutmut_10 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_11'] = x_main__mutmut_11 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_12'] = x_main__mutmut_12 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_13'] = x_main__mutmut_13 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_14'] = x_main__mutmut_14 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_15'] = x_main__mutmut_15 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_16'] = x_main__mutmut_16 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_17'] = x_main__mutmut_17 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_18'] = x_main__mutmut_18 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_19'] = x_main__mutmut_19 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_20'] = x_main__mutmut_20 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_21'] = x_main__mutmut_21 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_22'] = x_main__mutmut_22 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_23'] = x_main__mutmut_23 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_24'] = x_main__mutmut_24 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_25'] = x_main__mutmut_25 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_26'] = x_main__mutmut_26 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_27'] = x_main__mutmut_27 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_28'] = x_main__mutmut_28 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_29'] = x_main__mutmut_29 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_30'] = x_main__mutmut_30 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_31'] = x_main__mutmut_31 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_32'] = x_main__mutmut_32 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_33'] = x_main__mutmut_33 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_34'] = x_main__mutmut_34 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_35'] = x_main__mutmut_35 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_36'] = x_main__mutmut_36 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_37'] = x_main__mutmut_37 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_38'] = x_main__mutmut_38 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_39'] = x_main__mutmut_39 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_40'] = x_main__mutmut_40 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_41'] = x_main__mutmut_41 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_42'] = x_main__mutmut_42 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_43'] = x_main__mutmut_43 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_44'] = x_main__mutmut_44 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_45'] = x_main__mutmut_45 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_46'] = x_main__mutmut_46 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_47'] = x_main__mutmut_47 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_48'] = x_main__mutmut_48 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_49'] = x_main__mutmut_49 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_50'] = x_main__mutmut_50 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_51'] = x_main__mutmut_51 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_52'] = x_main__mutmut_52 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_53'] = x_main__mutmut_53 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_54'] = x_main__mutmut_54 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_55'] = x_main__mutmut_55 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_56'] = x_main__mutmut_56 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_57'] = x_main__mutmut_57 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_58'] = x_main__mutmut_58 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_59'] = x_main__mutmut_59 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_60'] = x_main__mutmut_60 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_61'] = x_main__mutmut_61 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_62'] = x_main__mutmut_62 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_63'] = x_main__mutmut_63 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_64'] = x_main__mutmut_64 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_65'] = x_main__mutmut_65 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_66'] = x_main__mutmut_66 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_67'] = x_main__mutmut_67 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_68'] = x_main__mutmut_68 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_69'] = x_main__mutmut_69 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_70'] = x_main__mutmut_70 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_71'] = x_main__mutmut_71 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_72'] = x_main__mutmut_72 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_73'] = x_main__mutmut_73 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_74'] = x_main__mutmut_74 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_75'] = x_main__mutmut_75 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_76'] = x_main__mutmut_76 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_77'] = x_main__mutmut_77 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_78'] = x_main__mutmut_78 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_79'] = x_main__mutmut_79 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_80'] = x_main__mutmut_80 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_81'] = x_main__mutmut_81 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_82'] = x_main__mutmut_82 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_83'] = x_main__mutmut_83 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_84'] = x_main__mutmut_84 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_85'] = x_main__mutmut_85 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_86'] = x_main__mutmut_86 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_87'] = x_main__mutmut_87 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_88'] = x_main__mutmut_88 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_89'] = x_main__mutmut_89 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_90'] = x_main__mutmut_90 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_91'] = x_main__mutmut_91 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_92'] = x_main__mutmut_92 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_93'] = x_main__mutmut_93 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_94'] = x_main__mutmut_94 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_95'] = x_main__mutmut_95 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_96'] = x_main__mutmut_96 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_97'] = x_main__mutmut_97 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_98'] = x_main__mutmut_98 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_99'] = x_main__mutmut_99 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_100'] = x_main__mutmut_100 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_101'] = x_main__mutmut_101 # type: ignore # mutmut generated
